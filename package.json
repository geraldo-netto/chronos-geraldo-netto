{
  "name": "cinnamon-chronos",
  "version": "0.0.1",
  "description": "Development tooling for Cinnamon Chronos, the Chronos Calendar Cinnamon applet: the test suites, the coverage gate and the linters. The applet itself ships from files/.",
  "license": "GPL-2.0-or-later",
  "author": "Geraldo Netto <geraldonetto@gmail.com> (https://github.com/geraldo-netto)",
  "contributors": [
    "Claus Colloseus (ccprog) <ccprog@gmx.de> — original calendar@ccprog applet",
    "Simon Wiles (simonwiles) — original calendar@simonwiles.net applet"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/geraldo-netto/cinnamon-chronos.git"
  },
  "bugs": {
    "url": "https://github.com/geraldo-netto/cinnamon-chronos/issues"
  },
  "homepage": "https://github.com/geraldo-netto/cinnamon-chronos#readme",
  "private": true,
  "scripts": {
    "lint": "npm run lint:js && npm run lint:py",
    "lint:js": "eslint .",
    "lint:py": "python3 -m pyflakes files/chronos@geraldo-netto test || { status=$?; if python3 -c 'import pyflakes' 2>/dev/null; then exit $status; fi; echo 'pyflakes is not installed: pip install pyflakes (the Python lint is a gate, not an optional extra)'; exit 1; }",
    "i18n:check": "node scripts/check-i18n.mjs",
    "package:spices": "node scripts/package-spices.mjs",
    "release:bump": "node scripts/release.mjs bump",
    "release:check": "node scripts/release.mjs check",
    "test": "npm run test:js && npm run test:py",
    "test:js": "node test/helpers/coverage.js",
    "test:py": "python3 test/helpers/coverage.py",
    "test:js:raw": "node --test --experimental-test-coverage --test-coverage-include='files/chronos@geraldo-netto/*.js' --test-coverage-include='files/chronos@geraldo-netto/5.4/*.js' --test-coverage-include='scripts/*.mjs' --test-coverage-lines=98 --test-coverage-branches=90 --test-coverage-functions=100 test/*.test.js",
    "test:py:raw": "python3 -m unittest discover -s test -p '*.py'"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.7.0",
    "espree": "^11.2.0"
  }
}
