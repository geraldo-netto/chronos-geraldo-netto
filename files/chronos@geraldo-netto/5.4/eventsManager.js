/* global imports */

const EventsManagerModule = imports.ui.appletManager.applets["chronos@geraldo-netto"].eventsManager;

if (typeof module !== "undefined") {
    module.exports = EventsManagerModule;
}
