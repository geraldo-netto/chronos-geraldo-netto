/* global imports */

const HolidayConstants = imports.ui.appletManager.applets["chronos@geraldo-netto"].holidayConstants;

if (typeof module !== "undefined") {
    module.exports = HolidayConstants;
}
