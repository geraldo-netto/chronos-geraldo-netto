# cinnamon-chronos TODO

## Findings

| ID | Category | Severity | Status | Effort | Description | Notes |
|----|----------|----------|--------|--------|-------------|-------|
| T569 | localization/i18n; CI; release engineering | Medium | open | S | Regenerate and commit the translation template so the packaging gate is green. | **[verified]** `npm run i18n:check` fails at the byte-current comparison (`translation template is stale at line 60`). Regenerating in a temporary copy changes only the POT creation timestamp and the `appletMenuBuilder.js` source references for the existing “Go to today” and “Date and Time Settings” messages; the catalogs pass validation/currentness and no msgid changed. |
| T578 | configuration discoverability; localization/i18n; UI / UX; documentation | Medium | open | M | Add settings for enabling religious observances and selecting religions, and document the local-data behavior and date limitations. | **[verified]** The settings schema exposes only country/region public-holiday controls. Add a master switch and one switch per supported religion, bind changes to repaint the calendar without restarting, translate user-visible religion and observance names, update the README/privacy disclosure, regenerate the translation template, and cover schema/runtime parity. Decide the grid styling before enabling: the annotator strips `calendar-work-day` from every annotated cell, but religious observances are not days off, and a merged public+religious entry is indistinguishable by flags from a religious-only one (public rows may carry empty flags), so the religious/public distinction must be encoded before the annotator can style them differently. |
| T579 | security; memory and CPU management; robustness / recovery | High | open | M | Bound and sanitize calendar-event UIDs at the server boundary, including removal signals and log messages. | **[verified]** `eventData.js:111-125` retains an arbitrary string UID and logs it verbatim on invalid times; `eventIndex.js:29-38` stores IDs in a set and map. The 4 MiB signal limit in `eventsManager.js:227-257` is released after each batch, so sequential signals can retain up to 2,000 near-limit IDs, while `eventsManager.js:467-478` queues removal strings with no byte limit. Add one UID contract, omit raw IDs from logs, collapse oversized removals to an authoritative resync, and test sequential large IDs plus CR/LF and removal payloads. |
| T580 | security; memory and CPU management; persistence integrity | High | open | M | Bound and normalize holiday flags before span expansion and cache serialization. | **[verified]** `holidayRecord.js:70-80` requires only that `flags` be an array; `holidayServiceAdapters.js:126-142` passes a remote array through, and a 366-day span reuses it across 367 rows before `holidays.js:370-378` synchronously serializes the expanded cache. A near-4-MiB accepted response can therefore amplify toward gigabytes on Cinnamon's compositor thread. Limit flag count/type/length, cap serialized output as defense in depth, and add an adversarial spanning-holiday regression. |
| T581 | reliability / correctness; configuration integrity; UI / UX; test coverage | Medium | open | S | Propagate `show-events` setting changes to the calendar grid as well as the event column. | **[verified]** `calendar.js:1018-1022` initializes `events_enabled` and recomputes it only for manager-ready/calendar-change signals; `applet.js:375-384` applies the setting only through `AppletEventListCoordinator`, whose `apply()` hides the column and attempts a now-inactive fetch. After events are loaded, disabling the setting can leave cached event dots visible. Recompute grid event state, cancel obsolete work on disable, refresh on re-enable, and test the off/on transition with populated data. |
| T582 | reliability / correctness; UI / UX; test coverage | Medium | open | S | Reformat existing event rows when Cinnamon's 12/24-hour preference changes. | **[verified]** `eventView.js:302-308,854-865` snapshots `desktopSettings.use24h` into each row. The desktop signal reaches `applet.js:371-384`, but `appletCoordinators.js:117-122` refreshes the selected date only when `show-events` changes, so open rows retain the old format. Update row format state and repaint it; test `14:00` becoming `2:00 PM` through the real callback path without rebuilding the list. |
| T583 | robustness / recovery; settings integrity; test coverage | Medium | open | M | Normalize malformed saved world-clock values before the Cinnamon settings list loads them. | **[verified]** `settings_widgets_common.py:832-847` delegates directly to Cinnamon's real `JSONSettingsList`; malformed top-level values or rows such as `null`, `[null]`, or a numeric timezone raise `TypeError` during its attach/model-load path and can break the settings window. Runtime JS already filters malformed rows, but `test_settings_widgets_core.py:112-131` uses a fixture that merely counts list length and never exercises the inherited loader. Validate object/string fields, discard or repair bad rows, and make the fixture cover typed model loading. |
| T584 | robustness / recovery; lifecycle; localization/i18n; test coverage | Medium | open | S | Restart a degraded locale query when an applet is re-added after the last consumer canceled its retry. | **[verified]** `localeQuery.js:198-233` marks a failed environment degraded and permits only its forced retry; `localeQuery.js:103-110` cancels that timer when the last consumer leaves. A later normal registration remains blocked by the degraded gate despite unused attempts, leaving defaults for the process lifetime. Restart below the attempt cap on the 0-to-1 consumer transition and test fail, remove, re-register, then recover. |
| T585 | localization/i18n; asynchronous state; test coverage | Medium | open | M | Update holiday provider language when the asynchronous locale query settles. | **[verified]** `holidays.js:52,95-107,586-603` calls the lazy locale getter during provider construction and permanently passes that initial value into `HolidayRecordContract` and `OpenHolidaysServiceAdapter`; there is no language-change listener. The first instance in a non-English session can therefore keep English request/localization state after the real locale arrives. Resolve language per fetch or rebuild/invalidate on change, and test a deferred non-English response without applet reload. |
| T586 | localization/i18n correctness; configuration integrity; test coverage | Medium | open | S | Derive holiday display/request language from the message locale rather than `LC_ADDRESS`. | **[verified]** `holidayRecord.js:23`, `holidayServiceAdapters.js:54`, and `holidays.js:52` use the address-format locale for language choice. With a French UI and Italian regional formats, the otherwise French applet requests and selects Italian holiday names. Use `LC_MESSAGES` or `GLib.get_language_names()` for UI language, keep regional inference separate, and test deliberately different message/address locales. |
| T587 | dependency management; security; CI | Medium | open | S | Refresh the lockfile past the vulnerable `brace-expansion@5.0.7` and automate dependency-audit monitoring. | **[verified]** `npm audit --json` reports [GHSA-mh99-v99m-4gvg](https://github.com/advisories/GHSA-mh99-v99m-4gvg), High/CVSS 7.5 denial of service through unbounded expansion, on the dev-tool path `eslint` to `minimatch` to `brace-expansion`. A fix is available through `minimatch@10.2.6` and `brace-expansion@5.0.8`. Refresh the lock, require a clean audit, and keep the monitor scoped to development dependencies versus shipped code. |
| T588 | release engineering; documentation; test coverage | Medium | open | S | Make the documented release sequence package the version-bumped bytes. | **[verified]** `README.md:130-142` runs `release:bump` and then `package:spices` before staging or committing. The bump edits the working tree, while `package-spices.mjs:60-64,145-160` enumerates and reads Git-index blobs, so following the instructions packages pre-bump metadata. Commit/stage before packaging or reject index/worktree divergence, and add an end-to-end regression for the documented sequence. |
| T589 | test coverage; architecture boundaries; CI | Medium | open | M | Derive JavaScript coverage inputs recursively from the shipped-source manifest. | **[verified]** `test/helpers/coverage.js:35-49,59-61` hard-codes the applet root, `5.4/`, and flat `scripts/` with non-recursive reads and include globs. A future `6.0/` tree or nested shipped module can therefore receive zero coverage while the per-file gate passes. Discover shipped JS recursively, generate coverage includes from the same manifest, and test that an unloaded nested/versioned fixture fails the gate. |
| T590 | process and delivery governance; documentation; CI | Medium | open | M | Align the documented branch/release history with the actual upstream repository. | **[verified]** On 2026-07-29, `git ls-remote --heads --tags origin` returned only `refs/heads/develop`: no `main` and no `v*` tag. This conflicts with `README.md:144-149`, the inert `main` trigger in `ci.yml:14`, and the dated `0.0.1` release/link in `CHANGELOG.md:26,35-36`. Either create/protect the documented branch and publish the intended tag/release, or revise CI, changelog, and release docs to describe a develop-only unreleased state. |
| T591 | release engineering; provenance; CI; test coverage | Medium | open | M | Enforce annotated tags and release-branch ancestry before publishing a release. | **[verified]** `.github/workflows/ci.yml:110-112` accepts any `v*` tag, while `release.mjs:284-288` checks only that its text equals the manifest version. A correctly named lightweight tag on an unmerged commit can therefore publish despite the documented merged-`main` requirement. Fetch the release branch, verify ancestry and annotated-tag object type, and add negative tests for lightweight and off-branch tags. |
| T592 | documentation; configuration discoverability; developer experience | Medium | open | S | Document a Linux Mint Node.js installation path that satisfies the project's Node 22.13 floor. | **[verified]** `README.md:51` tells Mint contributors to run `sudo apt install nodejs npm`, but Mint 22.3's configured repository offers Node 18.19.1 while `package.json:35` requires `>=22.13.0`. Point to a maintained version-manager or official-binary route and require checking `node --version` before `npm ci`. |
| T593 | platform compatibility; CI; documentation | Low | open | S | Refresh or relabel the CI lane described as testing the “current” Node and Python versions. | **[verified]** `ci.yml:25-28,33-34` and `README.md:123` call Node 24/Python 3.12 current, while the official releases on 2026-07-29 are Node 26 Current and Python 3.14 stable. The project contracts are open-ended, so regressions in newer supported runtimes can go unnoticed. Test the current releases, add a periodically refreshed lane, or narrow the wording/support contract. |
| T594 | release reproducibility; documentation; test coverage | Low | open | S | Normalize archive metadata so repeated packaging produces the documented deterministic tarball. | **[verified]** `.github/workflows/ci.yml:92-96` sorts checksum inputs but invokes `tar` without normalized ordering, timestamps, ownership, or group; the packager creates files with fresh mtimes. Controlled equal-content trees with different mtimes produced different tar SHA-256 values, contrary to `README.md:155-157`. Normalize archive metadata and assert byte-identical archives from independently staged equal trees. |
| T595 | robustness / recovery; UI / UX; test coverage | Low | open | S | Contain `gnome-calendar` spawn failures on date-launch actions. | **[verified]** `eventView.js:107-118` calls the throwing `Util.trySpawn()` without protection after a cached PATH check; removal, permission changes, or process-creation failure then escapes the date-heading/no-events-button signal handler. Sibling `launchUuid()` already catches the same failure at `eventView.js:136-143`. Catch, log a sanitized message, return false, and test a throwing date spawn with no launched signal. |
| T596 | process and delivery governance; documentation; test coverage | Low | open | S | Restore unique stable IDs in the rejected-findings ledger and enforce ID uniqueness. | **[verified]** `TODO.md` assigns `R12` both to the religious-holiday mutant analysis and to the timezone-independence rejection. Renumber one row without changing its decision, and extend the ledger/static test to reject duplicate finding, rejection, decision, or merge-choice identifiers. |
| T598 | test coverage; CI | Medium | open | S | Make test discovery recursive in both suites so a test file in a subdirectory cannot silently never run. | **[verified]** `package.json:31-32` and `test/helpers/coverage.js:69` invoke the JS runner with `test/*.test.js` (the glob does not cross `/`), and `test/helpers/coverage.py:244` uses `unittest` `discover(pattern="*.py")` with no `__init__.py` anywhere under `test/`, so a contributor-added `test/integration/foo.test.js` or `test/widgets/test_bar.py` — even a failing one — never executes while `npm test` and both coverage gates stay green. The complexity gate already scans test subdirectories recursively (`test/complexity.test.js:78-86`), so nested test files are lint-checked but never run. Distinct from T589, which covers shipped-source coverage inputs. Use recursive patterns (and Python package markers or a recursive loader) or add a gate asserting every `*.test.js`/`test_*.py` under `test/` actually executed. |
| T599 | reliability / correctness; UI / UX; state machine integrity | Medium | open | S | Refresh the open event column when an event is rescheduled off the selected day. | **[verified]** With the menu open and an event listed for the selected day, an external reschedule to another day inside the 42-day window makes `eventIndex.js:125-198` rewrite buckets but report `selected_changed` only for the new date, so `selected-date-events-changed` never fires and `appletMenuBuilder.js:186-191` (the column's only listener) keeps the stale row while the grid dot moves — grid and column contradict each other until reselect or midnight reload. The moved-out-of-window branch (`eventIndex.js:165-174`) and the removal path (`eventsManager.js:487`) both handle their cases; the in-window move is the one gap, and the existing "moved event repaints" test moves an event within the same day only. Repro confirmed the emission counts. Propagate `selected_changed: true` when the selected date's bucket loses the event and add a cross-day-move regression. |
| T602 | robustness / recovery; localization/i18n | Medium | open | S | Treat a corrupt translation catalog like a missing one so it cannot abort the whole settings window. | **[verified]** `settings_i18n.py:16-29` calls `gettext.translation(..., fallback=True)` at module import, but `fallback=True` covers only missing files: a truncated or corrupt user-local `.mo` (interrupted install, disk-full) raises `OSError: Bad magic number` — reproduced with a garbage catalog. Cinnamon's `create_custom_widget` wraps `exec_module` in `except KeyError:` only, so the import-time OSError kills the entire xlet settings window (both `settings_widgets.py:27` and `settings_about.py:26` import the module) until the user deletes a file they do not know exists; Cinnamon's own `translate()` guards the same call class. Tests mock `gettext.translation` only with valid returns. Wrap each call in `try/except OSError`, fall through to the next directory then NullTranslations, and log the path once. |
| T603 | state machine integrity; UI / UX | Low | open | S | Keep the holiday pending marker until every in-flight month settles. | **[verified]** `calendar.js:647-663,721-727,738-769`: `awaited` is a local counter never re-read after the synchronous dispatch loop, and a success callback runs `setStatus("", provider)` unconditionally, so when a year-straddling grid issues two fetches the first completion erases the "loading…" marker while the second is still in flight — a slow or hanging January fetch then looks identical to a month with no holidays, the exact confusion the marker's own comment says it exists to prevent, and nothing re-arms it. The only pending-marker test uses a single month. Track outstanding months per `beginUpdate` (generation-guarded), keep the marker while the count is nonzero with errors still winning immediately, and test two async months settling apart. |
| T606 | reliability / correctness; performance | Low | open | S | Record the resolved-location key on the startup path so the first settings change does not re-issue the geocode. | **[verified]** `weather.js:176-196` (`schedule()`, the `on_applet_added_to_panel` path) resolves and caches the location but leaves `_resolved_location_key` empty — the key is written only in `queue()` (`weather.js:208-226`) — so the first weather-settings change after startup (units toggle, show-weather) sees a key mismatch in `_forgetIfLocationChanged`, drops the cached geocode, and re-issues the geocoding HTTP request, exactly the two-round-trips-per-toggle behavior the method's comment and the units-toggle regression test forbid; that test initializes via `queue()`, so the shipped startup-then-toggle sequence is never exercised. Record the key in `schedule()`/`refresh()` (or treat the initial empty key as unknown) and re-run the regression starting from `schedule()`. |
| T607 | memory and CPU management; security | Low | open | S | Project geocoder hits to the app-owned place record before caching. | **[verified]** `weatherServiceAdapters.js:342-355` copies every Open-Meteo hit field into the place record via `Object.assign`, and `weatherProviders.js:177-186,208-216` pins it in the geocode cache for applet lifetime (16 entries per resolver, two resolvers per applet), so a compromised or broken geocoder returning multi-megabyte junk fields under the 4 MiB response cap can retain on the order of 128 MiB inside the compositor while consumers only ever read latitude/longitude. `nominatimGeocodePlace` (`weatherServiceAdapters.js:402-424`) already projects to a minimal record, so the asymmetry is unintentional, and tests assert only coordinates/name. Return the same minimal projection from the Open-Meteo path, dropping unknown provider fields before caching. |
| T608 | state machine integrity; UI / UX | Low | open | S | Clear the tooltip-format warning when the fixed format can no longer be validated by a rendered stamp. | **[verified]** `appletPanelStatus.js:397-440,672-683,749-761`: `_tooltipFormatIssue` is cleared only inside `tooltipClockStamp()` on a successfully rendered stamp, and with `show-worldclocks` off `updateClockAndDate` passes empty clock entries, so a user who sets a bad `custom-tooltip-format`, disables world clocks, then fixes the format keeps the "Invalid time format; edit it in Settings" footer for the rest of the session while `issueStatus()` re-folds the stale flag on every menu refresh. The analogous panel `_formatIssue` is safe because `custom-format` participates in `_formatSignature`. Existing tests cover set-and-recover only via the panel path or with entries present. Validate the configured tooltip format once per `updateClockAndDate`, or clear the issue whenever no entries are renderable; test fix-while-clocks-off. |
| T609 | reliability / correctness; localization/i18n; robustness / recovery | Low | open | S | Fall back to the untranslated date formats when a translated strftime msgstr fails to render. | **[verified]** `appletPanelStatus.js:704-708` calls `.capitalize()` on the unchecked result of `formatClock` over `DATE_FORMAT_SHORT`/`DATE_FORMAT_FULL` (`dateFormats.js:52-55`), which are translator-supplied msgstrs in 15 catalogs; a broken directive makes `WallClock.get_clock_for_format()` return null, so the menu's day/date header throws on every open for that locale, permanently blank plus the generic runtime error. The project already treats translator strftime as untrusted (`badFormatFallback`, `appletPanelStatus.js:126-130`, added because xgettext does not mark these c-format) but not for these two formats, and the i18n gates check fuzz/completeness/freshness, never msgstr directive validity. Fall back per format to the known-valid untranslated msgid on null/empty, or add an i18n gate that renders each translated strftime msgstr. |
| T610 | robustness / recovery; lifecycle | Low | open | S | Kill the in-flight `locale` child when the last consumer cancels its query. | **[verified]** `localeQuery.js:88-110,287-304`: `cancelPendingLocaleQueries()` first removes all pending timers — including the armed 5-second deadline whose closure is the only holder of `proc` that calls `force_exit()` — then `_cancelPendingRequest` cancels only the read cancellable, which aborts the read but leaves the child running. Removing the last applet instance while `locale -k LC_TIME` is wedged on a hung NSS lookup (the module's own documented case) therefore leaves the wedged child for the rest of the session, exactly what `_armDeadline`'s comment says must not happen. Distinct from T584, which is about the degraded-state gate; this is the abandoned child process itself. Track the `Gio.Subprocess` per env alongside its cancellable and `force_exit()` it in `_cancelPendingRequest`; test cancel-while-wedged. |
| T611 | reliability / correctness; configuration integrity | Low | open | M | Align the settings dialog's local-timezone identity with the applet runtime so reserved-zone validation matches. | **[verified]** `timezone_data.py:40-57,125-128,203-223` derives the local zone by fully resolving `/etc/localtime` and never consults `TZ`, while `worldclockData.js:400-414,433-451` derives the reserved key from `GLib.TimeZone.new_local()` (`TZ` first, unresolved link name). With an alias symlink (`/etc/localtime → Asia/Calcutta`, resolved by Python to `Asia/Kolkata`) or a `TZ` override, the dialog's `is_reserved` misses the zone, previews and saves the clock, and the applet's `selectUserClocks` silently drops it — the exact validate-preview-save-disappear failure `is_reserved`'s docstring says it exists to prevent. Tests patch `local_timezone_name` outright, so no divergent identity is modeled. Mirror the runtime derivation (TZ first, unresolved link name) and/or reserve both resolved and unresolved names. |
| T612 | performance; wiring gaps | Low | open | S | Share one lazily-built timezone resolver between the weather entry and the clocks list. | **[verified]** `settings_widgets_common.py:269-278,324,832-858`: `WeatherLocationEntry.__init__` calls `weather_cities()`, which builds a `TimezoneResolver` (the ~594-zone map plus casefold sort) on the GTK main thread at every settings-window construction regardless of the `show-weather` revealer state — xlet-settings builds every page up front — defeating the documented lazy `timezone_resolver` on `ClocksList`, which later builds a second, separate resolver instead of reusing the first. `_COMPLETION_MODELS`/`_CITY_MODELS` memoize the Gtk stores but not the resolver. Defer `weather_cities()` to the entry's first focus/keystroke, or share one lazily-built module-level resolver. |
| T613 | release engineering; robustness / recovery | Low | open | S | Ignore and clean up crash-orphaned release temporary files. | **[verified]** `release.mjs:194-203,116-123` writes `<target>.chronos-release-<pid>.tmp` before the atomic rename, but `.gitignore:167-169` covers only basenames beginning `.chronos-release-` (lock and transaction names) and `cleanupReleaseStaging` removes only `.chronos-release-transaction-*` directories, so a SIGKILL or power loss between write and rename leaves e.g. `package.json.chronos-release-<pid>.tmp` that recovery never deletes (new pid, new tmp names) and `git add .` before the release commit stages. Add a `*.chronos-release-*.tmp` gitignore entry and have `cleanupReleaseStaging` delete stale tmp files beside each RELEASE_TARGET under the lock. |
| T614 | test coverage; code duplication | Low | open | S | Remove or de-duplicate the non-gating raw test scripts. | **[verified]** `package.json:31-32` defines `test:js:raw`/`test:py:raw`, referenced nowhere else (grep across md/yml/json hits only package.json). The JS variant enforces only Node's aggregate thresholds — the exact bypass `test/helpers/coverage.js:17-27` documents ("one file at 50 % disappears into the average") — and omits the per-file 100 %-line overrides and the never-loaded-file check; the Python variant runs with no coverage at all; and both hand-duplicate include globs/thresholds from coverage.js, so the copies silently diverge. Delete both scripts, or derive their flags from a shared constant and document them as explicitly non-gating. |
| T615 | documentation; CI | Low | open | S | Reconcile the README's "every push" claim with the workflow's push triggers. | **[verified]** `README.md:251-255` states the gates run "on every push and pull request", but `ci.yml:12-16` triggers pushes only for `develop`/`main` and `v*` tags, so a contributor pushing a feature branch without opening a PR runs nothing and the regression surfaces only when the PR opens. Distinct from T590, which concerns the nonexistent upstream `main` branch and tag. Widen the push trigger (e.g. all branches) or reword the README to name the actual trigger set. |

## Rejected

| id | finding | why rejected |
|----|---------|--------------|
| R01 | `dtEquals` duplicated between `eventData.js` and `eventFormat.js`. | Deliberate boundary: `eventFormat.js` stays free of GJS imports so it is Node-pure. Centralizing re-couples the modules for a one-line function. (Re-raised by the 2026-07-13 architecture scan; re-rejected.) |
| R05 | Barrel re-exports as the GJS `var` bindings importers need. | The re-exports **are** the import mechanism; tests reach the modules through them. |
| R06 | `eventView.js:70`: commented-out `Util.trySpawn(["gnome-calendar"])`. | Deliberate documented switch with a maintainer note directly above it. |
| R08 | No watchdog/metrics/circuit-breakers/health surface. | For a panel applet these are reasonable omissions; the provider failover with last-success ordering plus a per-year freshness gate is a coherent substitute. |
| R09 | Per-calendar colour strip conveys calendar identity by colour alone. | Not actionable here: `cinnamon-calendar-server` sends the calendar's colour and never its display name, so there is no name to announce. The strip is `Atk.Role.SEPARATOR` so a screen reader skips it. Needs an upstream change. |
| R10 | Panel label written with no compare-before-assign. | **[verified]** The tick is not 1 Hz: `WallClock` emits `notify::clock` only when the rendered string changes (0 emissions in 10 s with `%H:%M`), so the handler never sees the same string twice. |
| R12 | Six `religiousHolidays.js` mutants survive the operator/constant audit (computus f-term `+8→+9`, month-expression `7m→8m`, `_easterDate` hour literal, monthMap range guard ×3). | **Equivalent mutants.** Whole-domain witness searches over years 1–9999 found no distinguishing input for either computus mutant; the hour literal only dodges DST and never reaches the extracted month/day; the month-range guard is observationally subsumed by the per-row `row.month !== numericMonth` filter and kept only to skip a pointless year expansion. Every killable mutant (81/87) is pinned, most by the five-century anchor table with witness years 151/1700/3165. |
| R11 | `validDateParts`' **year** and **day** integer guards look unasserted (both mutants survive). | **Equivalent mutants.** An exhaustive witness search over 15 hostile value types found no distinguishing input: `d.getFullYear() === parts.year` and `d.getDate() === parts.day` compare strictly against the raw value, so the roundtrip check subsumes them. Only the **month** guard, whose comparison coerces (`"5" - 1`), is load-bearing; its regression coverage landed with T470. |
| R12 | The suite depends on the host timezone. | Ran the full JS suite under `TZ=Pacific/Auckland`, `TZ=America/Sao_Paulo`, `TZ=UTC`: 782/782 in all three. `test/helpers/clock.js` freezes the wall clock and the tests pin `LOCAL_TIMEZONE` explicitly. |
| R13 | `metadata.json` is missing `icon` / `author`. | Cinnamon's `requiredProperties` for an applet are `uuid`, `name`, `description`; `author` belongs in `info.json` per the Spices spec, and is there. `max-instances: -1` is valid. |
| R14 | The `5.4/` shims are indirection; the root modules "depend on files that only exist in the parent". | Intended and correct on every declared version: `appletManager.js:61` roots `imports.ui.appletManager.applets[uuid]` at the *applets folder*, so `[uuid]` is the parent dir. The seven-line shims work, `gjs_import.test.js` pins them, and the former `require()` host-detection risk was resolved with T464. |
| R15 | `settings_widgets_common.py` at 965 lines is a god module. | Many concerns, but they are all "the settings dialog"; the gi-free half is already extracted into `timezone_data.py`, and no change was identified that becomes dangerous because of the co-location. |
| R16 | `metadata.json` omits Cinnamon 6.6, so the applet is incompatible with the installed desktop. | Rejected after checking Cinnamon 6.6.7's loader: `cinnamon-version` entries are minimum compatible versions, and the declared 5.4 entry admits 6.6. The compatibility test now models that loader rule directly. |
| R17 | Sonar `css:S4654`: `icon-size` is an unknown CSS property. | **False positive.** Cinnamon's shipped `/usr/share/cinnamon/theme/cinnamon.css` uses the same property throughout; it is an St theme property, not browser CSS. The applet's `icon-size: 4em` is valid Cinnamon styling. |
| R18 | Sonar's nine analyzer-labelled bugs: unused `Tooltip` constructions, an unmodified holiday iterator/limit, implicit string sort, empty-matching URL regex and an unseeded `reduce`. | **False positives.** Cinnamon `TooltipBase._init` registers actor/stage callbacks holding the tooltip instance; `Date.setTime` mutates `iter`; `limit` is intentionally constant; package paths intentionally sort lexicographically; `urlForLog` intentionally accepts an empty optional path; and `joinPhrases` returns before `reduce` when the array is empty. Existing tests exercise each behavior. |
| R19 | Sonar's two vulnerabilities in `test/helpers/appletFixture.js` and `test/helpers/coverage.js`. | **False positives outside shipped code.** `"/tmp/cache"` is a return value in an in-memory GLib test double, not a filesystem write, and the coverage harness invokes the same developer/CI `node` selected by the already-trusted process environment. They do not affect the shipped applet or its local production gates; the related Sonar reconfiguration was rejected in R22. |
| R20 | Sonar's mass `var`, object-spread, class-field and test-stub findings. | Not defects. All `javascript:S3504` sites are zero-indented legacy GJS module bindings whose `var` visibility is the import/export mechanism covered by R05/D02. The remaining findings are compatibility-sensitive alternatives, deliberate empty test doubles, or trivial local style; changing them wholesale adds runtime-floor risk without correcting behavior. The feasible optional-chain and own-property cleanups were applied after re-evaluation, using older-runtime-safe equivalents in production GJS. |
| R21 | Sonar `javascript:S8786`: two end-whitespace regexes can backtrack super-linearly. | The third-party inputs are tightly bounded: summaries and holiday names to 300 code points, panel labels to 64, clock labels to 24, and at most eight clocks. Custom tooltip-format output is the configuration exception and is tracked at its ingress/render boundaries by T574; after that bound is enforced, rewriting the regex separately still adds no distinct mitigation. |
| R22 | Replace SonarCloud automatic analysis with scoped CI analysis that imports coverage and distinguishes tests from production sources (T557). | Rejected explicitly by the maintainer on 2026-07-23. No implementation will be pursued; the existing local and CI coverage gates remain authoritative. |

## Recorded decisions

| id | decision |
|----|----------|
| D01 | Keep the third-party endpoints rather than a bundled data set or a single vendor. A bundled table goes stale and covers fewer countries; a single provider breaks whenever it is down — the fallback chain exists because the primary is a hobbyist service. Risk accepted with mitigations (weather opt-in; one-time local-tzdata holiday default with visible third-party disclosure and explicit disable/override; size caps; bounded spans; shape checks in and out of cache; colour filtering; no URL logging). |
| D02 | **Keep the widened export surface.** ~80 exported names have no cross-module consumer — used inside their own file and otherwise only by `test/`. The applet runs inside a compositor where a bug costs the whole session; testing only through the composition root would be slow and flaky. **This does not license the inverse:** production must not bend to a test double's shape. |
| D03 | **`author` is the display name "Geraldo Netto", not the GitHub handle.** `info.json`/`package.json` use "Geraldo Netto"; `schema_static.test.js` asserts it. The Spices convention prefers the handle, but the display name is the maintainer's explicit choice. |

## Resolved merge choices

Decisions from the calendar@ccprog / Simon Wiles merge. Kept so future audits do not re-pick them.

| id | resolution |
|----|------------|
| G01 | Kept the `calendar@ccprog` world-clock settings workflow (Cinnamon-native settings, up to 8 user clocks plus built-in UTC/local, region/city selection, IANA/city entry, timezone validation, inherited coverage). Simon Wiles' separate GTK editor remains unported. |
| G02 | Kept `calendar@ccprog` locale-aware weekend behaviour (locale-derived work weeks plus a configurable one- or two-day weekend length). |
| G03 | Kept the copied `calendar@ccprog` visual assets as the initial merged set. The Chronos screenshot was refreshed on 2026-07-16; the icon was replaced with an original design and the orphaned root `calendar.png` deleted on 2026-07-18. |
