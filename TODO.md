# cinnamon-chronos TODO

## Findings

| ID | Category | Severity | Status | Effort | Description | Notes |
|----|----------|----------|--------|--------|-------------|-------|
| T709 | release & deploy engineering; documentation | Low | open | S | Document that adding a new export to an importer-loaded root module requires a Cinnamon restart, not just a reload. | **[verified live 2026-08-05]** `ReloadXlet` re-reads the applet's own files (a fresh `5.4/appletLifecycle.js` ran) but Cinnamon's GJS importer keeps its cached `imports.ui.appletManager.applets[uuid]` subtree, so the freshly reloaded code saw the *old* `ioUtils` module and threw `IoUtils.NetworkState is not a constructor` until `global.reexec_self()`; the 5.4 `require()` shims resolve through the same cached importer, so they are not a mitigation. Affects every Spices update that adds a new export to a root module: the applet can load broken after the update-triggered reload until the user restarts Cinnamon. Record the constraint where release work will see it (release/packaging docs) and prefer additive changes that keep new cross-module APIs out of already-shipped root modules within a release when practical. |
| T710 | reliability / correctness; data structure | High | open | M | Key multi-day event buckets on a day identity that survives a midnight DST transition. | **[verified]** `EventIndex.register()` walks `bounds.start` (`eventIndex.js:151-152,193-205`) with `add_days(1)`, which preserves h:m:s, and `date_only()` is `GLib.DateTime.new_local(y, m, d, 0, 0, 0)` (`eventData.js:58-66`). In zones whose DST jump is at midnight there is no local 00:00 that day — confirmed for Africa/Cairo 2023-04-28, where 22:00 UTC maps to 01:00 +03:00 — so `date_only(data.start)` resolves to 01:00 and every later `date_iter` is 01:00 of its day. Two failures in one loop: `dt_equals(bounds.end, date_iter)` (`:201`) can never match a 00:00 `data.end_date`, so the escape counter runs all 51 iterations and creates ~50 buckets; and every one of those keys is 3600 off from what `get()`/`getByUnixKey()` (`eventIndex.js:~230`), `calendar.js:120-123` `dateUnixKeys` and `eventsManager.current_selected_date` query. A multi-day event that starts on the transition date shows its dot and row on day one only; days two onward vanish from the grid and the event column, and the phantom buckets are retained until the month changes. Same shape in America/Havana, America/Santiago, Asia/Beirut, Asia/Gaza and Asia/Hebron. Derive the per-day key from a transition-safe identity (or normalise `date_iter` through `date_only()` each step), make the loop terminate on a date comparison rather than instant equality, and report rather than swallow an escape-counter exhaustion. |
| T711 | release & deploy engineering; process & delivery governance | Medium | open | S | Regenerate or version-normalise the translation template as part of the release bump. | **[verified]** `po/makepot:10,29` bakes `metadata.json`'s version into the header, and the committed template reads `"Project-Id-Version: chronos@geraldo-netto 0.0.1\n"` (`po/chronos@geraldo-netto.pot:9`). `RELEASE_TARGETS` covers only `package.json`, `package-lock.json` and `metadata.json` (`scripts/release.mjs:21-25`), and nothing in the bump re-runs `makepot`. `checkI18n` regenerates the template and compares it after normalising **only** `POT-Creation-Date` (`scripts/check-i18n.mjs:84-88,160-176`), so the first successful `release:bump` makes it throw `translation template is stale at line 9`. That is step 4 of the project's own documented recipe (`README.md:146-153`), whose `git add` two lines later also omits the `.pot`; the CI packaging job runs `release:check` then `i18n:check` back to back and reddens the same way. Either add the template to the release transaction (regenerating it) or normalise `Project-Id-Version` alongside the creation date, and cover a bump-then-check run. |
| T712 | data governance; UI / UX | Medium | open | S | Stop rewriting the saved world-clock list to disk when the settings window opens. | **[verified]** `ClocksList.__init__` calls `normalize_clock_setting(info, key, settings)` before `JSONSettingsList.__init__` (`chronos_settings_widgets_worldclocks.py:587`), and that helper calls `settings.set_value(key, normalized)` whenever the projection differs (`:279-289`) — a `JSONSettingsHandler` write that rewrites the config file and notifies the running applet. `normalize_saved_clocks` (`:249-276`) drops any non-dict row, any row whose `label` is blank after `strip()` even when its `timezone` is valid, any row whose timezone is a runtime built-in, and — via `break` at `:273-274` — everything past the eighth entry. The JS runtime applies the identical filter non-destructively at render time and never touches the stored list (`worldclockData.js:453-461,463-487`). Cinnamon constructs every page's widgets up front, so opening the settings window for any reason permanently deletes those rows with no prompt and no undo. Filter for display only, and persist a normalisation only in response to a user edit. |
| T713 | robustness / recovery; wiring gaps | Medium | open | S | Give `AppletMenuBuilder` ownership of the collaborators it constructs, so a failed build can still tear them down. | **[verified]** `build()` returns the calendar and event list only on success (`5.4/appletMenuBuilder.js:145-173`), and the applet's `this._calendar` / `this.event_list` are assigned solely from that return value (`5.4/applet.js:236-244`). `AppletMenuBuilder.destroy()` disconnects its own three id arrays but never calls `destroy()` on either object (`appletMenuBuilder.js:249-289`), and the builder already holds them (`:231`, `:382`). A throw anywhere in `:156-160` — `box.add_actor`, either view constructor, or `_addSettingsMenuItems` — therefore reaches the constructor's catch (`applet.js:149-155`), and both `destroyIfPresent` steps (`applet.js:587-588`) silently no-op. What survives for the session: the shared `org.cinnamon.desktop.interface` handler (`calendar.js:687-688`), the module-level `LocaleQuery.onLocaleInfoChanged("LC_TIME", …)` listener whose callback rebuilds the header and queues an update into a dead grid (`calendar.js:696-700`), three events-manager handlers (`calendar.js:707-713`), the pending `_update_id` idle, the navigation timeout reachable only through `Calendar.destroy()` (`calendarNavigation.js:98-104,127`), and `EventListRenderer`'s three GLib sources (`eventView.js:153-157`). `this._issueReporter = issueReporter` on the line after the last throwable call (`appletMenuBuilder.js:160-161`) has the same shape, defeating the detach step at `:253-258`. Assign each owned object as it is built and destroy it from the builder. |
| T714 | reliability / correctness; UI / UX | Medium | open | S | Carry an authoritative timezone on every geocoded place instead of falling back to the machine's zone. | **[verified]** `openMeteoGeocodePlace` returns `timezone` (`weatherServiceAdapters.js:398-418`) but `nominatimGeocodePlace` returns only name and coordinates (`:420-442`), and Open-Meteo hits are rejected outright below `MIN_TRUSTED_GEOCODE_POPULATION` (`:34,408-411`), so any smaller place — or any Open-Meteo outage — falls through to the Nominatim provider (`weatherProviders.js:157-173`) and yields a place with no zone. `AstronomyView._placeTimezone()` then silently substitutes `GLib.TimeZone.new_local()` (`5.4/astronomyView.js:97-109,128`) while `_observerDay` keeps the remote coordinates (`:123-134`), so `civilDayBounds` builds the *viewer's* civil day (`:30-46`) and `defaultFormatTime` prints in the viewer's zone (`:64-70`). For a location far east or west that is the wrong day's sunrise/sunset, or `MISSING_EVENT_TIME` when the crossing falls outside the local window — with nothing saying a fallback happened, and the same city reading differently depending on which geocoder answered. The uncached fallback also constructs a fresh `GLib.TimeZone` on every `update()`, which runs per clock notify while the menu is open (`5.4/applet.js:515-529`). Open-Meteo's forecast reply already carries `timezone` because `forecastUrl` asks `timezone=auto` (`weatherServiceAdapters.js:115-122`) and `weatherReading` discards it (`:242-255`); resolve a zone for every provider path, memoise the fallback, and make an unresolved zone visible rather than silent. |
| T715 | state machine integrity; UI / UX | Medium | open | S | Re-evaluate the event-index overflow flag when events are culled or removed. | **[verified]** `EventIndex.cull()` rebuilds the id/uid maps through `_rebuildEventState()` but leaves `_overflowed` untouched (`eventIndex.js:243-263,73-82`), and `remove()` does not touch it either (`:~236`). Only `clear()` (`:46-51`, reached solely on a month change via `eventWindow.js:42-44`) and `clearOverflow()` reset it, and `clearOverflow()` has exactly one production caller — the `_resync_overflow_pending` branch of a full range dispatch (`eventsManager.js:622`). So once the ceiling is touched, garbage-collecting the deleted events back down to a handful leaves the flag set: `eventWindow.js:86,101` keep passing `Boolean(this.index.overflowed)` into `set_events`, `EventList.setOverflowed` keeps the overflow label shown and keeps `_reportIssue("events-overflow", …)` armed (`5.4/eventView.js:673-696`). The user is told events were hidden on a day with three events until they navigate away and back. Recompute the flag from the retained count whenever the index shrinks. |
| T716 | reliability / correctness; UI / UX | Medium | open | S | Invalidate the cached calendar month window when the operating-system timezone changes. | **[verified]** `CalendarMonthWindowCache` keys on year/month/weekStart only and exposes no invalidation (`5.4/calendar.js:96-112`); `Calendar` never clears `this._monthWindows` (`:674`, read at `:1085`) in `destroy()`, `_buildHeader()`, the LC_TIME listener or `refreshToday()`. The cached window's `dateUnixKeys` are absolute unix seconds derived from the process timezone at construction (`:120-123`). The timedate1 `PropertiesChanged` subscription routes a `Timezone` change to `context.onTimezoneChanged()` (`5.4/appletLifecycle.js:418-433`), which the applet answers with `_reconcileWorldclocks()` alone (`5.4/applet.js:317-325`) — nothing touches the grid. After a `timedatectl set-timezone` (or an automatic change on a travelling laptop) events delivered afterwards are indexed under new-zone `date_only().to_unix()` keys while the displayed month still holds old-zone ones, so event dots stop appearing; `refreshToday()` → `_queue_update()` hits the same cache key, so the midnight tick does not heal it. Invalidate the window cache (and the day-cell name cache beside it) on the timezone signal, and cover a re-render across a zone change. |
| T717 | UI / UX; documentation | Low | open | S | Keep the "no calendar service" remediation visible when the selected day also carries a holiday. | **[verified]** `EventList._renderCurrentEvents()` projects the agenda down to `holidaysOnly()` when the service is unavailable and then gates `EVENTS_UNAVAILABLE_TEXT` on `!agenda` (`5.4/eventView.js:716-728`). With EDS down and a holiday on the selected day, `agenda` is the truthy projection, so the column renders one holiday row and nothing else — byte-identical to a healthy service on a day whose only entry is that holiday. The sentence that tells the user to install or enable Evolution Data Server is exactly what is dropped, and the footer report from `_syncIssues()` (`:693-703`) is then contradicted by the column itself. Show the unavailable notice alongside the holiday rows rather than instead of them. |
| T718 | configuration discoverability; UI / UX | Low | open | S | Make the holiday `country` schema default a value the settings widget can render. | **[verified]** the schema declares `"country": { "default": "" }` while its `options` map runs from `"none"` to the ISO-3 codes (`5.4/settings-schema.json`, `country` block). `CountryComboBox` builds `option_map` from those options alone (`chronos_settings_widgets_holidays.py:66-68`), so `on_setting_changed` calls `set_active_iter(option_map.get("") === None)` and `restore_entry_text` writes `""` into the entry on every focus-out (`:105-107,131-139`). The applet's one-time inference only replaces the empty sentinel at add-to-panel (`5.4/appletLifecycle.js:186-199`) and `initHolidayProvider` normalises `country == null` but not `""` (`:317-319`), so a "Reset to defaults" on a running applet leaves the key at `""` indefinitely: the Country field renders blank with only its placeholder, indistinguishable from a widget that failed to load, even though `NO_HOLIDAYS = "none"` is the declared off value (`settingsFacade.js:51`). Default the schema to `"none"` (or admit `""` into the widget's domain) while keeping the timezone inference's empty-sentinel contract. |
| T719 | process & delivery governance; test coverage | Low | open | S | Fail the i18n gate when no catalogs are found. | **[verified]** `checkI18n` builds `catalogPaths` from `readdir(poDir)` filtered on `.po` and imposes no floor (`scripts/check-i18n.mjs:141-150,181`); both `Promise.all([])` calls resolve, and `unexpectedCommonSourceCopies` short-circuits on empty input (`:53-56`). Deleting, renaming or mis-locating every catalog therefore prints `0 catalogs valid; translation template is current` (`:186`) and exits 0, leaving the CI packaging job green while the applet ships untranslated — the one regression the gate exists to catch. Assert against a tracked list of expected locales (or at minimum a non-zero floor) so a vanished catalog is a failure rather than a pass. |
| T720 | wiring gaps; reliability / correctness | High | open | S | Apply the timezone-inferred holiday country in the session that writes it. | **[verified]** `HolidaySettings.fillInitialCountryFromTimezone` (settingsFacade.js:186-195) persists the inferred country through `this._settings.setValue(COUNTRY_KEY, ...)` and returns it, but Cinnamon's `setValue` mutates `settingsData` in place before saving (/usr/share/cinnamon/js/ui/settings.js:479-483) and `_checkSettings` — the only code that runs bind callbacks and emits `changed::<key>` (:588-620) — is reached solely from `remoteUpdate` (:901-903), so a self-write emits nothing and `connectCountryChanged` (settingsFacade.js:207-209, wired at 5.4/appletLifecycle.js:309) never fires; the caller 5.4/appletLifecycle.js:193-199 discards the returned country and never calls `onHolidayPlaceChanged`, while `initHolidayProvider` already ran during construction with the empty country and took the `clearPlace()` branch (5.4/appletLifecycle.js:338-344, :330). Input: first install on a machine whose timezone maps to a supported country — "italy" is written into the instance settings but the provider stays cleared, so no holiday is marked and no fetch is issued until Cinnamon is restarted or the user edits a holiday setting; the sibling `fillEmptyWeatherLocation` (settingsFacade.js:308-316) already compensates for the identical silent-self-write by assigning `target.weather_location` by hand. Remediation: have `deferInitialHolidayCountry` consume the returned country and invoke `onHolidayPlaceChanged()` when it is non-empty. |
| T721 | reliability / correctness; UI / UX | Medium | open | S | Drive the event column's date heading and holiday row from the calendar's selection instead of a signal that is suppressed when no calendar service is active. | **[verified]** `EventWindowCoordinator.selectDate()` returns before any emission when `isActive()` is false (`eventWindow.js:59-61`), and `reloadSelected()` does the same (`:93`). A repo-wide grep shows the only writer of `EventList.set_date` (`5.4/eventView.js:596-609`) and of the builder's `_selectedEventDate` is the `"selected-date-changed"` handler on the events manager (`5.4/appletMenuBuilder.js:199-206`). `is_active()` is `_inited && showEvents && _calendar_server !== null && status !== STATUS_NO_CALENDARS` (`calendarServerConnection.js:239-242`), so with "Show events" on and evolution-data-server absent — or present with every calendar disabled — that signal never fires for the life of the session, while the column stays on screen: `AppletEventListCoordinator.update()` only sets `list.actor.visible = enabled` and `set_unavailable(enabled && !active)` (`5.4/appletCoordinators.js:126-137`). Two visible consequences. The heading label is built with no text (`5.4/eventView.js:381-388`) and `set_date` is its sole writer, so it renders permanently blank rather than saying which day is selected. And `_renderAgenda()` resolves the holiday row through `this._calendar.holidayForDate(this._selectedEventDate)` (`appletMenuBuilder.js:392-393`), where `_selectedEventDate` is stuck at the construction-time `eventList.selectedDate` — `GLib.DateTime.new_now_local()` at `eventView.js:345`, captured at `appletMenuBuilder.js:232` — so clicking through the grid moves the dots and the cell highlight while the column keeps announcing the applet's start date's holiday, or none. Feed both from `Calendar.getSelectedDate()`/the calendar's own `selected-date-changed`, which fires regardless of event availability. |
| T722 | reliability / correctness | Medium | open | M | Rank and population-gate Nominatim geocode hits the way Open-Meteo hits are ranked, instead of accepting its single unranked result. | **[verified]** `openMeteoGeocodePlace` (weatherServiceAdapters.js:398-418) deliberately returns `null` when its best candidate is below `MIN_TRUSTED_GEOCODE_POPULATION` (weatherServiceAdapters.js:34) so "the next geocoder in the queue arbitrates it", but the next geocoder cannot arbitrate anything: `nominatimGeocodeUrl` asks for `limit=1` (weatherServiceAdapters.js:111-112) and `nominatimGeocodePlace(data)` (weatherServiceAdapters.js:420-442) takes `data[0]` with no population floor, no exact/folded name comparison and no use of the query at all — `_geocodeLocation` binds the typed name into every normalizer (`normalize: (data) => provider.normalize(data, location)`, weatherProviders.js:240-247) and this one silently drops the second argument. Input: a location whose best Open-Meteo hit is a sub-1000-population namesake (the "Genova"/"Génova" class the floor was written for) -> Open-Meteo yields null -> `tryProvidersInOrder`'s `accept = Boolean(place)` (weatherProviders.js:258) falls through to Nominatim -> whatever OSM ranks first becomes the resolved place, so the panel temperature (appletPanelStatus.js:379-380) and the AstronomyView sunrise/sunset (5.4/astronomyView.js:123-134) are for a different city, with no marker distinguishing it from a confident hit. Fix by requesting `GEOCODE_CANDIDATE_COUNT` hits from Nominatim and running the same `betterPlace`/population ranking (weatherServiceAdapters.js:373-396) over them, or by refusing an unranked hit rather than accepting it. |
| T723 | wiring gaps; UI / UX | Medium | open | S | Give Nager.Date's `localName` a language tag `HolidayRecordContract.localizeName` can actually select, so non-English users see the local holiday name it already fetches. | **[verified]** `NagerDateServiceAdapter._name` emits `[{lang: "local", text: localName}, {lang: "en", text: name}]` (`holidayServiceAdapters.js:321-333`), but `HolidayRecordContract.localizeName` keeps only entries whose `lang` equals the message language or `"en"` (`holidayRecord.js:119-127`) and `LocaleQuery.messageLanguage` always returns a two-letter code or the fallback (`localeQuery.js:172-176`), so `"local"` can never match; the `usable[0]` escape at `holidayRecord.js:126` is unreachable because `_validHoliday` guarantees the `en` entry is non-blank (`holidayServiceAdapters.js:296-304`). Inputs: `es` desktop, country `arg` (absent from `OPEN_HOLIDAYS_COUNTRIES`, `holidayConstants.js:41-70`, so OpenHolidays short-circuits to `SERVICE_UNAVAILABLE` at `holidayServiceAdapters.js:256-259` and Nager answers) -> Nager's `{name: "New Year's Day", localName: "Año Nuevo"}` is stored and rendered as "New Year's Day" in the cell tooltip, the cell accessible name (`5.4/calendarAnnotations.js:429-436`, `5.4/calendar.js:349-350`) and the event view, with the Spanish name fetched, validated and then discarded. `test/holiday_adapters.test.js:179-186` pins the emission but nothing asserts it is ever displayed. Tag the entry with the country's real ISO-639 language (derivable from the country code) so `localizeName` can pick it, or make `localizeName` prefer a `"local"` entry over `"en"` when the message language is not English and no exact-language entry exists. |
| T724 | reliability / correctness; UI / UX | Medium | open | M | Extend the religious date tables past 2027 and make an out-of-window year visibly say so instead of rendering an empty religion. | **[verified]** `TABLES` in `religiousHolidays.js:74-101` covers only 2025-2027; `_dateOf` returns `null` for any other year (`:217-227`) and `_datesOf` turns that into an empty row list (`:266-273`), silently. Measured by running `holidaysForYear(year, [id])` per religion: 2027 gives islam=5, sikhism=3, judaism=55, bahai=2, jainism=2, taoism=3, hinduism=5, buddhism=3; 2028 gives islam=0, sikhism=0, judaism=0, bahai=0, jainism=0, taoism=0, hinduism=1, buddhism=2. Inputs: enable Islam (or Judaism, Sikhism, Bahá'í, Jainism, Taoism) and press the next-year button twice from today (2026-08-05) -> every month of 2028 renders zero observances with no marker, no tooltip and no `reportIssue` line, indistinguishable from "no observances this month"; `_omerDates` also collapses to `[]` because it reads the same tables (`:248-264`). The window expires within ~17 months of today and nothing in the build fails when it does. Extend the tables, add a static check that fails the suite once the covered window falls below a rolling horizon, and surface "religious dates unavailable for this year" through the existing holiday status path rather than rendering an empty month. |
| T725 | data structure; caching strategy | Medium | open | M | Stop `_rebuildIndex` from rewriting the year LRU, so eviction picks the genuinely least-recently-used year instead of the first-inserted one. | **[verified]** `_evictYear` calls `_rebuildIndex` (`holidayCache.js:475-481`), which replays `this.data` through `addUnique`/`_addUnique` (`:644-652`, `:661-690`), and `_addUnique` ends with `_touchYear(single.year)` (`:689`) — a `delete`+`set` that moves the year to the end of `_yearUse`. Every eviction therefore rewrites the whole recency order into `this.data` insertion order, discarding the recency `matchMonth` (`:814`) and `recordAttempt` (`:779`) recorded. Reproduced by driving the real class: fetch 2026 then 2025..2019 (8 years, `MAX_CACHED_YEARS` = 8), then `recordAttempt(2027, "global")` -> order becomes `2027,2025,2024,2023,2022,2021,2020,2019` with the just-used 2027 now the oldest; one more year touched (`recordAttempt(2028)`) evicts 2027 and `stale(2027)` flips from `false` back to `true`, so the 1-hour `RETRY_PERIOD` guard at `:799-803` is lost after a single sibling year — exactly the failure the comment at `:776-780` says the `_touchYear` in `recordAttempt` exists to prevent. Because the 42-day grid always straddles two years, a down provider then gets two fresh HTTP fetches on every `_update()` instead of one per year per hour, and `HolidayStatusLedger.prune(this.cache.cachedYears())` (`holidays.js:462`, `:160-168`) deletes the year's error record in the same step so the ⚠ reason disappears from the month label between attempts. Second reproduction: with data pushed 2019..2026 and `matchMonth(2026, 8)` marking 2026 most-recent, fetching 2027 then 2028 evicts 2019 and 2020 by data order while the rebuild re-sorts everything, so the LRU degenerates to FIFO-by-first-fetch. Have `_rebuildIndex` (and `_evictYear`) preserve the existing `_yearUse` ordering — rebuild the indexes without re-touching, or snapshot and restore the key order around the replay. |
| T726 | security; reliability / correctness | Medium | open | M | Bound the holiday cache read before the file is loaded, not after. | **[verified]** `readJsonFileAsync` (ioUtils.js:85-115) calls `file.load_contents_async` and only then applies the 4 MiB cap, inside `_parseCacheFile` (ioUtils.js:74-83, `tooBig(contents.length, MAX_CACHE_FILE_BYTES, ...)`), so the entire file is already resident in the compositor process by the time the guard runs; the module's own comment at ioUtils.js:36-41 states the threat exactly ("~/.cache/chronos@geraldo-netto/holidays.json is writable by anything running as the user", parsing it "would balloon the Cinnamon process") and the network path was rewritten into the chunked `_readCapped` (ioUtils.js:220-271) for precisely this reason, leaving the disk path as the one place where the cap documents an intent nothing enforces. Input: any process running as the user replaces the holiday cache with a multi-gigabyte file; on every applet construction and reload holidayCache.js:250, :300 and :349 call `readJsonFileAsync`, and Cinnamon allocates the whole file before the cap is consulted, so the shell OOMs or stalls instead of logging "past the cap" and continuing with an empty cache. Remediation: `query_info_async` on G_FILE_ATTRIBUTE_STANDARD_SIZE (or `read_async` plus the existing `_readCapped` chunk loop) and refuse before any bytes are buffered. |
| T727 | state machine integrity; reliability / correctness | Medium | open | M | Resume a locale query abandoned by teardown when the next consumer registers. | **[verified]** The abandoned branch of `fail()` (localeQuery.js:296-311) deliberately skips `_storeInfo`, so `localeGeneration` (localeQuery.js:180) never moves and `degraded[env]` is never set, and it re-issues the query only when `_consumers > 0` at the instant Gio delivers the cancellation. `registerLocaleConsumer` (localeQuery.js:62-77) resumes only envs present in `degraded`, and every consumer reaches the locale through `lazyLocaleValue` (localeQuery.js:400-410), whose module-scope memos at 5.4/calendar.js:69-71 return their cached value without calling `getInfo()` while `generation === localeGeneration`. Input: remove the applet during the first seconds after login while `locale -k LC_TIME` is still in flight (the window localeQuery.js:50-57 exists for), then re-add it after the cancellation callback has already run with `_consumers === 0`; nothing re-issues the query, `_lcAbday` and `_lcFirstWorkday` stay warm on the English/US defaults, and the calendar header keeps "Sun;Mon;Tue;..." plus the US work week for the rest of the session even though `locale` would answer instantly. Remediation: track abandoned-before-answer envs in their own set and resume them from `registerLocaleConsumer` beside `degraded`, or bump `localeGeneration` in the abandoned branch so the memos ask again. |
| T728 | UI / UX; reliability / correctness | Medium | open | S | Accept an exactly typed country name on focus-out instead of silently reverting it. | **[verified]** CountryComboBox.on_entry_focus_out calls restore_entry_text (chronos_settings_widgets_holidays.py:126-139), which rewrites the entry from self.value; self.value only ever changes in on_combo_changed (:113-124), which returns early whenever get_active_iter() is None, and GtkComboBox's entry-contents-changed handler sets the active item to -1 on every edit, so typing never produces an iter. Inputs: with Country = Portugal, click the Country field, type "Brazil" in full, press Tab or click another field. Outcome: the entry snaps back to "Portugal", the key is never written and holidays keep coming from Portugal, with no error text, no error style and no message anywhere — unlike the sibling widgets, which either commit on focus-out (WeatherLocationEntry.on_commit, chronos_settings_widgets_weather.py:189-192) or say why the input was refused (TIMEZONE_INVALID_PREVIEW). In restore_entry_text, first match the trimmed entry text against the folded option labels already stored in model column 2 and set_active_iter on an exact hit, restoring only when nothing matches. |
| T729 | reliability / correctness; UI / UX | Medium | open | S | Strip control characters from world-clock display names on both the settings and runtime sides. | **[verified]** normalize_clock_label (chronos_settings_widgets_worldclocks.py:240-246) only strips surrounding whitespace and clamps to 128 characters, and its JS twin clockInputLabel (worldclockData.js:494-497) only clamps, so a Display name pasted with an embedded newline or a C0/C1 control character is persisted verbatim through ClockEntrySerializer.serialize (:305-306) — even though textUtils.js:10-13 names "a world clock's label" as exactly what the shared rule is for, and sanitizeControlCharacters (textUtils.js:20-39) has holidays.js:400 as its only caller. Inputs: paste "Home\nOffice" into the Display name field of the add-clock dialog and save. Outcome: the popup row's St.Label and its Tooltip receive the raw string (5.4/worldclocks.js:73-89) so the row grows a second line and shifts the calendar grid, and the same string becomes one cell of the monospace panel tooltip whose padding is computed as Array.from(cell).length (5.4/appletPanelStatus.js:52-70), so the newline both splits the row and misaligns every column of the table. Run the label through sanitizeControlCharacters in clockInputLabel and mirror the same filtering in normalize_clock_label so the dialog cannot persist it in the first place. |
| T730 | caching strategy; reliability / correctness | Low | open | S | Give the geocode cache an expiry so a wrong or transient resolution cannot pin a place for the whole session. | **[verified]** `WeatherLocationResolver` caches resolved places with a size bound but no TTL (weatherProviders.js:178-234): `_remember` evicts only when `size >= _max_entries` (16), and `resolve()` short-circuits on any hit forever after (weatherProviders.js:219-223). The sibling reading cache does have one — `_freshReading` expires on `age >= this._cache_milliseconds` (weatherProviders.js:448-464, fed `cacheSeconds: REFRESH_SECONDS` at 5.4/appletLifecycle.js:226-228) — so a stale *reading* self-corrects in 30 minutes while a stale *place* never does. The only invalidation path is `WeatherProvider._forgetIfLocationChanged` (weather.js:236-246), which calls `_reading_repository.forget()` only when `key !== this._resolved_location_key`; re-entering the same text is a no-op, and nothing else in production calls `forget()`. Input: one glitched or ambiguous geocode round for "Rome" -> the wrong coordinates are pinned -> the panel weather and the astronomy sunrise/sunset stay wrong for the life of the Cinnamon session, with no timer, no retry and no user action short of typing a different city and typing it back that can clear it. Fix by stamping each cache entry with a resolution time and expiring it (a day is ample, since place coordinates are static) in `_remember`/`resolve`. |
| T731 | unused functions / methods; wiring gaps | Low | open | S | Delete the weather diagnostics accessors that only tests reach, or route the production code through them. | **[verified]** Not the D02 export surface — these are instance fields and methods on shipped classes that exist only so a test can read them, which is the inverse D02 explicitly does not license. Grepped every production caller across `files/`: `WeatherProvider._getHttpSession` and `._httpGetJson` (weather.js:157-163), the `_session`/`_location_resolver`/`_forecast_resolver` mirrors assigned in its constructor (weather.js:152-154), `CityWeatherProvider._getHttpSession` and `._session` (cityWeather.js:106, 113), `WeatherForecastResolver.lastProvider` (weatherProviders.js:345-347), `WeatherLocationResolver.cache` (weatherProviders.js:184-186), `WeatherRefreshScheduler.debounceId` and `.retryId` (weatherScheduler.js:82-88) and the `GEOCODE_LANGUAGE_FALLBACK` export (weatherServiceAdapters.js:42) have zero call sites outside `test/` — the sole readers are test/weather_io.test.js:34-166, test/weather_adapters.test.js:858-877, test/weather_provider.test.js:250-262/926/1219/1469/1758 and test/gjs_import.test.js:189/313. The comment at weather.js:150-151 calls them "compatibility-visible references for diagnostics", but no diagnostic path reads them, so the coverage gate certifies a surface production never exercises and any future change to session ownership will be validated only against the mocks. Remove them and let the tests assert through `_reading_repository`, or wire the real session/provider diagnostics to them. |
| T732 | UI / UX; accessibility | Low | open | S | Stop marking the empty timezone field invalid before the user has typed anything in the add-clock dialog. | **[verified]** ClockDialogBuilder.build_content ends with on_widget_changed(None) at chronos_settings_widgets_worldclocks.py:517 while both entries are still empty, so ClockDialogStatePresenter.update (:398-417) gets choice["timezone"] None with typed_invalid False from resolve_timezone_choice (:644-667) and falls into the else branch at :411-413, which calls _report(..., invalid="timezone"); _report (:419-434) then puts the GTK "error" style class on the preview label via set_error_state (:328-337) and on the timezone Gtk.Entry via set_invalid (:340-354). Inputs: open World Clocks and click Add. Outcome: the dialog appears with the untouched Timezone entry and the preview label drawn in the theme's error colour and an ATK description written on the entry, while the equally empty Display name entry is left clean, so the user and a screen reader are told they entered something wrong before entering anything. Gate the invalid marking on choice["typed_invalid"] or choice["reserved"] and let the empty state carry only the neutral TIMEZONE_EMPTY_PREVIEW text. |
| T733 | reliability / correctness; state machine integrity | Low | open | S | Recompute the world-clock Add button sensitivity whenever the list is repopulated from settings. | **[verified]** ClocksList recomputes `full = self.model.iter_n_children(None) >= MAX_CLOCKS` only inside update_button_sensitivity (chronos_settings_widgets_worldclocks.py:614-639), and Cinnamon invokes that method only from List.__init__, List.list_changed and the tree selection's "changed" signal; List.on_setting_changed (/usr/share/cinnamon/cinnamon-settings/bin/TreeListWidgets.py:346-361) clears and repopulates self.model and never calls it, and it is reached from JSONSettingsHandler.reset_to_defaults / load_from_file via do_key_update (JsonSettingsWidgets.py:189-227) wired to the settings window's overflow menu (xlet-settings.py:186-199, 523-528). Inputs: 8 clocks saved, settings window open, no row selected, choose "Reset to defaults" (or "Import from a file" with a shorter list). Outcome: the tree empties but the Add button stays insensitive and still tooltipped "No more than 8 clocks can be added.", so no clock can be added until the settings window is closed and reopened. Override on_setting_changed in ClocksList to call update_button_sensitivity() after delegating to the base implementation. |
| T734 | performance; robustness / recovery | Low | open | S | Bound the add/edit dialog timezone entry the way the label column is bounded, and fold the completion key once per keystroke instead of once per row. | **[verified]** ClockDialogBuilder.build_content sets label_column["max_length"] = MAX_CLOCK_INPUT_LABEL_LENGTH (chronos_settings_widgets_worldclocks.py:463-464) but hands timezone_column only completions and a placeholder (:465-467), so ListEditEntry's `if max_length` guard (:203-204) skips set_max_length and the Timezone Gtk.Entry accepts unbounded text; each edit of that entry runs timezone_completion_match (:99-117) once per row of the several-hundred-row completion model, and that function calls completion_key(key) — strip + lower + replace, three fresh copies of the whole key — inside the per-row callback, contradicting its own docstring claim that "the needle is folded once by the caller". Inputs: paste a multi-megabyte string into the Timezone field. Outcome: hundreds of full copies and scans of that string per keystroke on the GTK main thread, freezing cinnamon-settings; CountryComboBox has the same unbounded entry with plain_completion_match (chronos_settings_widgets_common.py:34-45). No IANA identifier exceeds a few dozen characters, so set a max_length on the timezone column (and on the country entry) and hoist the fold out of the per-row matcher. |
| T735 | data governance; configuration discoverability | Low | open | S | Derive the region-capable country list in code rather than storing it in the user's settings file. | **[verified]** `has_region` is a `generic` schema entry whose default is the ten-country list `["aus","bel","can","deu","esp","nzl","svk","che","gbr","usa"]` (`5.4/settings-schema.json`, `has_region` block), and it is read back out of the *user's instance file* by `HolidaySettings.regionCountries` (`settingsFacade.js:198-200`) — the sole input to `bindRegions()` (`:226-230`), which decides which `region_<country>` keys are bound onto `AppletProviderLifecycle.holidayRegions` (`5.4/appletLifecycle.js:312`), the object `onHolidayPlaceChanged()` indexes to find the region it sends with `setPlace()` (`:339-349`). Cinnamon's `_doUpgrade` keeps the stored value for every key whose type is not spinbutton/scale/combobox/radiogroup — `_checkSanity`'s default branch returns true (`/usr/share/cinnamon/js/ui/settings.js:836-851,857-877`) — so the array is frozen at whatever shipped on the user's first install. The next release that adds a region-capable country therefore ships `region_<new>` plus a widened `has_region` default and every upgrading user keeps the old list: the settings dialog still renders the new region combobox, because its own `dependency: country=<new>` is satisfied by the country key alone, and every choice made in it is discarded — the key was never bound, `holidayRegions[<new>]` is `undefined`, and `setPlace()` silently requests nationwide holidays. Nothing logs the mismatch. Move the list beside `SUPPORTED_COUNTRIES` in `holidayConstants.js` and have `bindRegions()` read it from there, leaving only genuine user choices in the instance file. |
| T736 | release & deploy engineering; documentation | Low | open | S | Verify the executable mode of every tracked 0755 file in the released Spices tar, or stop claiming that it does. | **[verified]** `git ls-files --stage -- README.md files info.json screenshot.png` reports six `100755` entries — `files/chronos@geraldo-netto/5.4/settings_widgets.py`, `chronos_settings_widgets_common.py`, `chronos_settings_widgets_holidays.py`, `chronos_settings_widgets_weather.py`, `chronos_settings_widgets_worldclocks.py`, `po/makepot` — and `package-spices.mjs:244-245` reproduces each index mode into the package, but the release job's post-download guard checks only three of them (`.github/workflows/ci.yml:141-143`) and the comment above the archive step calls them "the three tracked executables" (`ci.yml:94-96`). The checksum manifest cannot cover the gap: `archive-spices.sh:9-11` records only `sha256sum` digests, which carry no mode bits, so `sha256sum -c` (`ci.yml:139`) passes on a tree whose `chronos_settings_widgets_holidays.py`, `_weather.py` or `_worldclocks.py` lost `+x` in the upload/download round trip. `README.md:178-180` states the release job "verifies every checksum and executable mode", which is false for half the set. Drive the mode assertions from `git ls-files --stage` instead of a hand-listed trio, or drop the vestigial `+x` from the three module files so the claim and the guard agree. |
| T737 | process & delivery governance; configuration discoverability | Low | open | S | Give the flat ESLint config a matching entry for root-level `.mjs` files and an `ignores` for `dist/`, so `npm run lint:js` stops passing vacuously. | **[verified]** every config object in `eslint.config.mjs:34-95` carries a `files` restriction (`files/**/*.js`, `scripts/**/*.mjs`, `test/**/*.js`), and nothing matches the root-level config file itself, so ESLint lints it with an empty rule set: `npx eslint --print-config eslint.config.mjs` prints `rules count: 0`, and feeding `import js from "@eslint/js";\nconst unused = 1;\nexport default totallyUndefinedIdentifier;` through `npx eslint --stdin --stdin-filename eslint.config.mjs` exits 0 with no messages — an undefined identifier and an unused binding in the one file that defines the project's only static gate are unlintable. The same absence of `ignores` means `eslint .` walks the packager's own output: with `dist/` present from `npm run package:spices` (`package-spices.mjs:252-255`, the step `README.md:123` and `ci.yml:89` prescribe), the run enumerated 155 files of which 53 were `dist/chronos@geraldo-netto/files/...*.js`, each also at `rules count: 0` — duplicated work reported as clean regardless of content, because `.gitignore:83` is invisible to ESLint. Add a root `*.mjs` config object and an explicit `ignores: ["dist/**"]`. |
| T738 | test coverage; process & delivery governance | Low | open | S | Build the JS per-file coverage file list from the filesystem rather than the Git index, so a new applet module cannot escape the gate before it is staged. | **[verified]** `shippedJavaScriptFiles()` resolves its list through `listTrackedSpicesFiles()` (`test/helpers/coverage.js:49-58`), which is `git ls-files --stage` (`scripts/package-spices.mjs:60-67`) — the index, not the working tree. That list becomes both the `--test-coverage-include=` set (`coverage.js:67-70,79-82`) and the iteration domain of `missingCoverageFailures()` (`coverage.js:101-111`), so a newly written, not-yet-`git add`ed `files/chronos@geraldo-netto/*.js` is neither instrumented nor reported as unmeasured: `npm test` prints "per-file coverage: every file meets 98/90/100" (`coverage.js:151`) while the new module sits at 0 %, which is exactly the false green the header comment at `coverage.js:4-11` exists to prevent and the gate AGENTS.md declares. The Python half does not share the hole — `test/helpers/coverage.py:26` globs `rglob("*.py")` off disk — so the two suites disagree about what "per file" means. Switch `shippedJavaScriptFiles()` to a filesystem walk of `files/<uuid>` (keeping the index list only for the packager), and cover the untracked-new-module case. |
| T739 | release & deploy engineering | Low | open | S | Preserve `metadata.json`'s existing formatting across a release bump instead of re-serialising the whole document. | **[verified]** `bumpReleaseLocked` rewrites each target with `JSON.stringify(files.metadata, null, 4) + "\n"` (`scripts/release.mjs:510-514`), and a round-trip check over the committed file shows that is not byte-preserving: `files/chronos@geraldo-netto/metadata.json:10` currently reads `"cinnamon-version": ["5.4"]` on one line and re-serialisation expands it to three (`"cinnamon-version": [` / `"5.4"` / `]`), so `npm run release:bump -- 0.0.2` emits a four-line diff in a twelve-line shipped Spices manifest where exactly one line changed semantically. The documented recipe then stages that file wholesale (`README.md:151`) into the `chore(release)` commit, and `README.md:156-157` describes the command as updating only the version owners, so the reviewer told to "Review the changes before staging them" is handed unrelated churn in the file Cinnamon parses at load. `package.json` and `package-lock.json` round-trip identically at indent 2, so only `metadata.json` is affected; either re-flow `metadata.json` in-repo to the writer's output, or patch the version line in place rather than re-serialising. |

## Rejected

| id | finding | why rejected |
|----|---------|--------------|
| R05 | Barrel re-exports as the GJS `var` bindings importers need. | The re-exports **are** the import mechanism; tests reach the modules through them. |
| R06 | `5.4/calendarLauncher.js:50`: commented-out `Util.trySpawn(["gnome-calendar"])`. | Deliberate documented compatibility switch with a maintainer note directly above it. |
| R08 | No watchdog/metrics/circuit-breakers/health surface. | For a panel applet these are reasonable omissions; the provider failover with last-success ordering plus a per-year freshness gate is a coherent substitute. |
| R09 | Per-calendar colour strip conveys calendar identity by colour alone. | Not actionable here: `cinnamon-calendar-server` sends the calendar's colour and never its display name, so there is no name to announce. The strip is `Atk.Role.SEPARATOR` so a screen reader skips it. Needs an upstream change. |
| R12 | Six `religiousHolidays.js` mutants survive the operator/constant audit (computus f-term `+8→+9`, month-expression `7m→8m`, `_easterDate` hour literal, monthMap range guard ×3). | **Equivalent mutants.** Whole-domain witness searches over years 1–9999 found no distinguishing input for either computus mutant; the hour literal only dodges DST and never reaches the extracted month/day; the month-range guard is observationally subsumed by the per-row `row.month !== numericMonth` filter and kept only to skip a pointless year expansion. Every killable mutant (81/87) is pinned, most by the five-century anchor table with witness years 151/1700/3165. |
| R11 | `validDateParts`' **year** and **day** integer guards look unasserted (both mutants survive). | **Equivalent mutants.** An exhaustive witness search over 15 hostile value types found no distinguishing input: `d.getFullYear() === parts.year` and `d.getDate() === parts.day` compare strictly against the raw value, so the roundtrip check subsumes them. Only the **month** guard, whose comparison coerces (`"5" - 1`), is load-bearing; its regression coverage landed with T470. |
| R23 | The suite depends on the host timezone. | Re-verified on 2026-07-30: the full JS suite passed 926/926 with every coverage gate under each of `TZ=Pacific/Auckland`, `TZ=America/Sao_Paulo`, and `TZ=UTC`. `test/helpers/clock.js` freezes the wall clock and the tests pin `LOCAL_TIMEZONE` explicitly. |
| R13 | `metadata.json` is missing `icon` / `author`. | Cinnamon's `requiredProperties` for an applet are `uuid`, `name`, `description`; `author` belongs in `info.json` per the Spices spec, and is there. `max-instances: -1` is valid. |
| R14 | The `5.4/` shims are indirection; the root modules "depend on files that only exist in the parent". | Intended and correct on every declared version: Cinnamon's applet manager roots `imports.applets` at the applets folder, so `[uuid]` is the parent directory. The thin shims work, `gjs_import.test.js` pins them, and the former `require()` host-detection risk was resolved with T464. |
| R16 | `metadata.json` omits Cinnamon 6.6, so the applet is incompatible with the installed desktop. | Re-verified against Cinnamon 6.6.9's loader: `cinnamon-version` entries are minimum compatible versions, and the declared 5.4 entry admits 6.6. The compatibility test models that loader rule directly. |
| R17 | Sonar `css:S4654`: `icon-size` is an unknown CSS property. | **False positive.** Cinnamon's shipped `/usr/share/cinnamon/theme/cinnamon.css` uses the same property throughout; it is an St theme property, not browser CSS. The applet's `icon-size: 4em` is valid Cinnamon styling. |
| R18 | Sonar's remaining analyzer-labelled bugs: unused `Tooltip` constructions, implicit string sort, an empty-matching URL regex, and an unseeded `reduce`. | **False positives.** Cinnamon `TooltipBase._init` registers actor/stage callbacks holding each tooltip instance; package paths intentionally sort lexicographically; `urlForLog` intentionally accepts an empty optional path; and `joinPhrases` returns before `reduce` when the array is empty. Existing tests exercise each behavior. |
| R19 | Sonar's two vulnerabilities in `test/helpers/appletFixture.js` and `test/helpers/coverage.js`. | **False positives outside shipped code.** `"/tmp/cache"` is a return value in an in-memory GLib test double, not a filesystem write, and the coverage harness invokes the same developer/CI `node` selected by the already-trusted process environment. They do not affect the shipped applet or its local production gates; the related Sonar reconfiguration was rejected in R22. |
| R20 | Sonar's mass `var`, object-spread, class-field and test-stub findings. | Not defects. All `javascript:S3504` sites are zero-indented legacy GJS module bindings whose `var` visibility is the import/export mechanism covered by R05/D02. The remaining findings are compatibility-sensitive alternatives, deliberate empty test doubles, or trivial local style; changing them wholesale adds runtime-floor risk without correcting behavior. The feasible optional-chain and own-property cleanups were applied after re-evaluation, using older-runtime-safe equivalents in production GJS. |
| R21 | Sonar `javascript:S8786`: two end-whitespace regexes can backtrack super-linearly. | Re-verified after T574: every input is bounded before either regex, including event/holiday names at 300 code points, configured formats and rendered stamps at 256, panel labels at 64, clock labels at 24, and at most eight configured clocks. Rewriting the regex separately adds no distinct mitigation. |
| R24 | T701: prefer an authoritative empty holiday result over an earlier provider failure (`holidayAdapters.js` exhaustion picking `failure \|\| emptyResult`). | **The premise is wrong: the current outcome is order-independent and deliberate.** An empty payload is believed only when *every* provider returns one — stated in `_classify`'s comment and pinned by two tests ("an empty answer is believed once every provider gives one", "an empty answer followed by a hard failure is not cached as success"). Verified empirically on 2026-08-05 by driving the real chain both ways: failure-then-empty and empty-then-failure both yield `last_error = "Holiday service unavailable"` with the year uncached, so no reordering bug exists. Implementing the row would make the answer depend on chain order — and `orderProvidersByLastSuccess()` reorders the chain at runtime, so the same evidence would produce a holiday-free year on one run and an error on the next. The unanimity rule exists because an empty array is exactly how Enrico reports a country it does not cover: believing one provider's empty while another was unreachable would cache a wrong holiday-free calendar for the 50-day period. |
| R22 | Replace SonarCloud automatic analysis with scoped CI analysis that imports coverage and distinguishes tests from production sources (T557). | Rejected explicitly by the maintainer on 2026-07-23. No implementation will be pursued; the existing local and CI coverage gates remain authoritative. |

## Recorded decisions

| id | decision |
|----|----------|
| D01 | Keep the third-party endpoints rather than a bundled data set or a single vendor. A bundled table goes stale and covers fewer countries; a single provider breaks whenever it is down — the fallback chain exists because the primary is a hobbyist service. Risk accepted with mitigations (weather opt-in; one-time local-tzdata holiday default with visible third-party disclosure and explicit disable/override; size caps; bounded spans; shape checks in and out of cache; colour filtering; no URL logging). |
| D02 | **Keep the widened export surface.** ~80 exported names have no cross-module consumer — used inside their own file and otherwise only by `test/`. The applet runs inside a compositor where a bug costs the whole session; testing only through the composition root would be slow and flaky. **This does not license the inverse:** production must not bend to a test double's shape. |
| D03 | **`author` is the display name "Geraldo Netto", not the GitHub handle.** `info.json`/`package.json` use "Geraldo Netto"; `schema_static.test.js` asserts it. The Spices convention prefers the handle, but the display name is the maintainer's explicit choice. |

## Resolved merge choices

Decisions from the calendar@ccprog / Simon Wiles merge. Kept so future audits do not re-pick them.

| id | resolution |
|----|------------|
| G01 | Kept the `calendar@ccprog` world-clock settings workflow (Cinnamon-native settings, up to 8 user clocks plus built-in UTC/local, region/city selection, IANA/city entry, timezone validation, inherited coverage). Simon Wiles' separate GTK editor remains unported. |
| G02 | Kept `calendar@ccprog` locale-aware weekend behaviour (locale-derived work weeks plus a configurable one- or two-day weekend length). |
| G03 | Kept the copied `calendar@ccprog` visual assets as the initial merged set. The Chronos screenshot was refreshed on 2026-07-16; the icon was replaced with an original design and the orphaned root `calendar.png` deleted on 2026-07-18. |
