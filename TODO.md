# TODO

## Open

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| T1146 | open | low | s | Reject or safely normalize embedded NULs before handing saved weather locations to GTK. normalize_weather_location accepts Gen\u0000ova, but native Gtk.Entry displays only Gen; pressing Enter then writes Gen over the original setting and can select a different place. This occurs without widget destruction and is distinct from the lone-surrogate encoding failures in T1126. Keep Python/JS input policy consistent, make refused values recoverable, and cover NULs at the beginning/middle/end through construction, external updates and edit commits. |
| T1145 | open | medium | m | Cancel superseded and destroyed calendar-plugin loads before reading, decoding and validating their files. calendarPluginLoader.js:73,113,125 passes null cancellables to Gio, and the generation check at 169 runs after _loadOne has validated the manifest at 183. Native CJS/Gio reproduction starts three loads of one valid 744 KB, 4096-event plugin, then immediately destroys the loader: all three abandoned reads and full validations still finish, decoding about 2.16 MB and taking about 789 ms despite delivering no callbacks. Own and cancel per-generation I/O, skip stale decoding/validation and always close streams; cover repeated refresh and destruction while reads are pending. |
| T1144 | open | medium | s | Preserve weather location when its settings widget is destroyed. The CommitOnEditEnd destroy callback (chronos_settings_widgets_common.py:285–288) invokes weather commit_edit after GTK has cleared the entry text (chronos_settings_widgets_weather.py:193–194,222). Native GTK with the production widget and an in-memory settings store reproduces an untouched stored/displayed Genova and no writes before WeatherLocationEntry.destroy(), followed by a write of the empty string. Preserve the pending value while the entry is alive or commit before child teardown; closing settings must retain untouched values and any intended pending edit. Add native destruction coverage because the existing text-preserving GTK stubs miss this data loss. |
| T1143 | open | low | s | Align weatherIcon and its tests with the documented Open-Meteo code enumeration instead of accepting gaps inside WMO_CONDITION_RANGES (weatherServiceAdapters.js:76–101). Codes 46,47,52,54,58,59,60,62,64,72,74,76,97,98 are absent from the [provider's code table](https://open-meteo.com/en/docs#weathervariables) but receive confident condition icons. The later WMO_CONDITIONS fixture and unknown-code assertions promise the provider contract, while the earlier fuzz test explicitly admits 97/98. Use explicit supported-code membership, keep unknown conditions undescribed, and check every integer 0..100 against the same complete contract. |
| T1142 | open | low | s | Keep accepted weather readings finite through unit conversion in weatherFormat.js:97–100. A composed forecast refresh accepts finite temperature 2e307 as a successful Open-Meteo reading, but celsius * 9 / 5 overflows its intermediate product and displays Infinity°F, even though the converted result 3.6e307 is representable. Avoid unnecessary intermediate overflow and reject/fall back when the final display conversion cannot be finite. Cover large finite values, Number.MAX_VALUE and normal positive/negative controls through provider, cache and display boundaries. |
| T1140 | open | low | xs | Remove the obsolete-runtime ownsProperty helper in holidayServiceAdapters.js:61–66 and use Object.hasOwn at its two call sites. Its comment retains the helper because older Cinnamon releases lack Object.hasOwn, but current shipped modules already require Object.hasOwn and the project explicitly requires no compatibility. Update the affected fixtures/comments to the current runtime contract. |
| T1138 | open | low | m | Build month grids from civil dates instead of fixed 24-hour increments in 6.0/calendarMonthWindow.js:_beginDate/_buildDays. Production arithmetic under Pacific/Apia in December 2011 places December 31 (Saturday) in Friday's column and shifts the following cells; Pacific/Kwajalein in September 1969 duplicates September 30 and misaligns the remaining weekdays. calendarGrid.js:493–502 positions cells by index, so it cannot correct these timezone date-line transitions. Preserve unique civil dates and weekday alignment, explicitly handle skipped local dates, and add skipped/repeated-day timezone fixtures. |
| T1136 | open | low | s | Reject out-of-range HTTPS source ports in both calendar manifest validators (calendarPluginData.js:105–106 and chronos_calendar_plugin_data.py:38–40). Their URL patterns allow any one-to-five-digit port, accepting https://example.org:65536 and :99999 as provenance links even though these exceed the valid port range and URL parsing rejects them. Bound explicit ports to 0..65535 and share boundary fixtures across Python and JS. The strict calendar input fuzz audit reproduces this through calendar.plugin.source-port from case 0 with any seed; malformed-manifest and neighbor checks also expose acceptance and loading of invalid manifests. |
| T1135 | open | medium | s | Preserve the selected civil date when the local timezone changes. CalendarNavigation stores an absolute JS Date, while 6.0/calendar.js:327–330 refreshTimezone only invalidates the month window. Native private-process reproduction: select June 15, 2026 at noon in Pacific/Kiritimati, switch process TZ to Pacific/Honolulu, clear JS date caches and call the real applet._onTimezoneChanged; getSelectedDate becomes June 14. Retain civil year/month/day and rebase selected and queued navigation dates before applet.js:358–361 refreshes world clocks and ticks the agenda. Keep grid and event-window selection aligned with eventWindow.js's existing civil-date-preservation contract. |
| T1134 | open | low | xs | Correct the religious-observance flag contract comments in religiousCatalog.js:16–17 and holidayRecord.js:143–153. They claim each religion ID is the second flag and participates in styling, while religiousHolidays.js:438–446 intentionally emits only religious_holiday and test/religious_holidays.test.js explicitly asserts that contract. Describe the current single-flag behavior and clarify why religion IDs remain reserved provider input if that filtering is still intended. |
| T1132 | open | low | xs | Remove the compatibility-only aviationWeatherUrl wrapper/export in weatherServiceAdapters.js:203–206,637 and its obsolete test/import fixtures. Its comment explicitly retains the old single-URL API, it discards the second antimeridian query, and repository searches find only test callers; the production provider already uses aviationWeatherUrls. Retaining this superseded API conflicts with the project's no-compatibility policy. Keep the actual plural adapter and its antimeridian coverage. |
| T1131 | open | low | s | Finish Go to today keyboard-focus handover after the target month grid exists. In 6.0/appletPanelStatus.js:_setHomeEnabled, focusSelectedDay runs during calendarNavigation.js:setDate after selectedDate changes but before the old grid is rebuilt. Native reproduction: browse June 2026, focus Go to today and press Return; the selection becomes September 8, but focus remains on the now-insensitive home actor with can_focus=false because September was absent from June's 42 cells. Transfer focus after date navigation/rendering completes and cover returning from a non-overlapping month. |
| T1130 | open | low | s | Align the runtime plugin directory with the documented Python absolute-only XDG_DATA_HOME policy in calendarPluginLoader.js:142. Native GLib.get_user_data_dir returns `relative` when XDG_DATA_HOME=relative, while Python plugin_directory ignores that value and uses ~/.local/share. Settings therefore imports/lists/enables a personal plugin in one directory while the applet looks relative to its working directory and cannot load it. Use the same fallback policy and cover relative, absolute, empty, and unset values across the actual runtime and settings paths. |
| T1129 | open | medium | s | Stop handled month-browse wheel events from propagating through 6.0/calendarNavigation.js:onScroll and 6.0/calendar.js:_onScroll. Native Cinnamon with MenuViewport receives one wheel-down over a day cell, advances September 8 to October 8, and also scrolls the outer body from 0 to 50 pixels, visibly shifting the grid. Return Clutter.EVENT_STOP through both callback layers when navigation handles the input; retain propagation for ignored input as appropriate. Cover the composed handler and surrounding viewport so one notch performs one action. |
| T1127 | open | low | s | Make truncated world-clock labels reachable by pointer hover in 6.0/worldclocks.js:53–56,93–100. Native Cinnamon displays a long label as `A deliberately long Tok…`, but the non-reactive containing St.Widget and non-reactive label prevent enter events, so its full-label tooltip never appears. Native picking hits the surrounding ScrollView; enabling reactivity on both the container and label makes the existing tooltip work. Keep labels non-actionable and add hover coverage that accounts for parent pickability. |
| T1125 | open | medium | s | Sanitize and bound unavailable-calendar display filenames in chronos_settings_widgets_calendars.py:248,265–273 while retaining the exact basename separately for deletion. A native GTK reproduction with a regular file named by bytes `bad-\xff.json` raises TypeError converting the surrogateescaped label to gchararray and aborts CalendarPluginChoices construction. A 240-character filename expands the settings window to 1,759 pixels on a 1,366-pixel display; newline and bidi names enter the label verbatim. Use valid Unicode, control-safe bounded display text and wrapping/ellipsizing; cover non-UTF8, long, and control-character filenames without breaking valid neighbors or file removal. |

## Blocked / Deferred

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |

## Rejected / Won't fix

| id | status | severity | effort | description |
| --- | --- | --- | --- | --- |
| T1016 | wont_fix | medium | xs | Keep the current gettext catalog selection without per-message fallback from user to system catalogs; the maintainer declined translation changes. |
| T774 | wont_fix | — | — | The shipped paryushana-start dates do not consistently match a Jain tradition, and the key names no sect. The maintainer declined choosing one; revisit only when the encoded tradition is named. |
| T775 | wont_fix | — | — | Guru Gobind Singh Jayanti cannot fit one Gregorian date per year without choosing a Sikh reckoning; active calendars can produce zero or two dates. The maintainer declined choosing one. |
| T776 | wont_fix | — | — | Do not implement Chinese observances without an in-tree ephemeris and a bounded authoritative range. ICU disagrees with validated civil dates near midnight, historical time standards differ, and future delta-T makes some dates undecidable; carrying that machinery was declined. |
| T816 | wont_fix | — | — | Do not duplicate every Python coverage percentage and override in contributor prose; the executable coverage gate remains authoritative. |
| R05 | rejected | — | — | GJS var barrel re-exports are the module import mechanism, not unused indirection, and tests reach modules through them. |
| R06 | rejected | — | — | The commented gnome-calendar spawn is a documented compatibility switch with an adjacent maintainer note. |
| R08 | rejected | — | — | A panel applet does not require watchdogs, metrics, circuit breakers, or a health surface; provider failover and freshness checks are proportionate. |
| R09 | wont_fix | — | — | The calendar color strip cannot announce a calendar name because cinnamon-calendar-server publishes only color. It is exposed as a separator so screen readers skip it; an upstream contract change is required. |
| R12 | rejected | — | — | The surviving religiousHolidays mutants are equivalent: exhaustive witnesses distinguish none, the hour never reaches the returned date, and the month guard is subsumed by row filtering. |
| R11 | rejected | — | — | The surviving validDateParts year/day guard mutants are equivalent because strict Date round-trip comparisons subsume them; only the coercing month guard is independently load-bearing. |
| R23 | rejected | — | — | The suite is not host-timezone dependent: its clock is frozen, LOCAL_TIMEZONE is explicit, and the full gate passes under widely separated timezones. |
| R13 | rejected | — | — | metadata.json need not contain icon or author; Cinnamon requires uuid, name, and description, while the Spices author belongs in info.json. |
| R14 | rejected | — | — | The 6.0 versioned loaders import root modules through Cinnamon's applet-folder namespace; the deployed Cinnamon 6.6 desktop requires this module mechanism, covered by import tests. |
| R16 | rejected | — | — | Cinnamon-version entries control minimum admission independently of versioned-directory selection; metadata admits the verified Cinnamon 6.6 series while its loader selects 6.0/. |
| R17 | rejected | — | — | icon-size is a valid St theme property used by Cinnamon itself, so Sonar's browser-CSS warning is a false positive. |
| R18 | rejected | — | — | Sonar's tooltip, lexical-sort, optional-path regex, and reduce findings are false positives: lifecycle callbacks retain tooltips and the other behaviors are deliberate and tested. |
| R19 | rejected | — | — | Sonar's reported test-helper vulnerabilities are not shipped boundaries: /tmp/cache is an in-memory double value and the harness invokes the already-trusted Node executable. |
| R20 | rejected | — | — | GJS export bindings, deliberate test doubles, and local style are not defects by themselves; change them for a concrete behavior or maintenance benefit. |
| R21 | rejected | — | — | The two end-whitespace regexes receive bounded inputs, so their theoretical backtracking does not create an unbounded workload and a separate rewrite adds no mitigation. |
| R24 | rejected | — | — | An empty holiday response is trusted only when every provider agrees. Preferring one empty response over another provider's failure would cache unsupported countries as holiday-free and make results depend on runtime provider order. |
| R25 | rejected | — | — | Applying the Open-Meteo population floor per candidate fixes Malmo but misroutes Genova to Guatemala. The current primary refusal plus typed-name-ranked Nominatim fallback resolves both correctly. |
| R22 | wont_fix | — | — | The maintainer declined replacing SonarCloud automatic analysis with scoped CI analysis; local and CI coverage gates remain authoritative. |
| R27 | rejected | — | — | Enumerating requested versus inflight locale queries is equivalent for reachable state because both maps are written and cleared together and cancellation guards on the inflight record. |
| R28 | rejected | — | — | EventIndex event-id and day maps are synchronized by register, remove, clear, and rebuild; their only temporary difference is inside one synchronous call and cannot be observed. |
| R29 | rejected | — | — | A resync retry cannot inherit a temporary overflow warning because the reload idle retires and publishes the marker before any retry runs. |
| R30 | rejected | — | — | Holiday annotation passes do not share live counters: every render advances a generation and stale callbacks return before mutating counters, credit, status, or cells. |
| R31 | rejected | — | — | The reported scan measured cyclomatic complexity, not the documented cognitive limit; JavaScript and Python cognitive-complexity gates already fail at 11. |
| R26 | wont_fix | — | — | Opening world-clock settings normalizes and rewrites saved rows, potentially discarding invalid, built-in, or ninth-and-later entries. The maintainer knowingly declined changing that behavior; reverse only by a new decision. |
