# cinnamon-chronos TODO

## Findings

| ID | Category | Severity | Status | Effort | Description | Notes |
|----|----------|----------|--------|--------|-------------|-------|
| T862 | resource management; performance | Medium | open | S | Aborting a lazy HTTP session leaves the session and its connection state retained after applet removal. | **[found 2026-08-06 during full production rescan]** `LazyHttpSession.abort()` calls the Soup session's `abort()` but leaves `_session` pointing at it. Both the weather repository and holiday service retain their `LazyHttpSession`, and the removed applet retains those provider graphs; the project already documents that the applet object remains reachable after removal. Thus every add/remove cycle keeps two aborted Soup session wrappers and their internal pools/configuration reachable for the Cinnamon session. The lazy wrapper has no valid use after its owning provider is destroyed, and a later `get()` is already blocked by those providers' `_destroyed` guards. Fix: detach `_session` before/while aborting (with exception-safe release), so teardown both cancels traffic and releases the heavy transport object. |
| T861 | resource management; performance | Medium | open | S | Removing the applet leaves the selected day's full rendered agenda reachable. | **[found 2026-08-06 during full production rescan]** `CinnamonCalendarApplet.event_list` retains the `EventList` after `AppletMenuBuilder.destroy()`, and `EventList.destroy()` cancels renderer sources but never clears `_rows` or `_eventDataList`. The later menu-actor destruction disposes the visual tree, but up to `MAX_RENDERED_EVENT_ROWS` `EventRow` objects still retain their event records and disposed actor wrappers, while `_eventDataList` retains the selected day's source list/holiday projection. This applet explicitly has an after-removal reachability lifetime, and its calendar/world-clock/astronomy siblings already drop their comparable state in `destroy()`. Repeated add/remove cycles therefore retain the heaviest popup column. Fix: make `EventList.destroy()` release rows and agenda/model state after cancelling all renderer sources, without attempting to redraw disposed actors. |
| T860 | reliability / correctness; state machine integrity | High | open | S | A queued Nominatim job that raises at dispatch can strand its provider chain forever. | **[found 2026-08-06 during full production rescan]** `NominatimRequestQueue._drain` catches a throwing `job.start`, releases the queue slot, then rethrows. That works only when `enqueue()` drains immediately inside `ProviderUtils.attemptOrFail`'s stack. A rate-limited job normally starts later from the spacing timer, after that stack is gone; its exception reaches the GLib callback and no one calls the geocoder's `onResult`. A real synchronous edge exists before `IoUtils.httpGetJson` can apply its dispatch guard: the default repository evaluates `this.getHttpSession()` first, and lazy `Soup.Session` creation may throw during reload/disposal. The weather repository's inflight key and a city round's `outstanding` counter then never settle, so the request and remaining concurrency slot are lost until the next full refresh. Fix: give queued jobs an explicit failure continuation (or wrap their dispatch at the queue seam), release first, then report failure into the provider chain for both immediate and delayed starts without double-settling. |
| T859 | reliability / correctness; resource management | Medium | open | S | A synchronous locale-output dispatch failure leaves its deadline live and counts one failed query twice. | **[found 2026-08-06 during full production rescan]** `_requestInfo` arms `_armDeadline(...)` before `_readLocaleOutput(...)` registers `communicate_utf8_async`. If that async API raises synchronously, the outer catch calls `_degrade` directly, releasing the query, incrementing `attempts`, notifying defaults, and arming a retry—but the already-scheduled deadline still owns an unsettled `settlers`. Five seconds later it calls `settlers.fail()`, degrades the same attempt again, increments the counter again, and may schedule a second retry. One dispatch failure can therefore consume two of the three-attempt budget and retain the finished subprocess/cancellable until the deadline fires. Fix: register the async read before arming its deadline (callbacks cannot run until control returns to the main loop), or settle through the same `settlers.fail` object and explicitly disarm the deadline. |
| T858 | reliability / correctness; multi-instance | High | open | S | One throwing locale listener prevents later applet instances from receiving the resolved locale and can prevent retry scheduling. | **[found 2026-08-06 during full production rescan]** `_storeInfo` filters the process-wide `listeners` array and invokes it with bare `forEach`. A listener exception stops delivery to every later registered applet instance. On a failed locale query, `_degrade` calls `_storeInfo` before it arms the 60-second retry, so the same exception also exits `_degrade` early and permanently leaves that locale on defaults with attempts remaining but no timer. On success, `_readLocaleOutput` catches the exception as if parsing/IO failed, but `settlers` is already settled and cannot recover the missed listeners. Fix: settle all matching listeners with exception isolation and only rethrow after required retry/state transitions are secured; notification failures must not be reclassified as locale-query failures. |
| T857 | reliability / correctness; robustness / recovery | High | open | S | A synchronous calendar range-call failure bypasses fetch failure handling and can leave the displayed month empty indefinitely. | **[found 2026-08-06 during full production rescan]** `EventsManager._dispatchMonthFetch` increments the fetch generation and calls `CalendarServerConnection.setTimeRange(...)` without a `try/catch`; the adapter directly calls `call_set_time_range`. A live proxy can still raise synchronously during owner-loss/disposal races. By then a changed month has already reset the event index and the prior retry chain/GC timer were cancelled, but `call_finished` will never run to set `refresh-error-changed` or arm backoff. The exception escapes a selection/idle callback and no path owns this failed month. Fix: funnel dispatch-time throws through the same current-generation failure transition as an async `finishSetTimeRange` failure, without trying to finish a result that does not exist. |
| T856 | state machine integrity; reliability / correctness | High | open | M | Any exception while applying one queued calendar mutation can freeze the ordered mutation stream for the rest of the session. | **[found 2026-08-06 during full production rescan]** `_apply_next_event_mutation` removes the head only after `_apply_event_mutation` returns. It runs both synchronously from the first DBus signal and later inside an idle. If indexing, reconciliation, or a consumer reached through `_flush_pending_emit`/`emit` raises, the head remains, no next idle is armed, and later `_enqueue_event_mutation` calls see a non-empty queue so they never restart immediate draining. All later add/update/remove/client-disappeared signals accumulate behind the poisoned head; the grid and agenda stop updating. Fix: make the queue transition exception-safe—release/account for the failed head, preserve ordering by collapsing remaining uncertain mutations to one authoritative resync, schedule that resync, and rethrow/log only after queue progress is guaranteed. |
| T855 | reliability / correctness; robustness / recovery | High | open | S | Calendar-server consumer callbacks can corrupt connection recovery state. | **[found 2026-08-06 during full production rescan]** `CalendarServerConnection._calendar_server_ready` runs `callbacks.onReady()` inside the same broad `try` that finishes and installs the proxy. If the consumer callback raises, a successfully connected proxy is torn down and a retry is armed; a repeatable UI-side exception becomes an endless reconnect loop. Conversely, `_handle_name_owner_notify` calls `callbacks.onStatusChanged()` before `queueRetry()`, so a callback exception leaves the proxy disconnected with no watch or retry and events stay dead for the session. These callbacks are notifications, not connection operations, and must not decide whether lifecycle cleanup/recovery runs. Fix: keep proxy finish/installation error handling separate from `onReady`, and put owner-loss notification around a `finally` (or queue retry before notification) so recovery always advances while callback errors still surface. |
| T854 | robustness / recovery; reliability / correctness | Medium | open | S | Event timestamp validation assumes UTC year bounds also guarantee a valid local `GLib.DateTime`. | **[found 2026-08-06 during full production rescan]** `eventData.js:eventUnixTime` accepts the complete UTC range for years 1–9999, then `EventData` passes both endpoints to `GLib.DateTime.new_from_unix_local` and dereferences them. At the upper endpoint (`253402300799`), any positive local offset crosses into year 10000; at the lower endpoint (`-62135596800`), any negative offset crosses into year 0. GLib's documented `DateTime` domain ends at 9999 and construction may return `null`, so a feed can pass the numeric guard and crash the DBus delivery handler at `this.end.compare(...)` or `date_only(...)`. Fix: construct each local `DateTime` once, reject the event when either result is null, and retain the existing wire-number/range validation as the cheap first gate. |
| T853 | reliability / correctness | Medium | open | S | One throwing holiday-fetch waiter prevents every later waiter for the same year from being notified. | **[found 2026-08-06 during full production rescan]** `HolidayService._acceptYear` and `_abandonYear` both iterate the callback array returned by `HolidayInflight.settle` with a bare `for ... waiting()`. The 42-day grid intentionally joins two month requests to one year fetch, so this is a real fan-out path: if the first repaint callback raises (for example during an actor lifecycle race), the second month never receives the settled data/status. The inflight entry was already deleted, so the lost callback is not retried by this request. Fix after T852: use the same settle-all callback discipline—invoke every waiter, retain the first exception, then rethrow it after all waiters have run. |
| T852 | reliability / correctness | Medium | open | S | One throwing cache-ready callback discards every later callback waiting behind the same holiday-cache load. | **[found 2026-08-06 during full production rescan]** `HolidayCache._flushReady` copies and clears `_onReady`, then calls `waiting.forEach((callback) => callback())` without exception isolation. Initial grid construction queues multiple month continuations behind the asynchronous disk read. If one continuation raises, `forEach` stops, the remaining callbacks have already been removed from `_onReady`, and their months do not fetch or repaint until some unrelated later update. Fix: invoke every queued callback, remember the first exception, and rethrow only after the queue is fully settled; keep `_loading` cleared before dispatch. |
| T851 | reliability / correctness | Medium | open | S | A synchronous stream-read dispatch failure escapes the HTTP adapter without settling the request. | **[found 2026-08-06 during full production rescan]** `ioUtils.js:_readNextChunk` calls `stream.read_bytes_async(...)` with no `try/catch`. Gio async APIs may raise before registering their callback (the same failure shape `_dispatchOrFail` already handles for `session.send_async`), especially around disposed streams/sessions during xlet teardown. On a first or later chunk dispatch failure, neither `exits.abort` nor `deliver` runs: the stream stays open, the whole-request deadline stays armed until 60 seconds, and the weather/holiday provider chain never receives its null result. Fix: route synchronous `read_bytes_async` throws through `exits.abort`, preserving the read's settle-once path and stream/cancellable cleanup. |
| T840 | platform; documentation | Medium | open | S | Nothing verifies the shipped Python against the interpreter the oldest admitted desktop carries. | **[found 2026-08-06 while working T837]** The settings dialog is Python that runs in the *user's* process, not in Cinnamon, so its floor is the user's interpreter — and the two floors the repository states do not meet. `metadata.json:10` admits `"cinnamon-version": ["6.0"]` as a minimum, and Cinnamon 6.0 shipped in Linux Mint 21.3 on Ubuntu 22.04, whose system Python is 3.10; `README.md:16-17` states the runtime requirement as "Python 3 with GTK bindings (`python3-gi`)" with no version at all. T837 raised the *development* floor to 3.12, which is the right call for a runner image but leaves the gates matrix exercising 3.12 and 3.14 only. The code is fine today — every tracked `.py` still parses at `feature_version=(3,8)` — so nothing is broken; what is gone is the check that keeps it that way, and a `match` statement or a 3.11-only call would now land green and crash the settings dialog on a desktop `metadata.json` says is supported. Fix: pick one and make the two agree — either state a runtime Python floor in the README's "To run the applet" list and hold the shipped modules to it (a syntax-only check at that version is enough and needs no interpreter), or raise `cinnamon-version` to the release whose distributions all carry 3.12. |
| T845 | reliability / correctness; documentation | Medium | open | M | An in-place update followed by an xlet reload runs the new `6.0/` code against the previous session's cached root modules, and the applet dies in its constructor. | **[found 2026-08-06 on a live Cinnamon 6.6.9 desktop, reproduced three times]** Cinnamon re-evaluates the versioned tree on `org.Cinnamon.ReloadXlet` but not the root modules: `6.0/weather.js` and `6.0/worldclockData.js` resolve through `imports.ui.appletManager.applets["chronos@geraldo-netto"].<name>`, and that importer namespace is cached for the life of the Cinnamon process. Observed after rsyncing a tree containing `bf3df45` and `31eb4d3` over an install that predated them: `Weather.registerWeatherConsumer is not a function` thrown from `appletLifecycle.js:284` aborted `CinnamonCalendarApplet` construction, and the `_destroy` that followed threw twice more (`cancelPendingWeatherRequests`, `releaseWorldclockConsumer`) — three errors and a dead applet from one reload. Live introspection confirmed the mechanism rather than a packaging fault: `weatherProviders.locationCacheKey` (added earlier) resolved while `registerWeatherConsumer` and `cancelPendingWeatherRequests` (added later, same file) were `undefined` on the cached module object. A full `RestartCinnamon` clears the cache and loads clean. The teardown `try/catch` in `applet.js:60` did its job — one throwing step did not strand the others — but the user is left with no applet and a bare TypeError naming neither the cause nor the cure, and the applet-manager "Reload" button and Looking Glass both reach this path. Fix: have `initProviders` check the consumer-registration functions it needs and, when they are missing, report the upgrade-in-progress state through `AppletIssueReporter` with the action ("restart Cinnamon to finish the update") instead of throwing a bare TypeError; and say in `README.md` step 3 that an in-place update needs a full Cinnamon restart, not an xlet reload. |
| T847 | accessibility; UI / UX | Low | open | S | Below the clock cap the Add button is stripped of the tooltip Cinnamon gave it, leaving an icon-only button with no accessible name. | **[found 2026-08-06 driving the real settings window over AT-SPI]** `chronos_settings_widgets_worldclocks.py:642-645` clears the Add button's tooltip with `set_tooltip_text("")` plus `set_has_tooltip(False)` whenever the list is under `MAX_CLOCKS`. The comment above it says the button "had no tooltip", but Cinnamon's `TreeListWidgets.py:183` sets `_("Add new entry")` on it — the premise held for some earlier Cinnamon and does not hold for 6.6.9, so the else-branch now erases a stock label instead of clearing an empty one. Measured on the live World Clocks page with 7 of 8 clocks saved: the Add button reports an empty accessible description while its four siblings report "Remove selected entry", "Edit selected entry", "Move selected entry up" and "Move selected entry down". The button is icon-only (`xsi-list-add-symbolic`), so a screen reader announces an unnamed push button — the same defect class as T842, in the widget that starts the flow. Fix: restore `_("Add new entry")` in the else-branch instead of `""`, keeping `CLOCK_LIMIT_MESSAGE` for the full case. |
| T848 | documentation | Low | open | S | The README's manual install cannot remove a file that upstream deleted, and a stale version directory stays loadable. | **[found 2026-08-06 reinstalling onto a live desktop]** `README.md:88-90` installs with `rsync -a --exclude '__pycache__'` and no `--delete`, so anything the repository dropped survives in the user's applet folder forever. The install on this machine still carried a whole `5.4/` tree, deleted from the repository when the multiversion floor moved to `6.0`; for a `"multiversion": true` applet that directory is not inert, it is a candidate the loader picks on any Cinnamon older than 6.0. Adding `--delete` alone does not fix it either: the `__pycache__` exclude protects the directory from deletion, so rsync removes `5.4/`'s sources and leaves orphan `.pyc` files behind, which is what happened here and had to be cleared by hand. The same command also copies editor backups — the working tree carries gitignored `po/*.po~` files that `rsync -a` happily installs, though `package-spices.mjs` keeps them out of the release archive because it packages tracked files only. Fix: document `rsync -a --delete --delete-excluded --exclude '__pycache__' --exclude '*~'`, and say why `--delete-excluded` is needed rather than leaving it as an unexplained flag. |
| T849 | UI / UX | Low | open | S | The clock dialog rejects invalid and built-in timezones but accepts one that is already saved. | **[found 2026-08-06 driving the Add-clock dialog over AT-SPI]** With `Asia/Tokyo` already in the saved list, typing it again left the preview reading "Timezone to save: Asia/Tokyo" and the OK button sensitive; the other two rejection paths behaved correctly in the same run (`Not/AZone` → "Invalid timezone", `Europe/Rome` → "UTC and local time are already shown as built-in clocks", OK insensitive in both). `TimezoneResolver.normalize`'s `reserved` argument is not the missing check — it means "is a built-in", and it is wired correctly — so nothing in the dialog consults the rows already in the model. `MAX_CLOCKS` is 8, so a duplicate spends one of eight slots to draw the same time twice. Fix: decide whether a duplicate zone under a second label is a use case worth keeping; if not, reject it with its own message alongside the built-in one, and if so, record the decision so a later audit does not re-raise it. |
| T850 | accessibility | Low | open | S | Combobox settings rows announce their stored value where the user sees a label. | **[found 2026-08-06 driving the settings window over AT-SPI]** The Weather units combo reports the accessible name `si` while displaying "SI (Celsius)", and the ten `region_*` combos report `global` while displaying "Nationwide only"; a screen reader reads the schema value, not the option. The applet's own custom combo does not have the defect — "Mark as weekend days" reports `Two days`, the label — and the Country combo escapes it through its entry child, whose text is `Italy` even though the combo itself reports `ita`. The affected rows are the ones declared `"type": "combobox"` in `6.0/settings-schema.json`, so the naming comes from Cinnamon's stock widget rather than from this repository, but it manifests in this applet's settings and the in-repo custom-widget pattern already shows the working shape. Fix: confirm against a stock xlet that the behaviour is upstream, then either carry a custom combo for these rows or record it as blocked upstream with the Cinnamon issue linked. |
| T815 | documentation | Low | open | S | Comments and a shared test fixture name a module that no longer owns the code. | **[found 2026-08-06 during a full rescan]** `worldclockData.js:54` says `TZ_NO_REGION` is "Named to match chronos_settings_widgets_common.py's TZ_NO_REGION", `test/fixtures/timezone_city_cases.json:2` says the JS `timezoneCityName` and "the Python `local_city_name` (chronos_settings_widgets_common.py)" must agree, and `test/worldclocks.test.js:517` repeats the reference. Neither symbol is in that file any more: `TZ_NO_REGION` is at `chronos_timezone_data.py:36` and `local_city_name` at `:134`, while `chronos_settings_widgets_common.py` now holds only the folded matcher and the resolver singleton. These pointers are the only thing linking two implementations of a contract that has no automated cross-language gate (T786), so a maintainer following them lands in the wrong file. Fix: repoint all three. |
| T816 | documentation | Low | open | S | `AGENTS.md` and CI understate the enforced Python coverage gate. | **[found 2026-08-06 during a full rescan]** `AGENTS.md:15-16` and `.github/workflows/ci.yml:62` both say "Python 98 % lines per file", but `test/helpers/coverage.py:33-35` declares `LINE_THRESHOLD = 98.0`, `BRANCH_THRESHOLD = 90.0` and `FUNCTION_THRESHOLD = 100.0`, and `gate_failures` (`:327-338`) applies all three per file, plus a `LINE_OVERRIDES` bump to 100 % for `chronos_settings_widgets_common.py`. The Python gate is in fact identical to the JS gate. The understatement tells a contributor that Python branch and function coverage are unenforced, which surprises them at commit time. Fix: state "JS and Python alike: 98 % lines / 90 % branches / 100 % functions per file" in both places and mention the per-file override. |
| T818 | documentation | Low | open | S | The README overstates when CI runs. | **[found 2026-08-06 during a full rescan]** `README.md:302` says the workflow runs the gates "on every push and pull request", but `.github/workflows/ci.yml:12-16` restricts the push trigger to `branches: [develop]` and `tags: ['v*']`. A push to a feature branch with no open PR runs nothing. Commit `7acf492` removed `main` from that list but kept the restriction. Fix: say "on every pull request, and on pushes to `develop` and `v*` tags", or widen the trigger to match the sentence. |

## Rejected, deferred and won't fix

| ID | Disposition | Item | Rationale |
|----|-------------|------|-----------|
| T774 | Won't fix | `paryushana-start` 2025 matches no Jain tradition, and the key names no sect. | **[declined by the maintainer 2026-08-06]** Jain practice forks further than the two main sects: Svetambara Paryushan 2025 ran 22-29 August with Samvatsari on 29 August, Digambara Dasa-Lakshana ran 30 August to 8 September, and the shipped `2025: [8, 20]` is neither; the 2026 cell does match the Svetambara start, so the column is not consistently one tradition either. Tapa-gaccha keeps Samvatsari on Bhadrapada Shukla 4 and other Svetambara lineages on 5, with Sthanakavasi and Terapanth differing again. Picking a sect is a content decision, not a defect the code can settle, and the same argument reaches every lunisolar key in `TABLES`. Unpark by naming the tradition each key encodes. |
| T775 | Won't fix | `guru-gobind-singh-jayanti` cannot be represented by one date per year, and the applet has not chosen a reckoning. | **[declined by the maintainer 2026-08-06]** Two calendars are in active use and they disagree. Under Bikrami reckoning the anniversary is Poh Sudi 7, which falls twice in some Gregorian years and not at all in others - 9 January 2022 and 29 December 2022, leaving 2023 with none, then 17 January 2024 - and the table holds one cell per year. The SGPC adopted Purewal's tropical Nanakshahi calendar in 2003, moved the month starts back onto Bikrami in 2010, and by 2014 had reverted while keeping the name; the 1999/2003 version was rebranded Mool Nanakshahi in 2017 and is what the PSGPC and most diaspora gurdwaras follow. In 2024 the SGPC observed 17 January while PSGPC and US gurdwara panels had observed 5 January. The shipped `[1, 6]` for 2025 and 2026 matches neither. Choosing a reckoning is a content decision, and representing it needs the table to yield zero or two dates in a year. Unpark by deciding which calendar the applet follows and saying so per key. |
| T776 | Won't fix | The Chinese folk observances are computable, but only with an in-tree astronomical implementation and only for part of the range. | **[declined by the maintainer 2026-08-06]** The rule is fully specified by GB/T 33661-2017 (Purple Mountain Observatory) - a month starts on the day at 120 deg E containing the new moon, month 11 contains the December solstice, and the leap month is the first zhongqi-less month between consecutive month-11s - so `chinese-new-year` and `ghost-festival` are computable and `qingming` needs only solar longitude 15 deg. What is declined is building and carrying the ephemeris to do it. Three findings stand and are kept so no later audit re-derives them. **`Intl`/ICU is not a usable shortcut**: its Chinese calendar disagrees with a validated Meeus ch.49 implementation in 6 of 301 years (1808, 1814, 1916, 1954, 2027, 2030), and the Hong Kong Observatory open data confirms ICU is the wrong one for 2027 - the new moon is 2027-02-06 23:56 CST, four minutes from midnight, and ICU says 7 February. **The range would not be 1800-2100**: before 1929 the calendar used Beijing apparent solar time at 116d25'E, about 14 minutes off 120 deg E plus the equation of time, moving roughly 18 of 1595 pre-1929 new moons to a different civil date. **Two lunations are undecidable at any precision** - 2057-09-28 23:59:57 CST and 2097-08-07 23:59:39 CST - because delta-T past 2050 is not knowable to seconds; also the "second new moon after the solstice" shortcut breaks in 2033 for the first time since the 1645 reform. Unpark by deciding to carry an ephemeris in-tree and a bounded range with it; ground truth for a test is the HK Observatory conversion tables, 1901-2100. See T740, which parks the rest of the non-Hebrew observance tables. |
| R05 | Rejected | Barrel re-exports as the GJS `var` bindings importers need. | The re-exports **are** the import mechanism; tests reach the modules through them. |
| R06 | Rejected | `6.0/calendarLauncher.js:50`: commented-out `Util.trySpawn(["gnome-calendar"])`. | Deliberate documented compatibility switch with a maintainer note directly above it. |
| R08 | Rejected | No watchdog/metrics/circuit-breakers/health surface. | For a panel applet these are reasonable omissions; the provider failover with last-success ordering plus a per-year freshness gate is a coherent substitute. |
| R09 | Won't fix | Per-calendar colour strip conveys calendar identity by colour alone. | Not actionable here: `cinnamon-calendar-server` sends the calendar's colour and never its display name, so there is no name to announce. The strip is `Atk.Role.SEPARATOR` so a screen reader skips it. Needs an upstream change. |
| R12 | Rejected | Six `religiousHolidays.js` mutants survive the operator/constant audit (computus f-term `+8→+9`, month-expression `7m→8m`, `_easterDate` hour literal, monthMap range guard ×3). | **Equivalent mutants.** Whole-domain witness searches over years 1–9999 found no distinguishing input for either computus mutant; the hour literal only dodges DST and never reaches the extracted month/day; the month-range guard is observationally subsumed by the per-row `row.month !== numericMonth` filter and kept only to skip a pointless year expansion. Every killable mutant (81/87) is pinned, most by the five-century anchor table with witness years 151/1700/3165. |
| R11 | Rejected | `validDateParts`' **year** and **day** integer guards look unasserted (both mutants survive). | **Equivalent mutants.** An exhaustive witness search over 15 hostile value types found no distinguishing input: `d.getFullYear() === parts.year` and `d.getDate() === parts.day` compare strictly against the raw value, so the roundtrip check subsumes them. Only the **month** guard, whose comparison coerces (`"5" - 1`), is load-bearing; its regression coverage landed with T470. |
| R23 | Rejected | The suite depends on the host timezone. | Re-verified on 2026-07-30: the full JS suite passed 926/926 with every coverage gate under each of `TZ=Pacific/Auckland`, `TZ=America/Sao_Paulo`, and `TZ=UTC`. `test/helpers/clock.js` freezes the wall clock and the tests pin `LOCAL_TIMEZONE` explicitly. |
| R13 | Rejected | `metadata.json` is missing `icon` / `author`. | Cinnamon's `requiredProperties` for an applet are `uuid`, `name`, `description`; `author` belongs in `info.json` per the Spices spec, and is there. `max-instances: -1` is valid. |
| R14 | Rejected | The `5.4/` shims are indirection; the root modules "depend on files that only exist in the parent". | Intended and correct on every declared version: Cinnamon's applet manager roots `imports.applets` at the applets folder, so `[uuid]` is the parent directory. The thin shims work, `gjs_import.test.js` pins them, and the former `require()` host-detection risk was resolved with T464. |
| R16 | Rejected | `metadata.json` omits Cinnamon 6.6, so the applet is incompatible with the installed desktop. | Re-verified against Cinnamon 6.6.9's loader: `cinnamon-version` entries are minimum compatible versions, and the declared entry admits 6.6. The compatibility test models that loader rule directly. The floor was raised from 5.4 to 6.0 when T770 established that the HTTP layer is libsoup-3 only; the loader rule this row rests on is unchanged. |
| R17 | Rejected | Sonar `css:S4654`: `icon-size` is an unknown CSS property. | **False positive.** Cinnamon's shipped `/usr/share/cinnamon/theme/cinnamon.css` uses the same property throughout; it is an St theme property, not browser CSS. The applet's `icon-size: 4em` is valid Cinnamon styling. |
| R18 | Rejected | Sonar's remaining analyzer-labelled bugs: unused `Tooltip` constructions, implicit string sort, an empty-matching URL regex, and an unseeded `reduce`. | **False positives.** Cinnamon `TooltipBase._init` registers actor/stage callbacks holding each tooltip instance; package paths intentionally sort lexicographically; `urlForLog` intentionally accepts an empty optional path; and `joinPhrases` returns before `reduce` when the array is empty. Existing tests exercise each behavior. |
| R19 | Rejected | Sonar's two vulnerabilities in `test/helpers/appletFixture.js` and `test/helpers/coverage.js`. | **False positives outside shipped code.** `"/tmp/cache"` is a return value in an in-memory GLib test double, not a filesystem write, and the coverage harness invokes the same developer/CI `node` selected by the already-trusted process environment. They do not affect the shipped applet or its local production gates; the related Sonar reconfiguration was rejected in R22. |
| R20 | Rejected | Sonar's mass `var`, object-spread, class-field and test-stub findings. | Not defects. All `javascript:S3504` sites are zero-indented legacy GJS module bindings whose `var` visibility is the import/export mechanism covered by R05/D02. The remaining findings are compatibility-sensitive alternatives, deliberate empty test doubles, or trivial local style; changing them wholesale adds runtime-floor risk without correcting behavior. The feasible optional-chain and own-property cleanups were applied after re-evaluation, using older-runtime-safe equivalents in production GJS. |
| R21 | Rejected | Sonar `javascript:S8786`: two end-whitespace regexes can backtrack super-linearly. | Re-verified after T574: every input is bounded before either regex, including event/holiday names at 300 code points, configured formats and rendered stamps at 256, panel labels at 64, clock labels at 24, and at most eight configured clocks. Rewriting the regex separately adds no distinct mitigation. |
| R24 | Rejected | T701: prefer an authoritative empty holiday result over an earlier provider failure (`holidayAdapters.js` exhaustion picking `failure \|\| emptyResult`). | **The premise is wrong: the current outcome is order-independent and deliberate.** An empty payload is believed only when *every* provider returns one — stated in `_classify`'s comment and pinned by two tests ("an empty answer is believed once every provider gives one", "an empty answer followed by a hard failure is not cached as success"). Verified empirically on 2026-08-05 by driving the real chain both ways: failure-then-empty and empty-then-failure both yield `last_error = "Holiday service unavailable"` with the year uncached, so no reordering bug exists. Implementing the row would make the answer depend on chain order — and `orderProvidersByLastSuccess()` reorders the chain at runtime, so the same evidence would produce a holiday-free year on one run and an error on the next. The unanimity rule exists because an empty array is exactly how Enrico reports a country it does not cover: believing one provider's empty while another was unreachable would cache a wrong holiday-free calendar for the 50-day period. |
| R25 | Rejected | T742: apply the Open-Meteo population floor per candidate instead of to the ranked winner. | **The premise did not survive live data, and the change regresses the case the floor exists for.** The row was written from a synthetic payload in which an unaccented `Sao Paulo` hamlet outranked the city and dragged the whole answer down. Sampled live on 2026-08-06 (`language=en`, `count=10`), that payload does not occur: `Sao Paulo` returns `São Paulo`/Brazil (12,400,232) first, and the applet already resolves it correctly today with no fallback request. The real payloads that *do* refuse are `Malmo` — exact `Malmo`/United States (118) outranking `Malmö`/Sweden (362,133) — and `Genova`, whose only exact spelling is a thirty-person Veneto hamlet. Filtering candidates by the floor makes `Malmo` answer Sweden, but makes `Genova` answer **Génova, Guatemala (3,744)**, because that is then the best-ranked trustworthy hit. Both cases are structurally identical — an untrustworthy exact match displaced by a trustworthy fold-equal one — and differ only in the population of the fold-equal hit, so no non-arbitrary rule separates them. Today's behaviour is correct in both: the primary declines, and Nominatim (ranked by the typed name since T722) answers Genoa and Malmö. What is left of the finding is cost, not correctness — one extra request to a rate-limited geocoder, and a resolved place carrying no timezone, which is **T714**. The place-name folding half of T742 was implemented; only this half is rejected. |
| R22 | Won't fix | Replace SonarCloud automatic analysis with scoped CI analysis that imports coverage and distinguishes tests from production sources (T557). | Rejected explicitly by the maintainer on 2026-07-23. No implementation will be pursued; the existing local and CI coverage gates remain authoritative. |
| R27 | Rejected | T761: `cancelPendingLocaleQueries` enumerating `_inflight` rather than `requested` (the surviving operator mutant of that change). | **Equivalent mutant.** `_requestInfo` writes both maps in the same statement pair and `_releaseQuery` clears both together, so `Object.keys(_inflight)` is always a subset of `Object.keys(requested)`, and `_cancelPendingRequest` returns early for exactly the envs in the difference — it is guarded on the `_inflight` record itself. The two enumerations therefore cancel the same set for every reachable state; no input distinguishes them. Recorded so a later mutation audit does not re-raise it. |
| R28 | Rejected | T763: `EventIndex.hasEvent` reading `_eventIds` rather than `_daysById` (the surviving operator mutant of that change). | **Equivalent mutant.** The two collections are filled and emptied together by construction: `register` admits to `_eventIds` and then always reaches `_noteDay` at least once, because `_spannedDays` pushes its first day before testing the bound; `remove` and `_removeUidFromDays` drop from both; `clear` and `_rebuildEventState` reset both from the same walk. The one window where they disagree is inside a single synchronous `register()` call, between `_prepareEvent`'s `_removeFromBuckets` and the re-registration below it, which no caller can observe. `hasEvent` reads `_eventIds` because that is the set `_admitEvent` counts against `MAX_INDEXED_EVENTS` and `_resyncOverflow` consults, not because the other answer would differ. Witnessed by "fuzz: the id set and the day map always cover the same events" (`test/eventsManager.test.js`), which drives 400 randomized rounds of add/remove/cull/clear across and outside the window and asserts the two key sets stay equal — kept so a future change that breaks the invariant is caught even though no test can distinguish the mutant. |
| R26 | Won't fix | T712: stop rewriting the saved world-clock list to disk when the settings window opens. | Declined by the maintainer on 2026-08-06. The finding itself was verified and is not disputed: `ClocksList.__init__` calls `normalize_clock_setting(info, key, settings)` before `JSONSettingsList.__init__` (`chronos_settings_widgets_worldclocks.py:587`), which writes back through `settings.set_value(key, normalized)` whenever the projection differs (`:279-289`); `normalize_saved_clocks` (`:249-276`) drops non-dict rows, rows whose `label` is blank after `strip()` even with a valid `timezone`, rows naming a runtime built-in, and — via `break` at `:273-274` — everything past the eighth entry; and because Cinnamon builds every settings page up front, merely opening the window discards those rows with no prompt and no undo, while the JS runtime filters the same rows non-destructively at render time (`worldclockData.js:453-461,463-487`). Kept as a tombstone so future audits do not re-open it; reverse only on a maintainer decision. |
| T740 | Deferred | Publish 2028+ dates for the religious observance tables that are not Hebrew-calendar backed. | The Hebrew keys no longer need a table at all - `hebrewCalendar.js` computes them exactly for any year - and the Baha'i keys were corrected and now run 1844-2065: the pre-2015 Western convention (fixed 21 March) for 1844-2014, the Baha'i World Centre's published astronomical table for 2015-2065, and Ridvan derived as Naw-Ruz + 31 rather than tabulated. Past 2065 nothing authoritative exists - and 2092 and 2096 are 19 March on the best available reckoning, so the 20/21 alternation cannot simply be extrapolated. The remaining keys stay bounded, and computing them was evaluated and rejected on precision. **Islam**: tabular Hijri is closed-form but matches Umm al-Qura on only 55% of `eid-al-fitr` and 59-74% of the other keys over AH 1300-1523, and Umm al-Qura is itself tabulated data whose criterion changed four times, is absent before 1883, and is explicitly civil-only - its makers note first sighting can trail it by two days, and Eid al-Fitr 2025 was kept on two different days by 11 and 15 countries respectively. **Baha'i**: Naw-Ruz 2026 turns on about a minute between the equinox and Tehran sunset, and Tehran's 1189 m elevation alone moves that sunset five minutes; the definition changed in 2015 and the authoritative table stops at 2065. **Hinduism, Jainism, Buddhism, Sikhism**: there is no single correct civil date to compute - Diwali 2025 split Indian state governments between 20 and 21 October on local sunset, Vesak 2023 spanned 57 days across four rules, and Sikh reckoning changed in 2003 and was partly reverted in 2010. **Chinese**: computable but out of scope here; see T776. Unparks when the cited multi-faith calendars publish past 2027, or when a per-tradition authority is adopted and the convention recorded per key. See T774 and T775 for data defects found while checking. The suite gate no longer collides with this: it fails only once the tables cannot answer for the current year, and warns for a year before that, naming the religions that are about to lapse. |

## Recorded decisions

| id | decision |
|----|----------|
| D01 | Keep the third-party endpoints rather than a bundled data set or a single vendor. A bundled table goes stale and covers fewer countries; a single provider breaks whenever it is down — the fallback chain exists because the primary is a hobbyist service. Risk accepted with mitigations (weather opt-in; one-time local-tzdata holiday default with visible third-party disclosure and explicit disable/override; size caps; bounded spans; shape checks in and out of cache; colour filtering; no URL logging). |
| D02 | **Keep the widened export surface.** ~80 exported names have no cross-module consumer — used inside their own file and otherwise only by `test/`. The applet runs inside a compositor where a bug costs the whole session; testing only through the composition root would be slow and flaky. **This does not license the inverse:** production must not bend to a test double's shape. |
| D03 | **`author` is the display name "Geraldo Netto", not the GitHub handle.** `info.json`/`package.json` use "Geraldo Netto"; `schema_static.test.js` asserts it. The Spices convention prefers the handle, but the display name is the maintainer's explicit choice. |

## Resolved merge choices

Decisions from the calendar@ccprog / Simon Wiles merge. Kept so future audits do not re-pick them.

| id | resolution |
|----|------------|
| G01 | Kept the `calendar@ccprog` world-clock settings workflow (Cinnamon-native settings, up to 8 user clocks plus built-in UTC/local, region/city selection, IANA/city entry, timezone validation, inherited coverage). Simon Wiles' separate GTK editor remains unported. |
| G02 | Kept `calendar@ccprog` locale-aware weekend behaviour (locale-derived work weeks plus a configurable one- or two-day weekend length). |
| G03 | Kept the copied `calendar@ccprog` visual assets as the initial merged set. The Chronos screenshot was refreshed on 2026-07-16; the icon was replaced with an original design and the orphaned root `calendar.png` deleted on 2026-07-18. |
