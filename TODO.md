# cinnamon-chronos TODO

## Findings

| ID | Category | Severity | Status | Effort | Description | Notes |
|----|----------|----------|--------|--------|-------------|-------|
| T935 | UI / UX; adaptability | Medium | open | M | Verify the popup's new stacked layout on a live desktop. | **Done, at `HEAD`:** the layout choice is `menuLayout.js`, a pure function of work area, UI scale, desktop text-scaling factor and the two columns' measured natural sizes — horizontal while both columns fit inside `MENU_WORK_AREA_WIDTH_FRACTION` of the work area, stacked otherwise, and horizontal again when stacking would leave the agenda under the grid less than `MENU_STACKED_AGENDA_MIN_HEIGHT`, because side by side the agenda's own scrollbox absorbs the overflow while a stack would push the grid off the bottom. `6.0/appletMenuBuilder.js` measures the actors (`layoutMetrics`, which divides the text factor back out so the rule reads sizes at the default text size) and applies the answer by flipping `calendar-main-box`'s `vertical` and adding `calendar-main-box-stacked` — one property, so no actor is created, destroyed, reparented or reordered and the scroll adjustment, the child order the keyboard walks, the focused day and the selected event are untouched by construction. `6.0/applet.js` re-decides on menu open, on `on_orientation_changed`, and on the desktop text-scale signal now bound in `6.0/appletLifecycle.js` beside the clock keys; the geometry comes from `getWorkAreaForMonitor` for the applet's own monitor, so both panel orientations subtract their panel. `settingsFacade.js` owns the new `text-scaling-factor` key. Covered by `test/menu_layout.test.js` plus additions to `applet_view`, `applet_panel`, `applet_lifecycle` and `settingsFacade`. **Settled 2026-08-18:** stacked keeps the agenda *above* the month grid. The maintainer chose to accept agenda-first rather than pin a separate focus chain, so one child order serves both layouts and the keyboard walks the same sequence whichever way the popup is shaped — no second ordering to keep in step with the visual one. The code already does this; nothing changed for it. **Still missing, and why the row stays:** nothing here was seen on a running Cinnamon — narrow monitors, 2x scale, large text and long translations are verified as arithmetic over measured sizes, not as pixels, and the `spacing: 0.5em` rule for the stacked box in particular is unproven. See T943 for the re-decision this does not do while the popup is open. |
| T937 | API contract & compatibility; platform | Medium | open | M | Exercise shipped JavaScript with the actual CJS runtime at the declared Cinnamon 6.0 floor and at the current supported release. | Node lint/tests and the host CJS parser do not prove the declared runtime floor. Add pinned syntax/import smoke checks for the versioned loaders and compatibility shims without weakening the existing Node coverage gate. |
| T938 | test coverage; process & delivery governance | Medium | open | L | Decide whether this project carries a mutation runner at all, and if so install one the maintainer drives by hand. | `package.json` and `.github/workflows/ci.yml` contain no mutation runner or gate — verified 2026-08-17, neither `mutation` nor `stryker` appears in either. **Mutation tests here are written and executed by the maintainer, never by an assistant**, so what is open is the maintainer's decision plus the mechanical part an assistant may do: adding a runner, its configuration, and a bounded scope selector. Running it, establishing a baseline, and judging surviving mutants are the maintainer's, and the equivalent-mutant rationale continues to be recorded in the rejected table (R11, R12, R27, R28) as it already is. |
| T940 | reliability / correctness | Low | open | M | Query both sides of the antimeridian for the Aviation Weather METAR fallback, which needs two requests and therefore a provider-contract change. | **Done, at `HEAD`:** `weatherServiceAdapters.js` clamps the bbox to [-90, 90] latitude and [-180, 180] longitude in `aviationWeatherBox`, and `aviationWeatherStation` ranks candidates through `longitudeDelta`, which wraps a longitude difference to the shorter way round. **The original premise was wrong and is corrected here so nobody re-derives it** — re-verified live against aviationweather.gov on 2026-08-18: an out-of-range longitude is *not* rejected, `bbox=64,176,66,181` answers HTTP 200 with station UHMA. The 204 the row recorded for `bbox=-19.05,178.9,-17.05,180.9` is genuine emptiness, not rejection: the nearest stations to the seam are NFFN (-17.75, 177.44) and UHMA (64.73, 177.74) east of it and PMDY (28.21, -177.38) west of it, so nothing sits within ~2.2° of ±180 on either side. With `AVIATION_WEATHER_BBOX_DEGREES = 1` no place within 1° of the seam can reach a station whatever the wrapping does, so clamping alone does not revive the tier there — the tier is not dead, it is empty. What *was* an unconditional defect, and is now fixed, is the ranking: `stationLongitude - longitude` made 179.9°E and 179.5°W 359.4° apart instead of 0.6°, mis-ordering every candidate across the seam wherever a request returns more than one. **Still missing:** a box spanning ±180 has to be asked for as two boxes, and `weatherProviders.js` gives each provider exactly one URL (`url: (place) => ...` in `FORECAST_PROVIDERS`, ~:487), so a split box is a resolver-contract change — one provider yielding several requests and merging their station lists — not an edit to the adapter. Widening the box instead would be the cheaper alternative and should be judged against the request cost. |
| T946 | architecture / modularity / SOLID | Medium | open | M | Stop shipping Open-Meteo's `timezone` inside the domain weather reading; give the zone its own channel. | The reading record is documented three times as unit-free `{ condition, temperatureC }` (weatherServiceAdapters.js:265-270, weatherProviders.js:474-479, :529-530). `openMeteoReading` (weatherServiceAdapters.js:296-306) spreads a `timezone` onto it and `placeWithTimezone` (weatherProviders.js:514-519) reads `reading?.timezone` to patch the *place*. The reading is then cached by `_rememberReading` (:656), stored in `WeatherReadingStore` and handed to presenters, so its shape depends on which provider in the failover chain answered — the coupling the port comment at :555-557 says does not exist. Fix: have `normalize` return `{ reading, placeHints }`, or add a `timezoneFor(data)` hook on the registry entry, so `_placeResolved` merges the zone without the reading ever carrying it. |
| T947 | code duplication | Low | open | S | Three near-duplicate helper pairs in `weatherServiceAdapters.js` / `localeQuery.js` should collapse to one definition each. | (a) `aviationWeatherIcon` (:184-208) and `metNoIcon` (:308-330) are the same eight-line algorithm — normalise case, walk `[tokens, icon]` rules, first substring hit wins, else `WEATHER_UNKNOWN_CONDITION`; only the case direction and source field differ. Fix: `iconFromTokenRules(rules, text)` with the per-vendor tables as the only difference. (b) `metarNumber` (:215-226) is `coordinateNumber` (:430-438) without the range test. Fix: one `finiteNumber(value, minimum, maximum)`. (c) `geocodeLanguage` (:95-99) and `localeQuery.messageLanguage` (:219-223) are the same ISO-639-1 extraction, and in production both run on the same value — `appletLifecycle.js:239` passes `messageLanguage` as the resolver's language and `geocodeUrl` normalises it again. Fix: delete `geocodeLanguage`, have `geocodeUrl` take an already-normalised code. |
| T949 | observability; code duplication | Low | open | S | The panel weather exhausts its retry ladder silently while the city weather logs it — one policy, two implementations. | `weatherScheduler.js:178-182` documents `retriesExhausted()` as existing so "the caller can say so once". `cityWeather.js:228-248,504` consumes the boolean and logs the ceiling; `weather.js:314-320` calls `this._scheduler.retry(...)` and discards the return, so the panel provider burns its eight-attempt budget with nothing in the log. The retry *decision* is also written twice and differently: `weather.js` retries unless the error is `LOCATION_NOT_FOUND` or `OFFLINE`; `cityWeather.js` re-expresses it as `ok = cityError === LOCATION_NOT_FOUND`. Fix: one `shouldRetry(error)` predicate plus one ceiling log, used by both. |
| T950 | architecture / modularity / SOLID; code duplication | Medium | open | L | `weather.js` and `cityWeather.js` duplicate the whole provider lifecycle, and the copies have diverged. | Written twice: `_destroyed`, the generation counter and `_isCurrent(generation)`, `stop()` (`generation++` + `scheduler.stop()`), `destroy()` with the `_owns_reading_repository` branch, the `isOnline` injection defaulting to `() => true`, and the "offline is a state, arm no retry, call `succeeded()`" rule (weather.js:290-297 vs cityWeather.js:363-373). See T949 for the retry half, which has already drifted. Fix: extract a `WeatherConsumerBase` (or a `RefreshPolicy` collaborator) owning generation, destroy, online-gating and retry decision; both providers keep only their own fetch and presentation. |
| T952 | wiring gaps | Low | open | S | `debounceMs` is handed to the city weather scheduler and never used; the city provider debounces by a second, different mechanism. | cityWeather.js:127-137 forwards `debounceMs` into the scheduler, but `CityWeatherProvider` never calls `scheduler.queue()` — the only `_scheduler.queue` call site in the tree is weather.js:266. City rescheduling is instead short-circuited by `_applied_signature` (cityWeather.js:297). Two mechanisms for one problem, one of them dead. Fix: drop `debounceMs` from the city scheduler construction, or route city rescheduling through `queue()` and delete the signature short-circuit so there is one debounce rule. |
| T953 | architecture / modularity / SOLID | Low | open | S | `locationCacheKey` is a domain identity rule living downstream of Soup and GLib. | weatherProviders.js:77-79 defines it, and it is what `cityWeather.js` uses from that module besides the repository class — six call sites (:54, 184, 215, 329, 465, 511), all domain concerns: error-map keys, reading-store keys, city dedup. `weatherFormat.js` is the declared platform-free domain module and already owns `normalizeWeatherLocation`, which `locationCacheKey` wraps. Fix: move it to `weatherFormat.js` beside `normalizeWeatherLocation`; re-export from `weatherProviders` if a call site needs the old name. |
| T954 | code complexity; state machine integrity | Medium | open | L | `localeQuery.js` coordinates eleven module-global maps by convention; give it one record per env with explicit transitions. | `_pendingTimers`, `_consumers`, `localeInfoCache`, `requested`, `_inflight`, `degraded`, `attempts`, `abandoned`, `unanswered`, `listeners`, `localeGeneration` (localeQuery.js:41,65,179-197). The invariants between them — `requested[env]` ⟺ `_inflight[env]`; `abandoned` ⟹ skip `_degrade`; `unanswered` is the resume set for envs cancelled with no consumer — are enforced by hand across `_releaseQuery`, `_degrade`, `_settlers`, `_shouldAsk`, `_resumeAbandonedQuery` and `registerLocaleConsumer`. The cost is already in the file: roughly 120 of its 535 lines are comments narrating past bugs caused by two of these maps disagreeing, and the attempt-cap rule is duplicated at :77-91 and :339 with a `NOSONAR` note saying so. Fix: one module-scope `LocaleQueryRegistry` holding `{ state: idle|inflight|degraded|abandoned|settled, attempts, cancellable, proc }` per env, making `_shouldAsk` a transition guard instead of a three-map conjunction. |
| T957 | UI / UX | Low | open | S | The same sky reads `⛅ Partly cloudy` from Open-Meteo and `🌤 Fair` from the other two providers. | weatherServiceAdapters.js:56-63 records that open-ended buckets were replaced with closed ranges precisely because "the same real weather was described differently depending on which provider in the failover chain answered". But `WMO_CONDITION_RANGES` (:68-93) never emits `🌤`, while `AVIATION_COVER_ICONS.FEW` (:191-196) and `MET_NO_ICON_RULES`'s `fair` (:308-317) both do — so a lightly clouded sky flips glyph, wording and accessible name on every failover, inside the category the comment claims to have closed. Fix: decide whether `🌤` is a distinct class; if it is, map WMO code 1 to it and leave code 2 on `⛅`; if not, drop it from both vendor tables and from `WEATHER_CONDITIONS`. |
| T959 | architecture / modularity / SOLID | Medium | open | M | `PanelView` is 22 pass-through methods over `createPanelPort`, reproducing the failure mode it was introduced to prevent. | `6.0/applet.js:70-107` builds a 33-entry port; `6.0/appletPanelStatus.js:172-343` wraps it in a class whose bodies are `return this.port.X()` for all but four members — `weatherUnits` (:215), `setAccessibleName` (:262-279), `setHomeEnabled` (:317-337), `_hasKeyFocus` (:339-342) — which are the only ones carrying behaviour. Adding one datum to the presenter means edits in three files, and a typo in a view getter returns `undefined`, which is exactly the silent-blank failure the header comment at :160-171 says the design prevents. Fix: let `AppletPanelStatusPresenter` hold the port directly and keep only the four transforming members as free functions or a small behaviour object, leaving the port literal as the single interface definition. |
| T961 | architecture / modularity / SOLID; code complexity | Medium | open | L | `AppletMenuBuilder` is a builder, a runtime layout controller and an issue-reporter owner in one 509-line class. | Its own doc (6.0/appletMenuBuilder.js:106-108) says it "builds the menu contents and hands them back", and `agendaColumn.js:22-33` criticises this class for having held live state. It still owns `_mainBox`, `_calbox`, `_layout` (:120-123), `_naturalSize` (:199-206), `layoutMetrics` (:212-228), `get layout` (:230), `applyLayout` (:239-255) and `reflow` (:257-259) — invoked on every menu open, orientation change, text-scale change and now every `monitors-changed` — plus `AppletIssueReporter` (:38-104) and the home-button input handlers (:435-457). Four unrelated reasons to change one class, and the reflow path cannot be tested without constructing the whole menu. Fix: split out a `MenuLayoutController` constructed with `{ mainBox, calbox, eventListActor }`, and move `AppletIssueReporter` to its own module — it is a message-aggregation model with no St dependency beyond `label.set_text`. |
| T962 | code complexity | Medium | open | M | `updateClockAndDate` does six jobs in 56 lines on the applet's hottest path. | `6.0/appletPanelStatus.js:831-886` performs clock-stamp clamping, clock-entry gating, render-model construction, panel-label and accessible-name writes, today-cache read, home-button enable, two St.Label writes, tooltip write, issue-status aggregation, world-clock popup push and weather-source credit — once per second with a `%S` format. Fix: extract `_renderPanel(labelString)` and `_renderMenu(clockModel, clockEntries)` and leave `updateClockAndDate` as the short policy deciding which runs. |
| T963 | code duplication | Low | open | S | Five hand-rolled diff-then-write caches in the panel presenter, no two alike, one keyed by a dynamic property name. | `6.0/appletPanelStatus.js:317-337` (`setHomeEnabled`), `:672-679` (`_setTooltipModel`), `:794-800` (`_announce`), `:802-809` (`_setLabel`) and `:811-818` (`_setPanelLabel`). `_setLabel` and `_setPanelLabel` are the same function differing only in how the cache field is named, and `_setLabel`'s `this[cacheKey]` write makes `_rendered_day`/`_rendered_date` invisible to lint and to anyone grepping for them. Fix: one `_writeIfChanged(key, value, apply)` used by all five. |
| T966 | UI / UX | Low | open | S | With world clocks switched off, no surface credits the weather services at all. | `6.0/appletPanelStatus.js:883` writes `clockModel.sources` only, and `sources` is derived from clock rows (:663) via `_weatherSource` (:588-599), so with `show_worldclocks` false `getClockEntries()` is never called (:842 gates on `clocksOn`) and the credit is `""`. The comment at :880-881 calls the credit "a courtesy the data services are owed", and :709-712 records that the tooltip footer credit was removed *because* the popup still carries it — with clocks off, neither does. Mitigating: the credit's target is inside the hidden world-clock block, so the state is absent rather than wrong. Fix: restore the tooltip footer credit as the fallback when `worldclocksEnabled()` is false, or move the credit to the always-present issue/status label. |
| T969 | reliability / correctness | Low | open | S | `onHolidayPlaceChanged` names two different things, and the lifecycle calls one from inside the other. | `6.0/appletLifecycle.js:344` binds `this.onHolidayPlaceChanged`, whose body (:388-403) calls `this.context.onHolidayPlaceChanged()` twice — once as the `setPlace` completion callback (:399) and once unconditionally (:402). `onReligionsChanged` (:374-377) calls the context callback and deliberately *not* the method. Two objects, one identifier, opposite meanings — "resolve and apply the place" versus "repaint the calendar" — and a missing or added `context.` at any of five sites compiles, runs and changes behaviour: the `onReligionsChanged` case would start refetching holidays on every checkbox toggle. Fix: rename the applet-side callback to `onHolidayDataChanged`, or the lifecycle method to `_resolveHolidayPlace`. |
| T972 | API contract & compatibility | Low | open | S | `EventsManager.set_enabled(enabled)` implements only half the toggle its name promises. | eventsManager.js:148-152 ignores `enabled === true` entirely and only quiesces on `false`; `AppletEventListCoordinator.apply` (6.0/appletCoordinators.js:147-150) compensates with a forced `select_date` on the enable path. The manager therefore does not own both halves of its own state transition, and the name says it does. Fix: either rename to `disableIfOff(enabled)`, or move the forced reselect inside `set_enabled` and delete the coordinator's compensation. |
| T976 | architecture / modularity / SOLID | Medium | open | M | `CalendarGridHost` hands out the whole `EventsManager` and the whole holiday provider, defeating the port it is. | The 15-line comment at 6.0/calendar.js:176-190 says the port exists so collaborators cannot depend on the Calendar's shape and can be built and tested alone. `get eventsManager` and `get holidayProvider` (:191-255) hand out the collaborators whole — `CalendarEventDotRenderer.update` calls `get_colors_for_unix_key`, `annotate()` calls `.active` and `.getHolidays` — so testing either still needs a full-shaped double. `eventsManager` is also the only port member captured as a value at construction (:757) rather than a thunk, so it goes stale if the field is reassigned. Fix: replace with `colorsForUnixKey(dateUnixKey)`, `holidaysActive()` and `requestHolidays(year, month, cb)`; make `eventsManager` a thunk in the interim. |
| T980 | code complexity | Medium | open | M | `Calendar._buildHeader` is 115 lines doing nine unrelated things, and `EventRow`'s constructor is 124. | 6.0/calendar.js:925-1039 performs column-offset arithmetic, `destroy_all_children()`, a generation bump, a grid reset, month/year box ordering, four nav buttons with four `connect` calls, month label plus reason label plus annotator attach, year label plus render-cache reset, a conditional week-number header and a seven-iteration weekday-heading loop — and the destructive-rebuild semantics that invalidate in-flight annotation passes are buried in the middle of actor plumbing. 6.0/eventView.js:990-1113 has the same problem in a file that already demonstrates the fix: `EventList` decomposes its actor tree into `_buildSelectedDateLabel`/`_buildOverflowView`/`_buildNoEventsView`/`_buildEventsView` (:403-582) while `EventRow` inlines launcher policy, activation gating, ATK roles, hover signals, colour strip, two nested boxes, three labels, wrap/ellipsize config and the first `update_variations()`. Fix: split both along the seams their siblings already use. |
| T981 | code duplication | Low | open | S | Launch-availability policy is written four times in `EventList`, and there is already a named method for it. | 6.0/eventView.js:394, :429-431 (`_canLaunchCalendar()`), :612 (`canAdd`), :794 (`canLaunch`). Three are the identical `this._calendar_launcher.isAvailable() && !this._unavailable`; the fourth omits `_unavailable` only because it runs at construction, where it is `false`. Fix: call `_canLaunchCalendar()` at all three sites and delete the locals. In the same file, `set_unavailable` (:800-807) duplicates its tail across both branches: `if (!unavailable) this.set_no_events_text(_("No Events")); this._renderCurrentEvents();` says the same thing once. |
| T984 | architecture / modularity / SOLID | Medium | open | M | Two unrelated module-resolution mechanisms coexist inside single `6.0/` files, with no rule for which root module gets a shim. | 6.0/calendar.js:21-38 reaches root modules two ways in seventeen lines — `AppletModules.dateFormats/localeQuery/localeText/styleUtils` straight through `imports.ui.appletManager.applets[...]`, and `require("./settingsFacade")`/`require("./eventData")` through 6.0 shims that are themselves 16 lines of pass-through. 6.0/eventView.js:22-58 and 6.0/calendarAnnotations.js:12-19 do the same. `dateFormats`, `localeQuery`, `localeText`, `styleUtils` and `textUtils` have no shim while `eventData`, `settingsFacade`, `holidayConstants` and `eventFormat` do. Fix: pick one — add the five missing shims and forbid `AppletModules.*` inside `6.0/`, or drop the pass-through shims and use `AppletModules.*` uniformly — and pin the choice with a static test. |
| T985 | architecture / modularity / SOLID | Medium | open | L | `6.0/calendar.js` and `6.0/eventView.js` are each three files, and the pure halves cannot be loaded without a toolkit. | `6.0/calendar.js` (1140 lines, seven exported classes) holds pure month-window date math with no toolkit dependency (`CalendarMonthWindow`, `CalendarMonthWindowCache`, :96-174 — currently unloadable in a test without Clutter, St, Cinnamon, Pango and the `gtk30` gettext domain), the grid rendering layer (`CalendarGridHost`, `CalendarDayCellRenderer`, `CalendarEventDotRenderer`, `CalendarGridView`, :191-672, including Clutter allocation math) and the coordinator (`Calendar`, :674-1122). `6.0/eventView.js` (1134 lines) holds the agenda value type and composition policy (:60-125), the timespan formatter (:127-155), the renderer (:157-362), the actor tree plus accessibility plus issue reporting plus the unavailable/overflow state machine (:364-822), and the row with its presenter (:824-1126). Fix: `6.0/calendarMonthWindow.js` (no toolkit import), `6.0/calendarGrid.js`, `calendar.js` keeping `Calendar`; `6.0/selectedDayAgenda.js`, `6.0/eventRow.js`, `eventView.js` keeping `EventList` + `EventListRenderer`. |
| T987 | wiring gaps | Low | open | S | `HolidayFreshness`/`HolidayPersistWindow` are re-exported under a comment describing a seam that does not exist. | holidays.js:103-108 says they are re-exported "so the composition root and the suite can hand it different ones without reaching past the barrel". Verified across `files/` and `test/`: no consumer, and no path. `createHolidayProvider` (:713-733) forwards `httpSession, record, cacheRepository, status, inflight` and not these; `_fileBackedCache` (:278-287) calls `new HolidayCache(load, save)` with no third argument, so `HolidayCache`'s own `params.freshness`/`params.persistWindow` seam (holidayCache.js:585-591) is unreachable from the composition root. Substituting a staleness policy today means constructing the whole cache and injecting it as `cache` — exactly what the comment says you need not do. Fix: thread `freshness`/`persistWindow` through `createHolidayProvider` → `HolidayService` → `_fileBackedCache`, or delete both re-exports (and from `module.exports` at :831) and the claim. |
| T988 | architecture / modularity / SOLID | Medium | open | L | `holidayCache.js` is a pure store and 320 lines of Gio file I/O in one module, and the coupling bites at import time. | `HolidayCache` (holidayCache.js:584-1019) is an in-memory data structure — rows, two indexes, a month memo, a year LRU. `HolidayCacheRepository` (:160-482) is async Gio file I/O, legacy-file migration, etag merge-retry and country LRU eviction. They share nothing but a file, and :479 runs `GLib.build_filenamev([GLib.get_user_cache_dir(), …])` **at import**, so the pure store cannot be loaded without a GLib stub — which is exactly why test/helpers/holidayFixture.js:161-167 stubs `build_filenamev`/`get_user_cache_dir`/`mkdir_with_parents`. Fix: move `HolidayCacheRepository` plus `validCachedHoliday`/`validCachedStamp`/`validCachedYears` (which describe the file) into `holidayCacheRepository.js`; leave a Gio-free `holidayCache.js`; wire both at `createHolidayProvider`. |
| T989 | architecture / modularity / SOLID | Medium | open | M | `holidays.js` is a barrel, a composition root and four unrelated classes, so one rename costs three edits. | holidays.js:101-117 re-exports 17 symbols; :146-186 `HolidayStatusLedger` and :191-243 `HolidayInflight` are self-contained data structures already tested standalone (test/holiday_integration.test.js:513,538,574); :735-765 `HolidayProviderFacade` and :771-827 `ReligiousHolidayProvider` are presentation-facing decorators; :245-698 is the service and :713-733 the composition root. Any change to `holidayCache.js`'s export set forces an edit at :101-108, at the 23-name `module.exports` on :831, and at the export-name assertion in test/gjs_import.test.js:349. Fix: `holidayInflight.js`, `holidayStatusLedger.js`, `holidayProviderFacade.js`; keep `holidays.js` as service plus composition root. |
| T995 | reliability / correctness | Low | blocked | XS | Decide whether `recordFetch` should judge a provider's `Date` header against the injected receive time rather than the wall clock. | **Half done, at `HEAD`:** `_country` now passes `this._now()` to `validCachedYears` (holidayCache.js:241), so the repository's injectable clock governs both its stamp reads. **Still missing, and why the row stays:** `recordFetch` (:879-898) still calls `validCachedStamp(retrieved)` with the real clock while taking `received` injectably. Changing it to `validCachedStamp(retrieved, new Date(received).getTime())` was implemented and reverted, because it changes real behaviour that a deliberate test pins: "HolidayCache falls back to receive time when the Date header is missing" (test/holiday_cache.test.js:85-88) feeds a header one day *after* the artificial receive time and asserts the header wins. Under receive-time reference that header reads as future-dated and is refused. In production `received` defaults to `new Date().toUTCString()` so the two agree to within the request duration; the question is only how much provider clock skew ahead of receipt is legitimate. **Blocked on the maintainer:** should `recordFetch` reject any `Date` header later than the receive time (strict — and that assertion is rewritten), allow a bounded skew window (how wide?), or keep the wall clock and record the reason in a comment? Nothing is implemented until that is answered. |
| T1010 | architecture / modularity / SOLID | Low | open | S | 48 lines of actor-free astronomy logic live in the version-specific view module. | 6.0/astronomyView.js:30-77 — `zonedDateTime`, `civilDayBounds`, `bodyRows` and `defaultFormatTime` touch no St, no Clutter and no instance state, and `bodyRows` (:56-69) touches nothing but its arguments. The pure half of the same feature (`astronomy.js`) is a root module by design. So the rise/set and always-up/always-down copy rule is reachable only through a file that requires `imports.gi.St` and `imports.gi.Clutter`, and a future `6.x/` tree would have to fork it with the actor code. Fix: move all four into a root `astronomyDay.js` with a 16-line `6.0/` shim, leaving `AstronomyView` holding actor construction and the render memo. |
| T1013 | robustness / recovery | Low | blocked | XS | One malformed `zone.tab` line disables automatic holiday-country inference for the whole machine. | **Half done; the rest needs a maintainer decision.** The silence is fixed: `countryCodeFromZoneTab` now calls `reportMalformedZoneTabLine` (worldclockData.js) before it gives up, naming the file and the offending row, capped at 200 characters, and `test/worldclocks.test.js` pins that a clean table stays silent. **The decision:** should one bad row abort the whole table, or be skipped? Abort is defensible — a `zone.tab` we cannot parse is one we cannot trust to say which country a zone is in — but the rejected shapes include a five-column extension and a repeated identifier (`!seenTimezones.has(fields[2])`), both of which a distro could ship while every other row stays correct, and either withdraws the inference for the whole machine. Unparks when the posture is stated; the fix is `continue` instead of `return ""` if skipping is chosen. |
| T1015 | reliability / correctness | Medium | blocked | S | Opening any settings page writes a weather location the user never typed, contradicting the shipped tooltip. | chronos_settings_widgets_weather.py:141 calls `prefill_from_timezone()` from `__init__`, and :223-236 calls `self.set_value(city)` — a real `settings.set_value` → `save_settings()` → the applet's key change → geocode. Cinnamon builds every settings page up front, so opening the dialog persists a location even with `show-weather` off (the `dependency` hides the row; the widget is still constructed). The schema tooltip for `weather-location` says "Leave it empty and the panel shows a bare warning marker instead of a reading" — after this the key cannot stay empty; and the class docstring says the city is "put *in the field*, not resolved behind the user's back", while the write is what reaches the geocoder. Note this interacts with the README paragraph "Clearing the field saves an empty location … Reopening the settings dialog or reloading the applet restores the timezone-derived city", which describes the restore as intended: settle which of the two is the contract before changing either. Fix candidate: prefill only `content_widget.set_text` and let `commit()` persist on accept/focus-out, or gate the write on weather being enabled. **Blocked — maintainer decision needed:** which is the contract, the schema tooltip (`weather-location` may stay empty and the panel shows a bare warning marker) or the README paragraph (reopening the dialog restores the timezone-derived city)? The fix differs per answer: prefill only `content_widget.set_text` and let `commit()` persist, versus keeping the write and correcting the tooltip. Not implemented. |
| T1016 | reliability / correctness; i18n | Medium | open | XS | The settings translation loader picks one catalog directory and never chains the other, so a stale per-user `.mo` masks the system one forever. | chronos_settings_i18n.py:20-34 returns the *first* `GNUTranslations` found and checks `~/.local/share/locale` before `/usr/share/locale`. A user who once installed the applet per-user keeps that catalog; every msgid added since renders in English permanently even though the system-wide catalog has it, because the system catalog is never consulted. `gettext` supports exactly this through `add_fallback`. Fix: collect every `GNUTranslations` across both roots, chain them with `add_fallback`, return the head's `gettext`, and test that a msgid missing from the first catalog resolves from the second. |
| T1017 | observability | Low | open | XS | `chronos_settings_i18n` emits a log warning at import time, which `chronos_timezone_data` documents as the wrong thing to do. | chronos_settings_i18n.py:29, reached from the module-level `_ = load_translation()` at :37. chronos_timezone_data.py:305-311 states the rule and follows it: "Warned here rather than at import: importing a module should define things, not emit them. At import time cinnamon-settings has not configured logging yet, so the warning landed on the whole process's stderr or was dropped entirely, depending on import order." Fix: make `load_translation()` lazy behind a module function or `__getattr__`, or collect the diagnostic and log it from the first widget construction, as the resolver does. |
| T1022 | code duplication | Medium | open | M | Three copy-pasted completion trios and two copy-pasted commit-on-edit-end widgets in the settings modules. | (a) chronos_settings_widgets_worldclocks.py:123-142, chronos_settings_widgets_weather.py:53-78 and chronos_settings_widgets_holidays.py:99-104 each re-declare `_x_completion_columns` → `x_completion_model` → `attach_x_completion` over the general `common.completion_model`/`common.attach_completion`, differing only in a column function and two flags — three copies of the `if not rows: return None` guard and three call sites to keep in step when the matcher contract changes, which it already has. Fix: one `common.attach_suggestions(entry, rows, columns, *, inline=False, on_selected=None, text_column=0, min_key=2)`. (b) chronos_settings_widgets_weather.py:167-221 and chronos_settings_widgets_holidays.py:129-200 each implement the same edit-end lifecycle — `changed` clears the error mark, `activate`/`focus-out-event`/`destroy` commit, `on_*_commit` returns `False` to propagate, a `mark_refused` wrapper over `common.set_invalid` — with the comment "closing the settings window while the cursor is still in the field never fires focus-out" duplicated verbatim (weather:179-180, holidays:138-139). Fix: one `CommitOnEditEnd` mixin in `chronos_settings_widgets_common.py`; each widget supplies `commit(text)` and its refusal message. |
| T1026 | process & delivery governance | Low | open | S | `check_python_compat.py` proves parseability, not 3.10 compatibility, and its success line says more than it checked. | scripts/check_python_compat.py:26-31 uses `ast.parse(feature_version=(3, 10))`, which constrains a handful of grammar decisions and cannot see runtime API use; :51 then prints "Python 3.10 syntax: N shipped files". Shipping `datetime.UTC`, `typing.Self`, `tomllib`, `ExceptionGroup` or `itertools.batched` would pass and then fail at runtime on the declared floor. The docstring is honest; the output line is not. No live break today — every `dict[str, str]`/`tuple[...]` annotation sits under `from __future__ import annotations`. Fix: keep the parse gate, make the success line say "syntax only", and add either an allowlist check for symbols newer than 3.10 or a real 3.10 interpreter leg in CI. |
| T1043 | reliability / correctness; lifecycle | Medium | open | S | `moduleConsumerCount.release()` runs `onRelease` and `onLastRelease` for a release that matches no register, so an unbalanced call tears down process-wide state a surviving applet instance is still using — the opposite of what its own comment promises. | providerUtils.js:283-308. The comment claims "a release with no matching register cannot drive the count negative, so a caller that releases twice cannot tear down a surviving instance's state". Only the first half is true: `count > 0` guards the decrement, but `onRelease` and the `count === 0` test run regardless. With two instances registered, one instance releasing twice reaches zero and fires `onLastRelease` while the other is live — `NOMINATIM_REQUEST_QUEUE.cancelPending()` drops the survivor's in-flight geocodes and `weatherCityMemo.clear()` throws away its timezone-to-city memo. weatherProviders.js:277-279 names exactly this hazard for the legacy `cancelPendingWeatherRequests` alias it still exports ("a second call from a retry path decrements a count it never incremented"). Fix: a release with no matching register is a no-op — neither handler runs. |
| T1044 | reliability / correctness | Medium | open | S | `WeatherLocationEntry` reports an over-long stored location and then silently overwrites it with `""` on the first end-of-edit, including the `destroy` that fires when the settings window closes. | chronos_settings_widgets_weather.py:150-208. `on_setting_changed` blanks the field (`normalize_weather_location` returns `""` past the bound) and marks it refused — pinned by test_settings_widgets_specialized.py:39. But `connect_widget_handlers` wires `activate`, `focus-out-event` and `destroy` to `on_commit`, which commits the blanked field: `commit("")` is not refused, `location` is `""`, it differs from the stored value, so `set_value("")` runs. Opening the settings page and closing it — without ever touching the field — destroys the key the widget just finished explaining it could not show. The sibling `CountryComboBox.restore_entry_text` deliberately does the opposite for the same case. Fix: `commit` must not write an empty value over a stored value that is itself refused. |
| T1045 | UI / UX | Low | open | XS | `CountryComboBox` takes its "is not in the list" mark back off on focus-out, contradicting the guarantee `on_setting_changed` states two methods above. | chronos_settings_widgets_holidays.py:113-136, 186-200. `on_setting_changed` marks a stored country code this build does not offer and says "No row goes active, so `on_combo_changed` early-returns and this mark stays up." It does not: `activate`/`focus-out-event`/`destroy` reach `restore_entry_text`, which finds nothing typed, sets the entry to `""` and ends with `mark_refused(text.strip())` — `mark_refused("")`, which clears the mark. Tabbing through the field leaves an empty, unmarked combo for a key the applet is still querying. Fix: an end-of-edit that changed nothing must not clear a refusal the field did not raise. |
| T1046 | reliability / correctness; lifecycle | Medium | open | S | `WeatherRefreshScheduler.queue()` can park a spent or already-removed GLib source id in `_debounce_id`, which the next `stop()` then hands to `GLib.source_remove`. | weatherScheduler.js:193-205. Two holes, both of which `NominatimRequestQueue._scheduleJob` (weatherProviders.js:200-217) documents and guards against for the same class of timer: (a) the old id is removed but the slot is not cleared before re-arming, so a throw out of `_scheduleDebounceTimer` leaves a removed id behind; (b) nothing in the contract says the debounce callback may not run before `_scheduleDebounceTimer` returns — if it does, the callback zeroes `_debounce_id` and drains, and the assignment afterwards writes a spent id back into the slot. Either way the next `stop()` removes a source that is gone or belongs to something else. Fix: clear the slot before arming and write the returned id back only if it is still ours to write, as `_scheduleJob` does. |
| T1047 | reliability / correctness; lifecycle | Low | open | XS | `runTeardownSteps` calls `global.logError` unguarded, so a host without it aborts the teardown at the first failing step — the one thing the module exists to prevent. | 6.0/appletTeardown.js:22-30. Its own header: "Every step runs even if an earlier one throws: a teardown that stops at the first failure leaves the rest of the applet's signals, timers and actors connected to a destroyed object for the life of the session." The `catch` then dereferences `global.logError` with no guard, so the reporter is a step that can throw and take the remaining steps with it. `appletLifecycle.js:174-183` guards the identical call in `_finalizeQuietly`, and providerUtils.js guards every `global.log`/`global.logError` it makes. Fix: report defensively; never let the report end the loop. |

## Rejected, deferred and won't fix

| ID | Disposition | Item | Rationale |
|----|-------------|------|-----------|
| T774 | Won't fix | `paryushana-start` 2025 matches no Jain tradition, and the key names no sect. | **[declined by the maintainer 2026-08-06]** Jain practice forks further than the two main sects: Svetambara Paryushan 2025 ran 22-29 August with Samvatsari on 29 August, Digambara Dasa-Lakshana ran 30 August to 8 September, and the shipped `2025: [8, 20]` is neither; the 2026 cell does match the Svetambara start, so the column is not consistently one tradition either. Tapa-gaccha keeps Samvatsari on Bhadrapada Shukla 4 and other Svetambara lineages on 5, with Sthanakavasi and Terapanth differing again. Picking a sect is a content decision, not a defect the code can settle, and the same argument reaches every lunisolar key in `TABLES`. Unpark by naming the tradition each key encodes. |
| T775 | Won't fix | `guru-gobind-singh-jayanti` cannot be represented by one date per year, and the applet has not chosen a reckoning. | **[declined by the maintainer 2026-08-06]** Two calendars are in active use and they disagree. Under Bikrami reckoning the anniversary is Poh Sudi 7, which falls twice in some Gregorian years and not at all in others - 9 January 2022 and 29 December 2022, leaving 2023 with none, then 17 January 2024 - and the table holds one cell per year. The SGPC adopted Purewal's tropical Nanakshahi calendar in 2003, moved the month starts back onto Bikrami in 2010, and by 2014 had reverted while keeping the name; the 1999/2003 version was rebranded Mool Nanakshahi in 2017 and is what the PSGPC and most diaspora gurdwaras follow. In 2024 the SGPC observed 17 January while PSGPC and US gurdwara panels had observed 5 January. The shipped `[1, 6]` for 2025 and 2026 matches neither. Choosing a reckoning is a content decision, and representing it needs the table to yield zero or two dates in a year. Unpark by deciding which calendar the applet follows and saying so per key. |
| T776 | Won't fix | The Chinese folk observances are computable, but only with an in-tree astronomical implementation and only for part of the range. | **[declined by the maintainer 2026-08-06]** The rule is fully specified by GB/T 33661-2017 (Purple Mountain Observatory) - a month starts on the day at 120 deg E containing the new moon, month 11 contains the December solstice, and the leap month is the first zhongqi-less month between consecutive month-11s - so `chinese-new-year` and `ghost-festival` are computable and `qingming` needs only solar longitude 15 deg. What is declined is building and carrying the ephemeris to do it. Three findings stand and are kept so no later audit re-derives them. **`Intl`/ICU is not a usable shortcut**: its Chinese calendar disagrees with a validated Meeus ch.49 implementation in 6 of 301 years (1808, 1814, 1916, 1954, 2027, 2030), and the Hong Kong Observatory open data confirms ICU is the wrong one for 2027 - the new moon is 2027-02-06 23:56 CST, four minutes from midnight, and ICU says 7 February. **The range would not be 1800-2100**: before 1929 the calendar used Beijing apparent solar time at 116d25'E, about 14 minutes off 120 deg E plus the equation of time, moving roughly 18 of 1595 pre-1929 new moons to a different civil date. **Two lunations are undecidable at any precision** - 2057-09-28 23:59:57 CST and 2097-08-07 23:59:39 CST - because delta-T past 2050 is not knowable to seconds; also the "second new moon after the solstice" shortcut breaks in 2033 for the first time since the 1645 reform. Unpark by deciding to carry an ephemeris in-tree and a bounded range with it; ground truth for a test is the HK Observatory conversion tables, 1901-2100. See T740, which parks the rest of the non-Hebrew observance tables. |
| T816 | Won't fix | Spell out every enforced Python coverage percentage and per-file override in contributor prose. | **[declined by the maintainer 2026-08-06]** The documentation does not need that precision; the executable coverage gate remains authoritative. |
| R05 | Rejected | Barrel re-exports as the GJS `var` bindings importers need. | The re-exports **are** the import mechanism; tests reach the modules through them. |
| R06 | Rejected | `6.0/calendarLauncher.js:50`: commented-out `Util.trySpawn(["gnome-calendar"])`. | Deliberate documented compatibility switch with a maintainer note directly above it. |
| R08 | Rejected | No watchdog/metrics/circuit-breakers/health surface. | For a panel applet these are reasonable omissions; the provider failover with last-success ordering plus a per-year freshness gate is a coherent substitute. |
| R09 | Won't fix | Per-calendar colour strip conveys calendar identity by colour alone. | Not actionable here: `cinnamon-calendar-server` sends the calendar's colour and never its display name, so there is no name to announce. The strip is `Atk.Role.SEPARATOR` so a screen reader skips it. Needs an upstream change. |
| R12 | Rejected | Six `religiousHolidays.js` mutants survive the operator/constant audit (computus f-term `+8→+9`, month-expression `7m→8m`, `_easterDate` hour literal, monthMap range guard ×3). | **Equivalent mutants.** Whole-domain witness searches over years 1–9999 found no distinguishing input for either computus mutant; the hour literal only dodges DST and never reaches the extracted month/day; the month-range guard is observationally subsumed by the per-row `row.month !== numericMonth` filter and kept only to skip a pointless year expansion. Every killable mutant (81/87) is pinned, most by the five-century anchor table with witness years 151/1700/3165. |
| R11 | Rejected | `validDateParts`' **year** and **day** integer guards look unasserted (both mutants survive). | **Equivalent mutants.** An exhaustive witness search over 15 hostile value types found no distinguishing input: `d.getFullYear() === parts.year` and `d.getDate() === parts.day` compare strictly against the raw value, so the roundtrip check subsumes them. Only the **month** guard, whose comparison coerces (`"5" - 1`), is load-bearing; its regression coverage landed with T470. |
| R23 | Rejected | The suite depends on the host timezone. | Re-verified on 2026-07-30: the full JS suite passed 926/926 with every coverage gate under each of `TZ=Pacific/Auckland`, `TZ=America/Sao_Paulo`, and `TZ=UTC`. `test/helpers/clock.js` freezes the wall clock and the tests pin `LOCAL_TIMEZONE` explicitly. |
| R13 | Rejected | `metadata.json` is missing `icon` / `author`. | Cinnamon's `requiredProperties` for an applet are `uuid`, `name`, `description`; `author` belongs in `info.json` per the Spices spec, and is there. `max-instances: -1` is valid. |
| R14 | Rejected | The `5.4/` shims are indirection; the root modules "depend on files that only exist in the parent". | Intended and correct on every declared version: Cinnamon's applet manager roots `imports.applets` at the applets folder, so `[uuid]` is the parent directory. The thin shims work, `gjs_import.test.js` pins them, and the former `require()` host-detection risk was resolved with T464. |
| R16 | Rejected | `metadata.json` omits Cinnamon 6.6, so the applet is incompatible with the installed desktop. | Re-verified against Cinnamon 6.6.9's loader: `cinnamon-version` entries are minimum compatible versions, and the declared entry admits 6.6. The compatibility test models that loader rule directly. The floor was raised from 5.4 to 6.0 when T770 established that the HTTP layer is libsoup-3 only; the loader rule this row rests on is unchanged. |
| R17 | Rejected | Sonar `css:S4654`: `icon-size` is an unknown CSS property. | **False positive.** Cinnamon's shipped `/usr/share/cinnamon/theme/cinnamon.css` uses the same property throughout; it is an St theme property, not browser CSS. The applet's `icon-size: 4em` is valid Cinnamon styling. |
| R18 | Rejected | Sonar's remaining analyzer-labelled bugs: unused `Tooltip` constructions, implicit string sort, an empty-matching URL regex, and an unseeded `reduce`. | **False positives.** Cinnamon `TooltipBase._init` registers actor/stage callbacks holding each tooltip instance; package paths intentionally sort lexicographically; `urlForLog` intentionally accepts an empty optional path; and `joinPhrases` returns before `reduce` when the array is empty. Existing tests exercise each behavior. |
| R19 | Rejected | Sonar's two vulnerabilities in `test/helpers/appletFixture.js` and `test/helpers/coverage.js`. | **False positives outside shipped code.** `"/tmp/cache"` is a return value in an in-memory GLib test double, not a filesystem write, and the coverage harness invokes the same developer/CI `node` selected by the already-trusted process environment. They do not affect the shipped applet or its local production gates; the related Sonar reconfiguration was rejected in R22. |
| R20 | Rejected | Sonar's mass `var`, object-spread, class-field and test-stub findings. | Not defects. All `javascript:S3504` sites are zero-indented legacy GJS module bindings whose `var` visibility is the import/export mechanism covered by R05/D02. The remaining findings are compatibility-sensitive alternatives, deliberate empty test doubles, or trivial local style; changing them wholesale adds runtime-floor risk without correcting behavior. The feasible optional-chain and own-property cleanups were applied after re-evaluation, using older-runtime-safe equivalents in production GJS. |
| R21 | Rejected | Sonar `javascript:S8786`: two end-whitespace regexes can backtrack super-linearly. | Re-verified after T574: every input is bounded before either regex, including event/holiday names at 300 code points, configured formats and rendered stamps at 256, panel labels at 64, clock labels at 24, and at most eight configured clocks. Rewriting the regex separately adds no distinct mitigation. |
| R24 | Rejected | T701: prefer an authoritative empty holiday result over an earlier provider failure (`holidayAdapters.js` exhaustion picking `failure \|\| emptyResult`). | **The premise is wrong: the current outcome is order-independent and deliberate.** An empty payload is believed only when *every* provider returns one — stated in `_classify`'s comment and pinned by two tests ("an empty answer is believed once every provider gives one", "an empty answer followed by a hard failure is not cached as success"). Verified empirically on 2026-08-05 by driving the real chain both ways: failure-then-empty and empty-then-failure both yield `last_error = "Holiday service unavailable"` with the year uncached, so no reordering bug exists. Implementing the row would make the answer depend on chain order — and `orderProvidersByLastSuccess()` reorders the chain at runtime, so the same evidence would produce a holiday-free year on one run and an error on the next. The unanimity rule exists because an empty array is exactly how Enrico reports a country it does not cover: believing one provider's empty while another was unreachable would cache a wrong holiday-free calendar for the 50-day period. |
| R25 | Rejected | T742: apply the Open-Meteo population floor per candidate instead of to the ranked winner. | **The premise did not survive live data, and the change regresses the case the floor exists for.** The row was written from a synthetic payload in which an unaccented `Sao Paulo` hamlet outranked the city and dragged the whole answer down. Sampled live on 2026-08-06 (`language=en`, `count=10`), that payload does not occur: `Sao Paulo` returns `São Paulo`/Brazil (12,400,232) first, and the applet already resolves it correctly today with no fallback request. The real payloads that *do* refuse are `Malmo` — exact `Malmo`/United States (118) outranking `Malmö`/Sweden (362,133) — and `Genova`, whose only exact spelling is a thirty-person Veneto hamlet. Filtering candidates by the floor makes `Malmo` answer Sweden, but makes `Genova` answer **Génova, Guatemala (3,744)**, because that is then the best-ranked trustworthy hit. Both cases are structurally identical — an untrustworthy exact match displaced by a trustworthy fold-equal one — and differ only in the population of the fold-equal hit, so no non-arbitrary rule separates them. Today's behaviour is correct in both: the primary declines, and Nominatim (ranked by the typed name since T722) answers Genoa and Malmö. What is left of the finding is cost, not correctness — one extra request to a rate-limited geocoder, and a resolved place carrying no timezone, which is **T714**. The place-name folding half of T742 was implemented; only this half is rejected. |
| R22 | Won't fix | Replace SonarCloud automatic analysis with scoped CI analysis that imports coverage and distinguishes tests from production sources (T557). | Rejected explicitly by the maintainer on 2026-07-23. No implementation will be pursued; the existing local and CI coverage gates remain authoritative. |
| R27 | Rejected | T761: `cancelPendingLocaleQueries` enumerating `_inflight` rather than `requested` (the surviving operator mutant of that change). | **Equivalent mutant.** `_requestInfo` writes both maps in the same statement pair and `_releaseQuery` clears both together, so `Object.keys(_inflight)` is always a subset of `Object.keys(requested)`, and `_cancelPendingRequest` returns early for exactly the envs in the difference — it is guarded on the `_inflight` record itself. The two enumerations therefore cancel the same set for every reachable state; no input distinguishes them. Recorded so a later mutation audit does not re-raise it. |
| R28 | Rejected | T763: `EventIndex.hasEvent` reading `_eventIds` rather than `_daysById` (the surviving operator mutant of that change). | **Equivalent mutant.** The two collections are filled and emptied together by construction: `register` admits to `_eventIds` and then always reaches `_noteDay` at least once, because `_spannedDays` pushes its first day before testing the bound; `remove` and `_removeUidFromDays` drop from both; `clear` and `_rebuildEventState` reset both from the same walk. The one window where they disagree is inside a single synchronous `register()` call, between `_prepareEvent`'s `_removeFromBuckets` and the re-registration below it, which no caller can observe. `hasEvent` reads `_eventIds` because that is the set `_admitEvent` counts against `MAX_INDEXED_EVENTS` and `_resyncOverflow` consults, not because the other answer would differ. Witnessed by "fuzz: the id set and the day map always cover the same events" (`test/eventsManager.test.js`), which drives 400 randomized rounds of add/remove/cull/clear across and outside the window and asserts the two key sets stay equal — kept so a future change that breaks the invariant is caught even though no test can distinguish the mutant. |
| R29 | Rejected | T882: a resync retry can clear the index's temporary overflow flag without retiring the visible warning. | **The retry cannot inherit that marker.** `applyResync()` reaches `requestResync()`, whose only path queues `idleDoReloadSelected()`. That idle calls `retireStrandedResyncOverflow()` after `reloadSelected()` returns, regardless of whether the replacement range dispatched, declined, or threw inside `_dispatch()` and entered retry state (`eventFetchCoordinator.js:276-285`). Retirement clears `EventIndex.overflowed` and emits the index/selected-day state immediately. A focused reproduction forced the replacement dispatch to throw and observed `_resyncOverflowPending === false`, `overflowed === false`, and the visible false state before its retry ran. The direct `select_date()` test that can leave a manually injected marker armed is not a path `applyResync()` uses. |
| R30 | Rejected | T887: overlapping holiday annotation passes share counters and provider credit. | **No production pass shares a generation.** `Calendar._update()` increments `_holiday_update_generation` immediately before every `annotate()` call (`6.0/calendar.js:1105-1115`), including navigation, event delivery, settings repaint, and menu refresh. `_receiveMonth()` checks that render generation through `_isCurrent()` before decrementing `_awaited`, collecting provider credit, reporting status, or reconciling cells (`6.0/calendarAnnotations.js:438-449`). Therefore a callback from any older render is inert even when the holiday country is unchanged; the shared fields belong only to the one generation still admitted. Header rebuild and teardown also advance the same guard. Recorded because reading the field name as a country-only generation makes the race look real until the composition call is traced. |
| R31 | Rejected | T936: the five production functions whose cyclomatic proxy exceeds 10 violate the repository's documented cognitive-complexity ceiling and lack an automated gate. | **The scan measured a different metric, and the requested gate already exists.** `test/complexity.test.js` measures every shipped and tooling JavaScript function with a cognitive-complexity walker and fails at 11; `test/test_complexity.py` applies the same ceiling to every shipped and test Python function. Both run under `npm test`, their walkers have rejection fixtures, and the current suite is green. The reported cyclomatic scores therefore neither demonstrate a violation of the cognitive rule nor identify a missing gate. |
| R26 | Won't fix | T712: stop rewriting the saved world-clock list to disk when the settings window opens. | Declined by the maintainer on 2026-08-06. The finding itself was verified and is not disputed: `ClocksList.__init__` calls `normalize_clock_setting(info, key, settings)` before `JSONSettingsList.__init__` (`chronos_settings_widgets_worldclocks.py:587`), which writes back through `settings.set_value(key, normalized)` whenever the projection differs (`:279-289`); `normalize_saved_clocks` (`:249-276`) drops non-dict rows, rows whose `label` is blank after `strip()` even with a valid `timezone`, rows naming a runtime built-in, and — via `break` at `:273-274` — everything past the eighth entry; and because Cinnamon builds every settings page up front, merely opening the window discards those rows with no prompt and no undo, while the JS runtime filters the same rows non-destructively at render time (`worldclockData.js:453-461,463-487`). Kept as a tombstone so future audits do not re-open it; reverse only on a maintainer decision. |
| T740 | Deferred | Publish 2028+ dates for the religious observance tables that are not Hebrew-calendar backed. | The Hebrew keys no longer need a table at all - `hebrewCalendar.js` computes them exactly for any year - and the Baha'i keys were corrected and now run 1844-2065: the pre-2015 Western convention (fixed 21 March) for 1844-2014, the Baha'i World Centre's published astronomical table for 2015-2065, and Ridvan derived as Naw-Ruz + 31 rather than tabulated. Past 2065 nothing authoritative exists - and 2092 and 2096 are 19 March on the best available reckoning, so the 20/21 alternation cannot simply be extrapolated. The remaining keys stay bounded, and computing them was evaluated and rejected on precision. **Islam**: tabular Hijri is closed-form but matches Umm al-Qura on only 55% of `eid-al-fitr` and 59-74% of the other keys over AH 1300-1523, and Umm al-Qura is itself tabulated data whose criterion changed four times, is absent before 1883, and is explicitly civil-only - its makers note first sighting can trail it by two days, and Eid al-Fitr 2025 was kept on two different days by 11 and 15 countries respectively. **Baha'i**: Naw-Ruz 2026 turns on about a minute between the equinox and Tehran sunset, and Tehran's 1189 m elevation alone moves that sunset five minutes; the definition changed in 2015 and the authoritative table stops at 2065. **Hinduism, Jainism, Buddhism, Sikhism**: there is no single correct civil date to compute - Diwali 2025 split Indian state governments between 20 and 21 October on local sunset, Vesak 2023 spanned 57 days across four rules, and Sikh reckoning changed in 2003 and was partly reverted in 2010. **Chinese**: computable but out of scope here; see T776. Unparks when the cited multi-faith calendars publish past 2027, or when a per-tradition authority is adopted and the convention recorded per key. See T774 and T775 for data defects found while checking. The suite gate no longer collides with this: it fails only once the tables cannot answer for the current year, and warns for a year before that, naming the religions that are about to lapse. |
| T1027 | Deferred | `ioUtils.readTextFileCapped` reads the whole file and then checks the cap, which the same module condemns for the cache file. | **Needs a maintainer decision; not implemented.** ioUtils.js:72-83 calls `GLib.file_get_contents` and *then* tests `tooBig`. The comment at :111-118 condemns exactly that pattern for the holiday cache ("Ask the filesystem how big it is and refuse before issuing the read") and adds a pre-read `query_info_async`; the header at :62-71 half-answers it ("these two files are nothing like a holiday cache in size"). **The decision:** is "no unbounded read into the compositor's address space" absolute, or scoped to attacker-influenceable paths? `/etc/timezone` and `zone.tab` are root-owned, so the risk is a broken system rather than a hostile one — but the policy as this file states it carves out no such exception. Options: (a) add the pre-read `query_info` to `readTextFileCapped` and make the policy absolute; (b) narrow the policy comment to attacker-influenceable paths and say why these two are exempt. Unparks when one is chosen. |
| T1028 | Deferred | `weatherScheduler`'s retry callback runs `refresh()` unguarded while the periodic one wraps it, with no stated reason. | **Needs a maintainer decision; not implemented.** weatherScheduler.js:167-174 calls `refresh()` inside the one-shot retry timer with no try/catch; `_armPeriodic` (:122-136) wraps it and explains why ("An exception must not remove the recurring GLib source"). For a one-shot the source is removed anyway, so the only cost is an unhandled exception escaping a GLib callback and the retry ladder not advancing. **The decision:** is the asymmetry deliberate — let a retry throw loudly — or an oversight? Options: (a) wrap it and log, matching the periodic arm; (b) leave it and add the one comment saying a retry is meant to be loud. Unparks either way. |
| T1029 | Deferred | `MAX_WEATHER_READING_CACHE_ENTRIES` is defined as `WeatherFormat.MAX_GEOCODE_CACHE_ENTRIES`; nothing says whether that is coupling or coincidence. | **Needs a maintainer decision; not implemented.** weatherProviders.js:261. One panel place plus `ClockLimits.MAX_CLOCKS` cities is 9 today against a cap of 16, so it holds. If `MAX_CLOCKS` is ever raised past 15 the reading cache starts thrashing, and no test covers it; cityWeather.js:66-72 carries a comment about exactly this class of hidden cap coupling. **The decision:** one shared cap for both caches, or two independent constants with the reading cap derived from `MAX_CLOCKS + 1` and a test pinning the relation. Unparks when the intent is stated. |
| T1030 | Deferred | `WeatherProvider` increments `_request_generation` twice on one synchronous path. | **Needs a maintainer decision; not implemented.** weather.js:207 bumps the generation and then `_scheduler.schedule` synchronously reaches `refresh`, which bumps it again at :274. Harmless today because nothing observes the intermediate value, but "generation" then has two meanings on one code path. **The decision:** is the :207 bump load-bearing for an injected-scheduler case (a scheduler that defers, where the stale in-flight answer must be rejected before `refresh` runs), or removable? Answering it needs the intent, not the code. Unparks when stated; the fix is one line either way plus a test pinning which reading is rejected. |
| T1031 | Deferred | `eventFormat.js` has four near-duplicate range-end formatters whose branch orders differ; merging blind would change labels. | **Needs a maintainer decision; not implemented.** eventFormat.js:81-179 — `_prefixForTodaySelected`, `_prefixForOtherDaySelected`, `_suffixForTodaySelected`, `_suffixForOtherDaySelected` share the same three outcomes (a Today/time form, a nearby-weekday form, a `%x` form) but branch in different orders, and the prefix pair has a "started before" arm the suffix pair lacks. **The decision:** is the prefix/suffix asymmetry specified behaviour or accretion? Merging into one table-driven helper without that answer would change the label of every multi-day event spanning the ±4-day window. Unparks when the intended wording per case is written down — ideally as a table in the test. |
| T1032 | Deferred | A throw out of `eventMutationStream`'s idle callback may leave two live GLib sources tracked by one id. | **Needs a maintainer decision; not implemented, because it needs a fact about the runtime rather than a reading of this code.** eventMutationStream.js:190-216 — `applyNext()` calls `_recoverFailure(mutation)`, which calls `schedule()` and adds a *new* idle, and then rethrows. The throw propagates out of `_runScheduledMutation`, the `Mainloop.idle_add` callback, after `_eventBatchIds.shift()` has already dropped the current id. If GJS does not treat a thrown source callback as `SOURCE_REMOVE`, there are then two live idles with one tracked id and `reset()`/`destroy()` can remove only one — an orphan source outliving the applet. **The decision:** confirm GJS's return-value semantics for an exception thrown from a `gboolean` source callback on Cinnamon 6.0 and on the current release. If it is not guaranteed `SOURCE_REMOVE`, wrap `applyNext()` in `try/finally { return GLib.SOURCE_REMOVE; }`. Related to T937, which asks for the same kind of real-runtime evidence. |
| T1033 | Deferred | The day-cell dot box asks for a full re-render from inside a Clutter allocation; whether that can oscillate needs a live run. | **Needs a maintainer decision; not implemented.** 6.0/calendar.js:408-415 — the `dot_box` allocate handler calls `dotCapacityChanged()` → `_queue_update()` → a full 42-cell re-render, from inside an allocation. It converges in theory (capacity depends on box width and `max-rows`, not on child count) but the box width is itself allocation-derived. **The decision:** whether a theme in which the dot box width tracks its content can oscillate. This wants an instrumented run on a real desktop with a content-sized theme, not a code claim — the same evidence class as T935. Unparks with that observation. |
| T1034 | Deferred | `enqueue` drops `fetch-complete` markers while a resync is queued, and it is unclear whether reconciliation is still guaranteed. | **Needs a maintainer decision; not implemented.** eventMutationStream.js:116-140 drops `fetch-complete` while `_resyncMutationQueued`, so eventFetchCoordinator.js:104-108 `onFetchComplete` never arms the GC timer for that fetch. `onMutationApplied` usually covers it, but a fetch that delivers no adds after a flood collapse would leave reconciliation unscheduled until the next mutation. **The decision:** is a post-resync fetch guaranteed to deliver at least one `add`? If not, `fetch-complete` must bypass the resync drop. Answering it needs the calendar-server delivery contract, not this file. |
| T1035 | Deferred | `HolidayService.last_error`/`last_provider` are a public accessor pair — including a setter — read and written only by the suite. | **Needs a maintainer decision; not implemented.** holidays.js:289-304. Production reads status through `_statusFor` → `HolidayStatusLedger.for(key)`; the accessors are exercised only by test/holiday_cache.test.js and test/holiday_adapters.test.js. Decision D02 licenses a widened export surface for testing, so this is not automatically a defect — but D02 also says production must not bend to a double, and a public *setter* is closer to that line than a getter. **The decision:** keep them as a declared test seam, or remove them and rewrite roughly fifteen assertions against the ledger. Unparks either way. |
| T1036 | Deferred | The `IS_NODE` bootstrap comment is duplicated in four holiday modules and absent from two others. | **Needs a maintainer decision; not implemented.** The 15-line host-detection comment appears verbatim in holidays.js:14-27, holidayCache.js:14-27, holidayAdapters.js:14-27 and holidayServiceAdapters.js:27-40, while holidayRecord.js:13-14 and religiousHolidays.js:28-29 carry the bare expression with no comment — two conventions in one subsystem. The *code* cannot be factored out; it is the bootstrap. **The decision:** normalise downward (bare expression everywhere, one canonical explanation elsewhere) or upward (the full comment in all six). Purely a style call; see T1007, which fixes a real drift in the same family and is not blocked on this. |
| T1037 | Deferred | `6.0/applet.js` carries 26 two-line `_guarded("tag", …)` wrappers; a dispatch table would remove them and cost greppability. | **Needs a maintainer decision; not implemented.** `_onShowEventsChanged` (:469-472), `_onPanelFormatChanged` (:474-476), `_onTooltipFormatChanged` (:478-480), `_onPanelFontScaleChanged` (:487-489), `_onShowAstronomyChanged` (:508-510), `_onWeatherUnitsChanged` (:519-521) and twenty more are each pure boilerplate around one call. A `{ tag: fn }` table plus a generated dispatcher removes about fifty lines. **The decision:** the current form makes every handler independently greppable and breakpoint-able, which is worth something in a compositor-hosted applet where a bug costs the session. Whether the repetition is a cost here or the point is the maintainer's call. |
| T1038 | Deferred | `settingsFacade.js` wraps two different settings systems with different lifetimes; splitting it costs a shim and two call-site edits. | **Needs a maintainer decision; not implemented.** `DesktopSettings` (settingsFacade.js:113-181) wraps `Gio.Settings` on `org.cinnamon.desktop.interface` — manual disconnect, its own lifetime, missing keys reading as `false`. The other five classes wrap Cinnamon's `AppletSettings`, released wholesale by `finalize()`. The file header at :24-31 frames this deliberately as one boundary. It is defensible as one file, and it is also the direct cause of T1003. **The decision:** split into `settingsFacade.js` + `desktopSettingsFacade.js` — costing a new 16-line `6.0/` shim and edits in 6.0/appletLifecycle.js and 6.0/calendar.js — or keep one file and fix T1003 in place. T1003 is worth doing either way. |
| T1039 | Deferred | `SettingsWindowCenterer` never keeps its GLib idle source id, so a closed settings window is kept alive for up to 100 idles. | **Needs a maintainer decision; not implemented.** chronos_settings_widgets_worldclocks.py:551-552 — `GLib.idle_add(self.center)` holds a strong reference to the centerer and through it the `ClocksList` and its whole widget tree, and nothing can cancel it. It is self-limiting (100 attempts, then `False`), so it is not a leak — but if the window is closed during those idles, `get_toplevel()` is called on a destroyed widget tree. **The decision:** accept the bound as sufficient, or keep the source id and `GLib.source_remove` it from a `destroy` handler. |
| T1040 | Deferred | Settings widgets never unregister their `JSONSettingsHandler` listeners; whether that can matter needs xlet-settings knowledge. | **Needs a maintainer decision; not implemented, because the answer is outside this repository.** `JSONSettingsBackend.attach()` calls `self.settings.listen(key, cb)`, `JSONSettingsHandler` has no `unlisten`, and none of the four widgets disconnect on destroy. Whether cinnamon-settings ever rebuilds an xlet page against a surviving handler inside one process could not be established here; with multiple applet instances it appears to build one handler per instance, which would make this harmless. **The decision:** someone who knows the xlet-settings instance-switching path must confirm. If a rebuild is possible, every widget needs teardown, because the stale callbacks call `set_text`/`set_active_iter` on finalized GTK objects. |
| T1041 | Deferred | `_LAST_COMPLETION_KEY` is a single-entry global memo shared by two completion matchers that can be on screen together. | **Needs a maintainer decision; not implemented.** chronos_settings_widgets_common.py:46, :93-110. Correct as written, but the weather location field and the country combo can both be realised at once; if GTK ever interleaves their match functions with different keys, the memo degrades to a miss on every row and the "folded once per keystroke" property disappears silently, with no test able to see it. **The decision:** accept it (single-threaded, one popup at a time in practice) or make it a two-to-four entry dict. Related, and equally a call: `common.attach_completion` takes an `on_selected` parameter that two of its three call sites use, while `WeatherLocationEntry.ensure_completion` (chronos_settings_widgets_weather.py:188-190) connects `match-selected` itself afterwards — conform the third site, or drop the parameter. |
| T1042 | Deferred | `ClockEntrySerializer.serialize` hard-codes the column order the dialog reads from the schema. | **Needs a maintainer decision; not implemented.** chronos_settings_widgets_worldclocks.py:321-323 uses `CLOCK_COLUMN_IDS` order while `build_content` (:470) deliberately follows *schema* order. The base `List.add_item`/`edit_item` write the returned list into the model positionally and `list_changed` keys it by `columns[i]["id"]`, so swapping the two schema columns would break the write while the dialog kept working — the failure the comment at :296-306 describes, guarded today only by `schema_static.test.js`. (That comment's phrasing, "keys the persisted JSON by *position*", is loose: it keys by the schema id at that position.) **The decision:** leave it pinned by the schema test, or have `serialize` derive its order from the schema so the two halves cannot disagree. |

## Recorded decisions

| id | decision |
|----|----------|
| D01 | Keep the third-party endpoints rather than a bundled data set or a single vendor. A bundled table goes stale and covers fewer countries; a single provider breaks whenever it is down — the fallback chain exists because the primary is a hobbyist service. Risk accepted with mitigations (weather opt-in; one-time local-tzdata holiday default with visible third-party disclosure and explicit disable/override; size caps; bounded spans; shape checks in and out of cache; colour filtering; no URL logging). |
| D02 | **Keep the widened export surface.** ~80 exported names have no cross-module consumer — used inside their own file and otherwise only by `test/`. The applet runs inside a compositor where a bug costs the whole session; testing only through the composition root would be slow and flaky. **This does not license the inverse:** production must not bend to a test double's shape. |
| D03 | **Use the GitHub handle `geraldo-netto` in Spices `info.json`, and keep the display name "Geraldo Netto" in package metadata and user-facing attribution.** The upstream `validate-spice` contract rejects whitespace in `info.json.author`; `schema_static.test.js` pins the submission value while `package.json`, the README, source credits, and About dialog retain the human-readable name. |

## Resolved merge choices

Decisions from the calendar@ccprog / Simon Wiles merge. Kept so future audits do not re-pick them.

| id | resolution |
|----|------------|
| G01 | Kept the `calendar@ccprog` world-clock settings workflow (Cinnamon-native settings, up to 8 user clocks plus built-in UTC/local, region/city selection, IANA/city entry, timezone validation, inherited coverage). Simon Wiles' separate GTK editor remains unported. |
| G02 | Kept `calendar@ccprog` locale-aware weekend behaviour (locale-derived work weeks plus a configurable one- or two-day weekend length). |
| G03 | Kept the copied `calendar@ccprog` visual assets as the initial merged set. The Chronos screenshot was refreshed on 2026-07-16; the icon was replaced with an original design and the orphaned root `calendar.png` deleted on 2026-07-18. |
