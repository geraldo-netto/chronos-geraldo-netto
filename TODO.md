# chronos@geraldo-netto TODO

Audit ledger for this applet. Full-source rescan on 2026-07-13 against every `agent-instructions/` review category **including the special ones — wiring gaps, unused functions/methods, legacy/deprecation, and variable/function scope**. Run blind: eight parallel scanners (security/data-governance, reliability/state-machines, wiring-gaps/dead-code/scope, architecture/duplication, testing/fuzzing, UI/accessibility/i18n, performance/memory, docs/packaging/release) each read the source **without this ledger**; their findings were merged and deduplicated afterwards. Only four rows survived from the previous ledger (T434, T439, T455, T343) — every other row below is new.

**Findings verified by running or mutating the code are marked [verified]**, each naming the experiment. The testing scan ran ~85 mutants and reports the 21 that survived; the performance scan measured against the real modules under `cjs 115.1` and reports numbers.

Baseline: `npm test` green (670 JS tests, JS coverage per-file 98/90/100; Python 111 tests, 98 %+ lines), `npm run lint` clean, CI runs both on every push and PR. The gates are real — what this pass found is largely what they do not look at.

Open items: 65 (Critical 0, High 9, Medium 29, Low 27).

## Findings

### High

| ID | Category | Severity | Status | Effort | Description | Notes |
|----|----------|----------|--------|--------|-------------|-------|
| T463 | performance / memory | High | open | S | **[verified]** The applet is never released. Cinnamon's `Applet` base class has no `destroy()` (`grep -c destroy /usr/share/cinnamon/js/ui/applet.js` → `0`), so the chain `Main.uiGroup → AppletContextMenu → sourceActor (applet.actor) → _delegate → applet` outlives removal — plus this applet's own context-menu item (`appletMenuBuilder.js:255`), whose `activate` closure captures the builder. `_destroy()` releases timers, signals and the menu but keeps every heavy structure: the event index (a month of `EventData`, 4 `GLib.DateTime` each), `HolidayCache.data` + `_holidayIndex` + `_monthIndex`, the 42 `_day_cells`, both geocode caches. `CityWeatherProvider.destroy()` already clears `_readings`; the other three owners do not. | Verified: WeakRef + `--expose-gc` against the real `AppletMenuBuilder` — the applet stays reachable after `_destroy()`; 10 add/remove cycles retained **+38.2 MiB**. Every panel-edit drag, every enable/disable in the Applets dialog and every `on_applet_reloaded` strands another full copy for the rest of the login session. Fix: clear the owned state in `_destroy()` (`EventsManager` → `_event_index.clear()`; `HolidayService` → clear cache/indexes; `Calendar` → `_day_cells = []`) and remove the context-menu item the builder added. |
| T464 | legacy / forward-compat | High | open | M | **[verified]** The dual-loader preamble inverts on the next Cinnamon and the applet will not load. `utils.js:5-15` (and ~14 other root modules) branch on `typeof require === "function" ? require("./localeUtils") : imports.ui.appletManager.applets[uuid].localeUtils`, asserting in a comment that Cinnamon provides "neither `require()` nor `module`". True for 5.4–6.4. But Cinnamon **master** now sets `globalThis.require = xletRequire` (`js/ui/extension.js:366`), and `_requireLocal` resolves `./x` against `extension.meta.path`, which `findExtensionSubdirectory` has already repointed at the **`5.4/` dir** — so the root modules would look for `5.4/localeUtils.js`, `5.4/ioUtils.js`, `5.4/holidayCache.js`, none of which exist. | Verified: fetched `extension.js` at tags 5.4.0/5.6.0/5.8.0/6.0.0/6.2.0/6.4.0/6.4.5 (no `globalThis.require` in any) and at master (present), and read `_requireLocal`/`installXletImporter`/`findExtensionSubdirectory`. Not broken today; breaks on the Cinnamon after 6.4, and `test/gjs_import.test.js` pins the *current* assumption, so nothing will warn. Fix: make the guard test for the xlet importer rather than for `require`, or ship the root modules where `_requireLocal` will look. |
| T465 | i18n | High | open | S | **[verified]** Every locale renders the US month-day-year order. `localeUtils.js:203-204` wraps `DATE_FORMAT_SHORT = _("%B %-e, %Y")` and `DATE_FORMAT_FULL = _("%A, %B %-e, %Y")` in `_()` precisely so translators can reorder them — and **not one of the 15 catalogs translates either**. | Verified: extracted every msgid from every `.po`; these two are untranslated in all of ca/da/de/es/fi/fr/hu/it/nl/pt_BR/ru/sv/tr/vi/zh_TW, so gettext returns the msgid. A German user's event-list heading reads "Samstag, Juli 12, 2026" instead of "Samstag, 12. Juli 2026"; the same string is `ACCESSIBLE_DATE_FORMAT` (`5.4/calendar.js:39`), so her screen reader announces all 42 day cells that way too, and it is the tooltip's date line. The machinery is right; the data was never filled in. Fix: populate the 15 msgstrs. |
| T466 | i18n | High | open | S | **[verified]** The **default** panel label and tooltip clock hardcode a day-before-month order for every locale: `5.4/appletPanelStatus.js:23-24,38-40` — `"%d %b %H:%M"`, `"%d %b %l:%M %p"`, `"%d %b %H:%M:%S"`. Not passed through `_()`, not derived from `LC_TIME`, no `%x`. | Verified: `grep '%d %b' po/chronos@geraldo-netto.pot` → no match, while the `.pot` is otherwise complete (re-extracted every `_()`/`ngettext()` and every schema string: zero missing). An `en_US` user with default settings reads "13 Jul 14:52" on the panel while Cinnamon's own clock beside it says "Jul 13"; a Japanese or Hungarian user gets the wrong order too. `calendar.js:34-38` complains about exactly this class of bug for the accessible date — the panel is the case that was missed. |
| T439 | packaging | High | open | S | **[verified]** `5.4/icon.png` is committed as a git **symlink** (mode `120000` → `../icon.png`), not a file. Zip delivery (the Spices "Download" tab) and several install paths do not preserve symlinks: the entry lands as a broken link or a 12-byte file containing the literal text `../icon.png` — a corrupt PNG, so the applet is iconless. | Verified: `git ls-files -s` → `120000 …`. The rescan also found it **unnecessary**: the Spices spec puts the icon at `files/UUID/icon.png`, which already exists as a real 48×48 file; Cinnamon only adds `5.4/` to the icon *theme* search path. Fix: delete the symlink (or replace it with a real copy). Raised from Medium — the previous ledger had not established that zip delivery is the shipping path. |
| T468 | testing / correctness | High | open | S | **[verified]** After the GC culls deleted events, nothing repaints the grid — and the test cannot see it. **Mutation:** `eventsManager.js:522` `this.emit("events-updated")` → `"events-updatedX"` → **survived**. `calendar.js:794` is the only subscriber, so stale event dots persist on the 42 cells. The test at `eventsManager.test.js:669` is titled "gc timer culls stale events **and reports**" and asserts only `_event_index` state — the emit line is covered, never asserted. | Pure coverage theatre. Fix: assert the emit, and mutation-check it. |
| T469 | testing | High | open | S | **[verified]** The panel clock's tick signal name is unasserted. **Mutation:** `5.4/appletLifecycle.js:188` `this.clock.connect("notify::clock", callback)` → `"notify::clockX"` → **survived**; the panel would simply stop ticking. Sibling signals *are* pinned (renaming `"notify-resume"` or `"enter-event"` fails the suite), so this is a gap, not a policy. | Fix: a test that drives the real connect and fails on the rename. |
| T470 | correctness / validation | High | open | M | **[verified]** Two holes in the holiday adapters' remote-JSON validation, neither reachable by any test. (a) `holidayServiceAdapters.js:79` `validDateParts` accepts a **non-integer month**: deleting `!Number.isInteger(parts.month) ||` survives, with witness `{year:2026, month:"5", day:5}` → original `false`, mutant `true` (the roundtrip check coerces `"5" - 1` to `4`). Enrico hands `date:{year,month,day}` straight off the network into this validator, and a string month is then persisted and rejected on **reload** by `validCachedHoliday`'s `Number.isInteger` — the two validators disagree. (b) `:338` and `:421`: deleting the per-row `typeof holiday.name === "string" &&` (Nager), `typeof holiday.date === "string" &&` (Nager) or `typeof holiday.startDate === "string" &&` (OpenHolidays) all survive; without them a *single* malformed row flips the outcome from "skip that row" to "reject the whole country's payload as INVALID_RESPONSE and fail over to the next provider". | Verified by mutation; the tests only ever feed out-of-range **integers** (`month: 13`, `day: 32` — `holidays.test.js:427-430`), never a wrong **type**. Fix: property/fuzz tests that feed each adapter hostile row types. |
| T471 | testing | High | open | M | **[verified]** `holidays.js:206` `HolidayInflight.settle()`'s generation guard is never executed. **Mutation:** `if (!entry \|\| entry.generation !== generation)` → `if (false)` → **survived** (it would `TypeError` on `entry.callbacks` if ever reached with a missing key — proof no test reaches it). Its comment says "a response for a generation that is no longer current owns nothing here"; that is the stale-response defence, and it is unverified. Lines 207-208 are exactly the two the coverage report lists as uncovered for the file — which still scores 98.82 %, over the gate. | Fix: a test that lands a superseded response after a country change. |

### Medium

| ID | Category | Severity | Status | Effort | Description | Notes |
|----|----------|----------|--------|--------|-------------|-------|
| T472 | correctness | Medium | open | M | **[verified]** City weather is keyed by the user's free-text clock **label**, not by the city. `cityWeather.js:333` dedups on `locationCacheKey(city.label)` and `:392` stores the reading under it, but the reading is *of* `city.query`. Nothing enforces label uniqueness, and `clockDisplayLabel` clamps labels to 24 code points, so two labels sharing 23 characters also collapse. | Verified: drove the real `CityWeatherProvider` with the `cities` array `5.4/applet.js:331-335` builds. Two clocks both labelled "Home" (Europe/Lisbon, Asia/Tokyo) → `_readings` has one key, **Tokyo is never geocoded and never fetched**, and both rows display Lisbon's temperature with no staleness or error marker. Fix: key `_readings` and the dedup on `city.query`, and look up by timezone-derived city. |
| T473 | performance / memory | Medium | open | S | **[verified]** The live holiday structures are never pruned — only the persisted copy is. `holidayCache.js:386` `_windowedForPersist()` filters a **copy** to ±1 year, while `this.data`, `_holidayIndex`, `_monthIndex`, `years` and `attempts` accumulate every holiday of every year the user scrolls to, for the session. (`_matchedMonthCache` is LRU-capped at 32; the four beside it are not.) Ceiling is `MAX_EXPANDED_HOLIDAY_ROWS = 4000` × years browsed. | Verified against the real module: browsing 60 years at 15 holidays/yr → 900 rows, **+469 KiB**; at 100/yr → 5040 rows, **+1.44 MiB**. The persisted window keeps 45 and 252 rows. Compounds with T463 across reloads. Fix: LRU the years as `_rememberMonth` LRUs the months — a hard ±`YEAR_WINDOW` prune is **not** correct (the comment at `:381` records that it discarded a just-fetched out-of-window year and its freshness stamp, causing an infinite refetch loop). |
| T474 | performance / network | Medium | open | S | **[verified]** Any weather setting change throws away the cached geocode and re-issues two HTTP requests. `weather.js:214` `queue()` unconditionally calls `this._location_resolver.forget(settings.location)`, but it is reached from `_onWeatherSettingsChanged` for **all** weather keys — including `weather-units`. The reading record is deliberately unit-free and `_staleKey()` deliberately excludes units ("the same reading serves both", `:274-281`), so a unit toggle needs a re-render, not a refetch. | Verified with the real `WeatherProvider` + a fake HTTP layer: flipping °C→°F issues `geocoding-api.open-meteo.com` + `api.open-meteo.com` — two fresh round trips — and so does toggling weather off/on. On an Open-Meteo outage the geocode falls through to Nominatim, whose usage policy is 1 req/s. Fix: `forget()` only when `locationCacheKey(settings.location)` actually changed. |
| T475 | correctness / duplication | Medium | open | S | **[verified]** One truncation rule, four implementations — and the surrogate-safety fix was applied to only two of them. `worldclockData.js:142` and `5.4/appletPanelStatus.js:409` count **code points** (`Array.from`), the latter with a comment saying "a blind slice could split a surrogate pair". `eventData.js:64` (`clampEventSummary`, 300) and `holidayCache.js:53` (`clampHolidayName`, 300) do exactly the blind slice — on third-party feed text that then goes to Pango. A 300th-character emoji yields a lone surrogate. | Fix: one code-point-safe clamp helper, used by all four. |
| T476 | i18n / UI | Medium | open | S | **[verified]** A holiday provider's raw JSON error string is displayed verbatim as UI text. `holidays.js:353` stores the remote `data.error` (clamped and newline-stripped, but arbitrary vendor text) into `last_error`; `translateHolidayError()` is a lookup with a **passthrough fallback** (`HOLIDAY_ERROR_TEXT[error] \|\| error`), and the result lands in the `.calendar-holiday-reason` label, its accessible name and the tooltip (`5.4/calendar.js:100-107,634-643`). | A French user with holidays on for France sees "Juillet ⚠" with an untranslated English vendor sentence pinned under it, in an otherwise French UI. A broken or hostile endpoint puts arbitrary text there. Fix: display `HOLIDAY_ERRORS.INVALID_RESPONSE`; keep the raw string for `global.logError` only. |
| T477 | UI / accessibility | Medium | open | M | With world clocks off, the weather error text is reachable only by mouse hover. `5.4/appletPanelStatus.js:596-608` emits `weatherStatusLine()` ("Set a weather location", "⚠ Weather service unavailable", "Weather: loading…") **only** into `view.setTooltip()`, and Cinnamon's `Tooltip.show()` bails when `mousePosition` is unset — which is written only from `enter-event`/`motion-event` (documented at `calendar.js:514-522`). The holiday half got a real actor for exactly this reason (`.calendar-holiday-reason`); the weather half did not. | A user enables weather, leaves the location empty and unchecks "Show world clocks in the calendar menu": the panel shows a bare ⚠, and opening the menu with `<Super>c` shows no weather row, no clock rows and no tooltip — nothing anywhere tells him to set a location. (Screen-reader users are covered, via `_announce()`.) Fix: a status actor in the popup, like the holiday reason. |
| T478 | UI / docs | Medium | open | M | The per-city temperature is never drawn in the popup — only in the accessible name and the mouse tooltip. `5.4/worldclocks.js:192-219` writes `entry.time` to the visible `St.Label` and puts `entry.weather` **only** into `set_accessible_name()`; the grid has two columns and no temperature cell. Yet `settings-schema.json:151` promises units "used … for the temperature beside each world clock" and `:136` tells vertical-panel users the readout "shows only in the tooltip and the popup". | A user opening the menu by hotkey or on a touchscreen sees bare times; the temperatures the settings dialog promised exist only under a mouse. The comment at `worldclocks.js:206-211` claims to have fixed "reached neither a keyboard user nor a screen reader" — it fixed the screen-reader half. Fix: a third grid column (M), or correct the two schema strings (S). |
| T479 | accessibility | Medium | open | S | A finished event is distinguished from an upcoming one by **colour alone**, and their accessible names are identical. `5.4/eventView.js:701-711` sets `calendar-event-time-past` vs `calendar-event-time-future`; `_applyUpcomingState` writes a countdown only when the event starts on the selected day, so a past event and an upcoming-but-not-starting-today event both end with an **empty** countdown. `_announce()` builds the row name as `time — summary — countdown`, byte-identical for the two. | A red/green colour-blind user cannot tell the 09:00 stand-up that finished from the 09:00 slot of a multi-day booking that has not begun; a screen-reader user gets nothing at all. Fix: append a state word (`_("Ended")`) to the accessible name and add a non-colour cue. |
| T480 | i18n / RTL | Medium | open | S | `eventFormat.js:120` `ARROW_SEPARATOR = "  ►  "` (U+25BA) is hardcoded, absent from the `.pot`, and **not** in Unicode's `Bidi_Mirrored` set, so it does not flip under RTL. | In Arabic the row lays out right-to-left, so the end time sits left and the start time right — with the arrow still pointing right, i.e. from the end of the range back to its start. Every event time range reads backwards. Fix: make it a msgid, or use U+2192 (which mirrors), or pick per `St.TextDirection`. |
| T481 | testing | Medium | open | S | **[verified]** Six size/time constants are asserted **against themselves**, so each can be raised 100× with the suite green. Mutations that survived: `MAX_MEMOIZED_MONTHS` 32→3200 (`holidays.test.js:669` loops `MAX * 3` and asserts `size <= MAX`); `MAX_HOLIDAYS_PER_YEAR` 1000→100000 (`:3219` builds arrays of `MAX` and `MAX + 1` — a million-row payload would parse and expand on the compositor thread); `MAX_EVENT_SUMMARY_LENGTH` 300→30000; `LABEL_SUFFIX_MAX_LENGTH` 48→4800; `WEATHER_DEBOUNCE_MS` 750→75000 (every debounce test injects its own value, so the shipped default is only ever seen by the `gjs_import` double); and `MAX_GEOCODE_CACHE_ENTRIES` 16→1600, which appears in **no assertion at all** — its only occurrence in the tests is the `gjs_import` double re-declaring it. | Fix: assert the literal, or assert observable behaviour at a hard-coded boundary. |
| T482 | testing | Medium | open | S | **[verified]** The EDS reconnect clamp and both backoff ceilings in `eventsManager.js` are unasserted. Mutations that survived: `:175` `Math.min(this._server_retry_attempts + 1, 8)` → unclamped; `:38` `SERVER_RETRY_MAX_SECONDS` 300→30000; `:42` `FETCH_RETRY_MAX_SECONDS` 120→12000; `:692` `>= FETCH_RETRY_MAX_ATTEMPTS` → `>` (6 attempts instead of 5 — the constant's *value* is pinned, its *boundary* is not). The same clamp in `weatherScheduler.js:121` **is** killed: the two copies are tested asymmetrically. | A >8-hour retry gap ships green. Fix: assert the delays the scheduler actually arms. |
| T483 | testing | Medium | open | S | **[verified]** `holidayCache.js:553` `persist()`'s cleared-place guard is unasserted. **Mutation:** `if (!this.country) { return; }` → `if (false)` → **survived**, despite `holidays.test.js:293` being titled "an inflight fetch landing after clearPlace does not persist" — that test asserts through a `HolidayService` double, not through `HolidayCache.persist`. Without the guard a late fetch writes the payload under a literal `"null"` country key. | Fix: drive the real cache. |
| T484 | testing / gates | Medium | open | M | **[verified]** The Python gate measures **lines only** — no branch, no function coverage (the JS half is held to 98/90/100). A one-sided `if` is free. Demonstrated: `timezone_data.py` reports 100.00 % lines while `any_timezone_data()` (`:182`) survives mutation — `return self.has_timezone_data or bool(self.fallback_timezone_map)` → `return self.has_timezone_data` — and that predicate is what `settings_widgets_common.py:781` uses to decide whether to warn the user that timezone validation is degraded, i.e. exactly the path a user without pytz gets. | Fix: add branch coverage to `test/helpers/coverage.py` (it already walks bytecode with `dis`). |
| T485 | testing / DST | Medium | open | M | **[verified]** `5.4/calendar.js:1060` — the DST noon-anchor on the weekday-heading loop is unasserted. **Mutation:** `iter.setHours(12)` → `iter.setHours(0)` → **survived**. The identical anchor at `:154` (`_beginDate`) *is* killed. Anchoring at midnight and stepping by `MSECS_IN_DAY` lands on 23:00 of the same day across a fall-back transition, so `iter.getDay()` repeats a weekday and one column heading is duplicated while another is missing. | Fix: a test that fixes a DST-transition week (the `Intl`-backed pattern from `worldclocks.test.js`, T374, applies). |
| T486 | architecture / duplication | Medium | open | S | The lazy-HTTP-session lifecycle is implemented three times, and the extracted abstraction is used by neither weather provider: `weather.js:166-175` + the guarded abort at `:186`, `cityWeather.js:93-102` + `:165` (the same body verbatim), and `holidays.js:74-95` (`HolidaySession` — lazy create with the same two timeouts, the same guarded abort). | A `Gio.Cancellable`, a connection cap, a proxy setting or an abort-on-suspend must be added in three places, two of which are the ones that fan out to 8 cities. Fix: both weather providers take a `HolidaySession`. |
| T488 | architecture / complexity | Medium | open | M | **[verified]** Production cognitive complexity is **ungated** — `test/complexity.test.js:16` scopes the walker to `TEST_DIR = __dirname`, so the limit of 15 the tests are held to does not apply to the code. Measured with the project's own walker: `ioUtils.js:245` `httpGetJson` = **42**, `localeUtils.js:323` `_requestInfo` = **32**, `5.4/calendar.js:654` `CalendarHolidayAnnotator.annotate` = **20**, `ioUtils.js:48` `readJsonFileAsync` = **17**, `holidays.js:387` `HolidayService.retrieveForYear` = **16**. | `httpGetJson` alone carries the redirect-downgrade cancel, the declared-length cap, the streaming cap, the JSON-scalar guard and two Soup code paths in one body with three nested callbacks — precisely the function where an added branch silently bypasses a size or scheme guard. Fix: extract, and point the existing gate at `files/` too. |
| T490 | architecture / SRP | Medium | open | M | `localeUtils.js` (465 lines) holds six unrelated jobs: gettext binding and install-path resolution (`:99-150`), the `locale -k` subprocess state machine with process-global mutable maps and module-level timers (`:34-97`, `:323-421`), strftime date-format constants (`:193-205`), `MSECS_IN_DAY` (`:191`), the pure calendar date math `monthWindowStartOffset` (`:432`), and accessible-name template helpers (`:157-189`). Every module that wants `translate()` — `worldclockData`, `5.4/calendar`, `5.4/eventView`, `5.4/worldclocks` — links the subprocess machinery, its `_pendingTimers` set and its cross-instance `_consumers` counter. | That process-wide mutable state is the state that already produced the multi-instance teardown bugs the comments describe. Fix: split the i18n runtime from the date/format constants and the pure date math. |
| T492 | dead code | Medium | open | S | **[verified]** `ioUtils.js:328` — the `send_and_read_async` fallback is unreachable at runtime; it exists only for test doubles. The guard above it is `typeof session.send_async === "function"` (`:309`), and every Soup that has `send_and_read_async` (Soup 3 — the only Soup the supported 5.4–6.4 range ships) also has `send_async`, so the guard is always true. Its own comment ("stays for an older Soup") is false in the other direction too: the branch calls `Soup.MessagePriority`, `send_and_read_finish` and `Soup.Message.new`, all Soup-3-only. | Verified: `grep -rn send_and_read_async files test` → one production line and only test doubles (`utils.test.js:735…`, `weather.test.js:49`), all of which deliberately omit `send_async`. A dead branch kept alive by mocks — and it duplicates `_declaredTooLarge`'s pre-read guard. Fix: delete the branch and the doubles' reliance on it. |
| T493 | dead code / legacy | Medium | open | S | **[verified]** `5.4/appletLifecycle.js:197-208` — the entire UPower resume path is dead: a `UPowerGlib.Client()` is constructed per applet instance and two handlers connected (`notify-resume`, then `notify::resume` in the catch), neither of which can fire. The code's own next comment says so ("the UPower path above is dead on a current stack: `notify-resume` is gone and `notify::resume` names a property that no longer exists"), and the logind `PrepareForSleep` subscription immediately below exists *because* of it. | `onResume` is reachable only through logind. The client, the try/catch, `_up_client`, `_up_resume_signal_id` and its teardown step (`:271-276`) are all vestigial. Fix: delete. |
| T494 | dead code / wiring | Medium | open | M | **[verified]** `5.4/worldclocks.js:148-190` — the `limit` parameter and the `includeBuiltin === false` path of `getClockEntries`/`_selectClocks` are unreachable in production, including the early return at `:171`. The only production chain is `updateClockAndDate` → `getClockEntries(null, true)` (`appletPanelStatus.js:730`) → `PanelView.getClockEntries` (`:267`, which maps `null` → `undefined`) → `Worldclocks.getClockEntries(undefined, true)`. User clocks are already capped by `selectUserClocks`, so `userClockCount >= limit` cannot trip either. | Verified: the only callers passing a limit or `false` are tests (`worldclocks.test.js:595,601,649,734`, `applet.test.js:2671-2686`). Three layers of pass-through — presenter, view, widget — exist to carry two arguments nobody sends. Fix: drop the parameters. |
| T496 | packaging | Medium | open | M | **[verified]** The repo root doubles as the Spices applet root and carries the whole dev harness. The Spices layout for `UUID/` is `info.json`, `screenshot.png`, optional `README.md` and `files/`; this root also tracks `package.json`, `package-lock.json`, `eslint.config.mjs`, `test/`, `.github/`, `agent-instructions/` (21 files), `AGENTS.md`, `CLAUDE.md`, `TODO.md`, `calendar.png`. A cinnamon-spices-applets PR copies `UUID/` wholesale, so all of it lands in the catalogue repo and a reviewer bounces it. | (`files/` itself is clean — it contains only the UUID dir, which is the one hard structural rule.) Fix: decide the submission story — a packaging script that assembles the Spices subset, or move the harness where the PR excludes it. |
| T497 | licensing | Medium | open | S | **[verified]** No licence text and no per-file notices in what actually gets installed. `LICENSE` (GPL-2.0-or-later, consistent with `metadata.json:7` and `package.json:5`) sits at the repo root; Cinnamon installs only `files/chronos@geraldo-netto/`, where there is no `LICENSE` — and **0 of the 40 shipped `.js`/`.py` files carry a copyright or licence header**. | Verified: `head -5` over every shipped source; zero matches for copyright/SPDX/GPL. A GPL derived work of three authors ships with no licence and no notices. Fix: `files/chronos@geraldo-netto/LICENSE` plus an SPDX header per file. |
| T498 | packaging / docs | Medium | open | S | **[verified]** `screenshot.png` is inherited from `calendar@ccprog` and does not depict Chronos: 640×420, German UI, dated *Donnerstag, 30. Juni 2022*, showing a single "Detroit" clock row — not the UTC/local/cities weather table that `README.md:235-243` calls the headline feature. | Provenance *is* credited in the README; the problem is that the Spices listing and the README advertise a UI the screenshot does not show. Fix: take a current screenshot. |
| T499 | release | Medium | open | M | **[verified]** No release story. Zero git tags, no changelog, version pinned at `0.0.1` in both `metadata.json:6` and `package.json:3`, and `ci.yml` has only a `gates` job — no tag, release or packaging job. The Spices site offers an update to installed users **only** when `metadata.json`'s version rises, and nothing enforces or records that bump (`schema_static.test.js:21-22` pins metadata/package *agreement*, not that either increments). | Fix: a version-bump + changelog + tag flow, gated in CI. |
| T500 | CI | Medium | open | M | **[verified]** CI gates the code and nothing else. `.github/workflows/ci.yml` is honest about what it runs (no `continue-on-error`, no dead matrix, `lint:py` genuinely exits 1) — but every packaging and i18n defect in this ledger ships green: nothing rejects a symlinked `icon.png` (T439), runs `msgfmt -c` over the catalogs, checks the `.pot` is current (T523), or validates the Spices layout (T496). It also tests only Node 22 while `package.json:31` declares `>=20`, so the supported floor is never exercised. | Fix: a packaging job, and a Node 20/22 matrix. |

### Low

| ID | Category | Severity | Status | Effort | Description | Notes |
|----|----------|----------|--------|--------|-------------|-------|
| T501 | UI | Low | open | S | `5.4/appletPanelStatus.js:24,36,39-40,44` use `%l` — the **space-padded** 12-hour field. Horizontal 12h renders "13 Jul  2:52 PM" (double space) for any hour before 10; vertical 12h (`"%l%n%M"`) gets a leading space that visibly shifts the digits off-centre. The world-clock rows escape it only because `worldclocks.js:43` calls `.trim()`. | Fix: `%-l`. |
| T502 | accessibility | Low | open | M | Hardcoded pixel caps and icon size ignore the desktop's text-scaling factor: `5.4/stylesheet.css:30,74,87` (`max-width: 180px/220px/300px`) and `5.4/eventView.js:406` (`icon_size: 48`). St scales `px` by the HiDPI *ui* scale but **not** by `org.gnome.desktop.interface text-scaling-factor`. | At 1.5× text scaling, city names ellipsize a third earlier (recoverable only via a mouse-only tooltip), the holiday reason wraps to four lines, and the 48px icon sits beside text half again as large. The stylesheet already uses `em` correctly at `:34,38,72`. Fix: `em` throughout. |
| T503 | i18n | Low | open | S | The holiday status line is assembled from fragments: `5.4/calendar.js:620` `_("Holiday data: %s").format(_("loading…"))` slots a standalone lowercase msgid into a carrier sentence, and `:634-637` glues the provider name on with literal ASCII parentheses that never reach a translator. | The applet does this correctly elsewhere — `joinPhrases()`'s `_("%s — %s")` and `_("%s Last known reading")` are single msgids for exactly this reason. Fix: whole-sentence msgids. |
| T504 | UI | Low | open | S | A world clock whose timezone names no city (`Etc/GMT+3`, offset-only zones) gets a permanently blank temperature with no explanation: `timezoneCityName()` returns `""` (`worldclockData.js:63-80`), `_coerceCity()` silently drops it (`cityWeather.js:304-319`), and `_cityWeatherCells()` returns `["", ""]`. | Indistinguishable from a fetch that is pending or that failed. Fix: mark it as not-applicable rather than empty. |
| T505 | UI | Low | open | M | The panel label is uncapped for a long custom format. Only the *weather suffix* is capped (`LABEL_SUFFIX_MAX_LENGTH = 48`); `label_string` from the user's `custom-format` goes straight to `set_applet_label()` with no clamp, and the stylesheet sets no `max-width` or ellipsize. | `%A, %-d %B %Y — %H:%M:%S %Z` on a 1366×768 laptop consumes ~40 % of the panel and pushes the window list off it, with nothing to indicate the applet did it. Fix: clamp/ellipsize the label. |
| T506 | docs | Low | open | S | `README.md:236-237` documents a tooltip footer that no longer exists ("with the answering service named at the foot"). The code comment at `5.4/appletPanelStatus.js:604-606` records that the footer was removed and says it "is still credited … in the README" — the README was not updated. | Fix: correct the sentence. |
| T507 | testing | Low | open | S | **[verified]** Two more unpinned values. `weather.js:178`: deleting `this._request_generation++;` from `stop()` survives — the two tests that call `stop()` assert only that timers were removed, never that in-flight replies are discarded (mitigating: `stop()`'s only caller is `destroy()`, which is separately guarded, so this is nearly an equivalent mutant). `ioUtils.js:204`: `READ_CHUNK_BYTES` 64 KiB → `1` survives — correctness-neutral (the cap is enforced on the running total, which *is* tested), but a 1-byte chunk size would make every response pathologically slow and nothing would say so. | Fix: assert both. |
| T508 | testing | Low | open | M | `test/worldclocks.test.js:99-146` — the `GLib.DateTime` double re-implements strftime and **passes unknown `%X` tokens through as themselves**. `STRFTIME_FIELDS` covers ten tokens; production world clocks use only `%H:%M` today, so the double is currently faithful — but a format string that gains a token outside the table would "format" to itself and no assertion could notice. The padding rule is likewise a hand-rolled re-implementation that can drift from GLib's. | Fix: throw on an unknown token. |
| T509 | testing / parity | Low | open | M | The world-clock rules are implemented once per language, and only two of the three have a parity guard. `TZ_NO_REGION` and `timezoneCityName`/`local_city_name` are pinned across JS and Python (`worldclocks.test.js:436`, `schema_static.test.js:363`); the **built-in/reserved zone set is not** — JS `builtInTimezoneKeys` (`worldclockData.js:92-103`) vs Python `builtin_timezones` (`timezone_data.py:107`) and `RESERVED_TIMEZONES` (`:27`). | Adding or changing a built-in clock row is two edits in two languages, and nothing fails if one is forgotten: the drift shows up as a clock the dialog happily saves and the applet silently drops. Fix: a third parity test. |
| T510 | duplication | Low | open | S | `5.4/appletPanelStatus.js:79` (`describeWeather`) and `:350-353` (`_conditionWords`) each write the same condition-glyph → English word → translated word lookup, and the `⚠ + translateWeatherError(...)` phrase is built twice (`:473-474`, `:619`). | The two feed the tooltip and the screen-reader name respectively, so they can disagree about what the sky is. Fix: one helper. |
| T455 | dead code | Low | open | S | **[verified]** `cityWeather.js:34` `CITY_STALE_AFTER_SECONDS` is computed at load and exported, but production reads none of it — `CityWeatherProvider` derives its own threshold from the injected period at `:54`. Only `cityWeather.test.js` reads it. | Re-confirmed independently by the 2026-07-13 wiring scan. Fix: delete the constant and its `module.exports` entry. |
| T512 | dead code | Low | open | S | **[verified]** `weatherFormat.js:105` `formatReading(icon, celsius, units)` has no production caller: it is not re-exported by the `weather.js` barrel (`:32-64` lists every other `WeatherFormat` symbol), so no GJS consumer can reach it, and the only call sites are `weather.test.js:29` (which uses it to re-derive the panel string) and a stub in `gjs_import.test.js:181`. The panel renders via `formatTemperature` + `WEATHER_CONDITIONS`. | A test-only helper living in production. Fix: delete, or move it into the test. |
| T513 | dead code | Low | open | S | **[verified]** `holidayCache.js:446-450` `_syncIndex()` can never see a mismatch in production: `this.data` is written only by `setData` (which rebuilds the index) and `addUnique` (which updates `_indexedDataLength` in the same block). The only writers that break the invariant are tests pushing straight onto the array (`holidays.test.js:1005`, `:1228`) — a production resync branch that exists to make fixtures work. | Fix: delete the branch and have the fixtures go through the API. |
| T514 | dead code | Low | open | S | **[verified]** `settings_widgets_common.py:495-515` `ListEditComboBox`, and the `if 'options' in params` branch of `list_edit_factory` (`:560-563`) that is its only constructor, are unreachable from the shipped schema: `ClockDialogBuilder.build_content` builds its columns from `worldclocks.columns`, which are exactly two `{"type": "string"}` columns with no `options`, and no other caller exists. Exercised only by `test_settings_widgets.py:680-718`. | Fix: delete, or ship a schema that uses it. |
| T515 | dead code | Low | open | S | **[verified]** `5.4/appletLifecycle.js:123` `initProviders()` returns `holidayRegions` and nobody reads it (`applet.js:103-108` destructures five other keys; `grep -rn holidayRegions files test` finds no consumer). And `holidays.js:563` `HolidayProviderFacade`'s default argument `provider = new HolidayService()` re-creates exactly the self-assembling graph that `createHolidayProvider` (`:543-560`) was written to replace — unreachable, since the only production construction site always passes a provider. | Fix: drop both. |
| T518 | legacy | Low | open | S | **[verified]** Compat shims for versions this applet cannot run on. `localeUtils.js:271` `typeof output === "string" ? output : decodeUtf8(output)` — `communicate_utf8_finish()` returns a string in every GJS Cinnamon 5.4+ ships, which `ioUtils.js:32-34` states explicitly where it *deletes* the equivalent `imports.byteArray` shim. `5.4/appletLifecycle.js:217` `Gio.DBusSignalFlags ? Gio.DBusSignalFlags.NONE : 0` guards against a GIO that exists in no supported release. `ioUtils.js:145,193` — `message.request_headers \|\| message.get_request_headers()` and the `get_content_length` fallback, whose own comment says the second arm is there so "plain test doubles keep working". | Production fallbacks whose only consumer is a test double. Fix: delete, and fix the doubles. |
| T520 | security / bounds | Low | open | S | **[verified]** `holidayCache.js:159-161` — holiday rows are type-checked per row on cache load but never **counted**, while the network path bounds the same data twice (`MAX_HOLIDAYS_PER_YEAR = 1000`, `MAX_EXPANDED_HOLIDAY_ROWS = 4000`) — and the disk path is the one the comment at `:40` says must not be trusted. | Verified: a Node harness called `_country()` with 200,000 valid-shaped rows — **all 200,000 accepted**. Anything running as the user can plant them; the 4 MiB file cap bounds the outcome to a startup stall on the compositor thread, not a hang, and the attacker already has the user's UID — hence Low. Fix: slice to `MAX_EXPANDED_HOLIDAY_ROWS`, mirroring `expandData()`. |
| T521 | memory | Low | open | S | `holidayCache.js:316` — `this._pending[country]` is set and never deleted. `MAX_CACHED_COUNTRIES = 4` bounds the *file*, but the in-memory map retains a full year-of-holidays blob for every country the session ever selected (up to `SUPPORTED_COUNTRIES.length` = 58 while a user browses the settings dialog), and every subsequent `_flush` re-merges all of them into the freshly-read file before pruning. | Bounded, and the on-disk file stays correct, but it defeats the bound `MAX_CACHED_COUNTRIES` was added to enforce. Fix: delete the entries that survived the prune in `_flush`'s write callback. |
| T522 | CI / supply chain | Low | open | S | **[verified]** Three hardening gaps in `.github/workflows/ci.yml`: no `permissions:` block anywhere (`grep -c permissions` → `0`), so `GITHUB_TOKEN` inherits the repo default, which on many repos is `contents: write` — and the job runs `npm ci` + `npm test`, i.e. repository and dependency code; `:32` installs pyflakes with `--upgrade` and no pin or hash, while the JS side is lockfile-pinned; `:16,20,25` pin `actions/checkout@v4`, `setup-node@v4`, `setup-python@v5` to mutable major tags. | Fork PRs are safe (GitHub forces a read-only token), which is what keeps this Low. Fix: `permissions: contents: read`, pin pyflakes, pin the actions to SHAs. |
| T523 | i18n / packaging | Low | open | S | **[verified]** `po/chronos@geraldo-netto.pot` has stale source references: regenerating with `po/makepot` changes ~45 `#:` lines (e.g. `5.4/appletPanelStatus.js:29` → `:48`). The **msgid set is identical** — no string is missing, and `makepot` itself is correct. | Verified: ran `./po/makepot` on a scratch copy and diffed, ignoring `POT-Creation-Date`. Cosmetic, but it means the catalog is not regenerated in lockstep and the Spices translation tooling will churn. Fix: regenerate, and add the check to CI (T500). |
| T524 | i18n | Low | open | M | **[verified]** All 15 catalogs carry the same 2 **fuzzy** entries — `"City or town (e.g. Lisbon)"` and `"Type a country name"` — which gettext ignores, so both render in English in every locale. (The 3 untranslated entries are T465's two date formats plus one more.) | Verified: `msgfmt -c --statistics` and `msgattrib --fuzzy` over all 15 (each: 326 translated / 2 fuzzy / 3 untranslated). Fix: resolve the fuzzies. |
| T525 | testing / gates | Low | open | L | **[verified]** The 98 % line gate's slack is spent entirely on guard and teardown paths. The lines the gate itself reports as uncovered while passing are consistently `if (this._calendar_server === null) return;` / `if (this._destroyed) return;` / `if style is None: return` — i.e. the error handling: `eventsManager.js:234,244,678,685,711`; `holidays.js:207,492,503`; `settings_widgets_common.py:50,605,897`. On an 810-line file a 98 % gate buys ~16 free lines. | The gate is honest about the number; the number is just loose. Fix (if any): raise the floor, or require the guard paths specifically. |
| T343 | packaging | Low | open | S | **[verified]** `calendar.png` is a tracked 48×48 orphan at the repo root; nothing references it (grep across js/json/md/py/css is empty) and it is not the icon (different md5 from `files/chronos@geraldo-netto/icon.png`). It would ship in a Spices submission as dead weight. | Inherited from `calendar@ccprog`. Fix: delete. |

## Suggested order

1. **T439** — the icon does not survive delivery, so the applet cannot be submitted to Spices. Small.
2. **T463, T473, T474** — the memory and network waste, in that order (the leak compounds the cache growth).
3. **T468–T471, T481–T485** — the mutation survivors. Each is a test that cannot fail, and one of them (T470) is also a real validation hole.
4. **T465, T466** — the i18n data and the hardcoded date order: every locale is affected, and both are Small.
5. **T464** — the forward-compat landmine, before the Cinnamon after 6.4 ships.
6. Everything else, severity order. The dead-code cluster (T492–T494, T512–T519) is one sitting.

## Clean categories

Verified with no findings on the 2026-07-13 rescan (each re-derived from the source, not inherited from the old ledger):

- **Secrets, TLS, injection.** No credential in the tree (all seven services are keyless). Every endpoint `https://`, and `ioUtils.js:347` cancels a request *before* a redirect to plain `http` goes out, because the query string carries the user's location. No `set_markup`/`use_markup` anywhere; Cinnamon's own `Tooltip.set_text` calls `set_use_markup(false)`. Both inline-`style` sinks pass through `styleUtils.safeCssColor` — **fuzzed with 11 payloads** (`red; background-image: url(…)`, `)}*{background:red}`, `expression(alert(1))`, newline smuggling): every one returned `"transparent"`. Every subprocess is argv-form; `launchUuid` writes `"--uuid=" + uuid` as one argument so a feed-supplied UID beginning with `-` cannot be parsed as an option. No `eval`/`Function`/dynamic import; no SQL.
- **Untrusted-input bounds.** Body size capped twice (declared `Content-Length` refused before the read; the stream aborted at 4 MiB, which closes the chunked-transfer hole). Non-object JSON coerced to `null`. Every provider normalizer checks `Number.isFinite`/`Array.isArray` before use. Prototype pollution **tested**: a cache file containing `{"__proto__":{…}}` leaves `Object.prototype` untouched; the event list already uses `Object.create(null)` for feed-supplied UIDs. (The one gap is the missing row *count* on the disk path — T520.)
- **Cache file safety.** Fixed filename under `GLib.get_user_cache_dir()`; no user or remote input in the path; dir `0700`; `REPLACE_DESTINATION` unlinks-and-recreates rather than writing *through* a planted symlink; etag-guarded merge with a bounded retry loop; every row re-validated on load, including the future-stamp check.
- **Consent and egress.** Both networked features are off by default (`show-weather: false`, `country: "none"`), unknown countries re-asserted to `none` at startup. The world-clock **display name never leaves the machine**: only the IANA-derived city is geocoded, so a clock labelled "Mom's place" stays local. Calendar event summaries and hostnames never leave. Every log line is stripped of its query string (`_urlForLog`). README's egress list is an **exact** match for the 8 hosts in the source.
- **Timers, signals and teardown *of resources*.** Drove both weather providers through `destroy()` with requests in flight and retries armed, under a fake GLib loop tracking every id: zero leftover sources, zero `source_remove` on an unknown id, zero late callbacks. Generation counters abandon superseded work. Cinnamon's `XletSettingsBase.finalize()` covers the settings signals. (What teardown does **not** do is release the *data* — T463.)
- **Backoff and retry.** 24 h of simulated total failure: 54 attempts, no runaway; `backoffDelay` caps at the refresh period and adds jitter on top of the cap; `MAX_RETRY_ATTEMPTS = 8`; every HTTP path sets `timeout` + `idle_timeout` = 30 s. The holiday state machine never wedges: 20 rapid `_update()`s after a total failure issue 0 extra fetches.
- **Main-loop hygiene.** No synchronous I/O anywhere (one `query_exists()` stat aside). No sub-minute periodic timer: `notify::clock` fires only when the rendered string changes (0 emissions in 10 s with `%H:%M`). The idle tick is **1.3 µs, zero allocations**. The 42 grid cells are built once and mutated; dots are diffed by key; every label write has an equality guard. Heap delta after 200k `_update()` calls: **98 KiB** (flat). No `Intl.DateTimeFormat` in any loop.
- **Settings-schema → runtime parity.** All 17 keys bound and acted on; all 58 schema countries present in `SUPPORTED_COUNTRIES` and `COUNTRY_TO_ISO2` with no extras either way; `has_region`'s 10 entries match `REGION_TO_SUBDIVISION`; every region option has a subdivision mapping and there are no orphan table entries; `MAX_CLOCKS = 8` agrees across `worldclockData.js`, `cityWeather.js` and `settings_widgets_common.py`; every `dependency` in the schema is honoured by Cinnamon for custom widgets too.
- **Keyboard navigation of the day grid.** Roving focus (one tab stop), arrows/PageUp/PageDown/Home, RTL mirroring of Left/Right, focus moved into the grid on menu open, focus rings for every focusable actor. Genuinely good.
- **The gates themselves.** The JS coverage gate holds every file to 98/90/100 *and* globs the shipped tree from disk to fail any `.js` no test loads — a hole was looked for and not found. The eslint gate lints real text through the real config to prove `no-unreachable`/`no-dupe-keys`/`no-cond-assign` fire. `lint:py` genuinely exits 1 when pyflakes is missing. The `.pot`'s **msgid set is complete** (zero missing across all JS, the schema and both Python files). No `Lang.bind`, `imports.byteArray`, `toLocaleFormat`, `Soup.SessionAsync` or `queue_message` anywhere.
- **Not applicable**: database / migrations, multi-tenancy, Electron, Rust, ML / retrieval / RAG, vectorization, CLI surface, SQL injection, CORS/CSRF, prompt injection.

## Rejected

| id | finding | why rejected |
|----|---------|--------------|
| R01 | `dtEquals` duplicated between `eventData.js` and `eventFormat.js`. | Deliberate boundary: `eventFormat.js` stays free of GJS imports so it is Node-pure. Centralizing re-couples the modules for a one-line function. (Re-raised by the 2026-07-13 architecture scan; re-rejected.) |
| R05 | Barrel re-exports as the GJS `var` bindings importers need. | The re-exports **are** the import mechanism; tests reach the modules through them. (Does **not** cover T455/T512, which are genuinely unread.) |
| R06 | `eventView.js:70`: commented-out `Util.trySpawn(["gnome-calendar"])`. | Deliberate documented switch with a maintainer note directly above it. |
| R08 | No watchdog/metrics/circuit-breakers/health surface. | For a panel applet these are reasonable omissions; the three-provider failover with last-success ordering plus a per-year freshness gate is a coherent substitute. |
| R09 | Per-calendar colour strip conveys calendar identity by colour alone. | Not actionable here: `cinnamon-calendar-server` sends the calendar's colour and never its display name, so there is no name to announce. The strip is `Atk.Role.SEPARATOR` so a screen reader skips it. Needs an upstream change. |
| R10 | Panel label written with no compare-before-assign. | **[verified]** The tick is not 1 Hz: `WallClock` emits `notify::clock` only when the rendered string changes (0 emissions in 10 s with `%H:%M`), so the handler never sees the same string twice. |
| R11 | `validDateParts`' **year** and **day** integer guards look unasserted (both mutants survive). | **Equivalent mutants.** An exhaustive witness search over 15 hostile value types found no distinguishing input: `d.getFullYear() === parts.year` and `d.getDate() === parts.day` compare strictly against the raw value, so the roundtrip check subsumes them. Only the **month** guard, whose comparison coerces (`"5" - 1`), is load-bearing — that one is T470. |
| R12 | The suite depends on the host timezone. | Ran the full JS suite under `TZ=Pacific/Auckland`, `TZ=America/Sao_Paulo`, `TZ=UTC`: 670/670 in all three. `test/helpers/clock.js` freezes the wall clock and the tests pin `LOCAL_TIMEZONE` explicitly. |
| R13 | `metadata.json` is missing `icon` / `author`. | Cinnamon's `requiredProperties` for an applet are `uuid`, `name`, `description`; `author` belongs in `info.json` per the Spices spec, and is there. `max-instances: -1` is valid. |
| R14 | The `5.4/` shims are indirection; the root modules "depend on files that only exist in the parent". | Intended and correct on every declared version: `appletManager.js:61` roots `imports.ui.appletManager.applets[uuid]` at the *applets folder*, so `[uuid]` is the parent dir. The seven-line shims work, and `gjs_import.test.js` pins them. (The forward-compat risk is T464, which is about `require`, not the shims.) |
| R15 | `settings_widgets_common.py` at 991 lines is a god module. | Many concerns, but they are all "the settings dialog"; the gi-free half is already extracted into `timezone_data.py`, and no change was identified that becomes dangerous because of the co-location. |

## Recorded decisions

| id | decision |
|----|----------|
| D01 | Keep the six third-party endpoints rather than a bundled data set or a single vendor. A bundled table goes stale and covers fewer countries; a single provider breaks whenever it is down — the fallback chain exists because the primary is a hobbyist service. Risk accepted with mitigations (opt-in + default-off, size caps, bounded spans, shape checks in and out of cache, colour filtering, no URL logging). |
| D02 | **Keep the widened export surface.** ~80 exported names have no cross-module consumer — used inside their own file and otherwise only by `test/`. The applet runs inside a compositor where a bug costs the whole session; testing only through the composition root would be slow and flaky. **This does not license the inverse:** a production field, branch or fallback that exists only to fit a mock's shape is the code bending to the test, which D02 never covered — see T492, T513, T518. |
| D03 | **`author` is the display name "Geraldo Netto", not the GitHub handle.** `info.json`/`package.json` use "Geraldo Netto"; `schema_static.test.js` asserts it. The Spices convention prefers the handle, but the display name is the maintainer's explicit choice. |

## Resolved merge choices

Decisions from the calendar@ccprog / Simon Wiles merge. Kept so future audits do not re-pick them.

| id | resolution |
|----|------------|
| G01 | Kept the `calendar@ccprog` world-clock settings workflow (Cinnamon-native settings, up to 8 user clocks plus built-in UTC/local, region/city selection, IANA/city entry, timezone validation, inherited coverage). Simon Wiles' separate GTK editor remains unported. |
| G02 | Kept `calendar@ccprog` locale-aware weekend behaviour (locale-derived work weeks plus a configurable one- or two-day weekend length). |
| G03 | Kept the copied `calendar@ccprog` visual assets (icon, screenshot, calendar image) as the complete existing set for the merged applet. **Superseded in part by T498**: the screenshot depicts ccprog's 2022 UI, not Chronos, and must be retaken. |
