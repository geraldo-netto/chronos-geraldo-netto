# cinnamon-chronos TODO

Audit ledger for this applet. Latest full-source rescan on 2026-07-18 against every `agent-instructions/` review category **including the special ones — wiring gaps, unused functions/methods, legacy/deprecation, and variable/function scope** — run as six parallel domain audits (security/input, reliability/concurrency, architecture/wiring/dead code, performance/complexity/caching, tests/CI/release/dependencies, docs/i18n/UX/a11y/config). The scan covered all tracked source, tests, CI, documentation, packaging scripts, catalogs and assets. It excluded generated or cached material: `.git/`, `node_modules/`, `dist/`, `build/`, `coverage/`, `.cache/`, Python/tool caches, bytecode and compiled catalogs. Candidates were reproduced where possible, checked against the current source and deduplicated against this ledger and `git log --oneline` before inclusion; commits landed since the previous 2026-07-16 full pass (caldays fallback, cache-snapshot invalidation, i18n fuzzy gate, release handoff, CI pinning, icon replacement) received extra scrutiny. Product-strategy categories (OKR, PDCA, product analytics, product-market fit, discovery & validation) remain not applicable to a personal panel applet.

**Findings verified by running or mutating the code are marked [verified]**, each naming the experiment. Live provider responses or policies were consulted only where the current external contract was itself under review; source conclusions were checked locally.

Baseline: `npm test` green (748 JS tests, JS coverage per-file 98/90/100; Python 115 tests, 98 %+ lines), `npm run lint` clean, CI runs both on every push and PR. `npm audit --omit=dev` reports zero vulnerabilities. The gates are real — what this pass found is largely what they do not look at.

Open items: 19 (Critical 0, High 0, Medium 6, Low 13).

## Findings

### High

| ID | Category | Severity | Status | Effort | Description | Notes |
|----|----------|----------|--------|--------|-------------|-------|

### Medium

| ID | Category | Severity | Status | Effort | Description | Notes |
|----|----------|----------|--------|--------|-------------|-------|
| T526 | code complexity | Medium | open | M | The complexity gate contradicts the project rule: `test/complexity.test.js:14` sets `MAX_COGNITIVE_COMPLEXITY = 15` and compares with strict `>`, while the rule is "at or below 10" with "15 or higher forbidden" — so two forbidden exactly-15 bodies pass (`5.4/worldclocks.js:185 updateClocks`, per-tick hot path, and `5.4/appletMenuBuilder.js:130 destroy`) and the whole 11–14 band is unenforced: 22 production functions and 9 test bodies sit there (e.g. `eventView.js:317` constructor 14, `weatherProviders.js:229 _tryGeocodeProviders` 14, `appletPanelStatus.js:498 alignTooltipRows` 13, `ioUtils.js:106 writeJsonFileAsync` 13, `weather.js:227 refresh` 13, `eventData.js:278` 13, `localeQuery.js:68` 13). Change the gate to fail at `>= 15` now, refactor the two 15s, then ratchet MAX toward 10 with a shrinking allowlist. | **[verified]** Measured with the project's own walker (`test/helpers/cognitive.js`) over all tracked sources; the two exact-15 bodies pass the current gate. `weatherServiceAdapters.js:250 metNoIcon` (11) is a flat sequential mapping and plausibly exempt under the lookup-table rule. |
| T527 | i18n | Medium | open | S | All 15 `.po` catalogs are stale against the committed template: 2 msgids are missing entirely (the `openstreetmap-attribution` pair, `settings-schema.json:141,143`) and 2 tooltips were reworded after translation (`show-weather`, `country`), so 4 settings-dialog strings — including the privacy disclosure that the selected country is sent to third-party holiday services — render in English in every non-English locale. `msgmerge --update` each catalog against the pot, carry or translate the 4 entries, recompile. | **[verified]** `msgmerge <lang>.po pot \| msgfmt --statistics` reports "330 translated, 2 fuzzy, 2 untranslated" for all 15 catalogs; catalog headers say POT-Creation-Date 2026-07-13 while the committed pot says 2026-07-17 — the pot was regenerated without merging the catalogs, one day after the clean 2026-07-16 audit. Bundle with T541 (another tooltip reword) to avoid merging twice. |
| T528 | i18n | Medium | open | S | The i18n gate has the blind spot that let T527 pass CI: `scripts/check-i18n.mjs` validates each catalog internally (`msgfmt -c`, active-fuzzy rejection) and proves the pot matches the source, but never compares catalogs against the pot — `npm run i18n:check` prints "15 catalogs valid; translation template is current" while every catalog is 4 entries out of date. Add a step that msgmerges each catalog against the committed pot into a temp file and fails on fuzzy or untranslated entries, with a regression case in `test/i18n.test.js`. | `checkI18n()` is per-file `validateCatalog` plus committed-pot-vs-regenerated diff (lines 41–68); no po-vs-pot comparison anywhere. `ec02598`'s fuzzy rejection only sees flags already present in the file, and un-merged catalogs have none. |
| T530 | test coverage | Medium | open | M | The release-gate validators in `scripts/release.mjs` are mutation-live: disabling the version-owner mismatch guard (lines 58–62) or the dated CHANGELOG heading guard (64–67) leaves `test/release.test.js` fully green, because `scripts/*.mjs` sit outside the JS coverage gate (it globs only `files/`) — 79.87 % lines / 64.52 % branches, with semver rejection, changelog link checks, bump input validation and CLI dispatch all uncovered. These guards are the tag-gated release integrity check. Add denied-path tests (mismatched versions, missing dated heading, missing links, empty Unreleased, bad date, non-semver, unknown command) and consider coverage-gating `scripts/*.mjs`. | **[verified]** Mutations `.test(files.changelog) && false` and mismatch-guard→`if (false)` both survive `node --test test/release.test.js` (run in a scratchpad copy; repo untouched). |
| T531 | test coverage | Medium | open | M | `checkI18n()`'s template-freshness check — what `12d21ab` added the CI packaging job to enforce — has zero test coverage (`scripts/check-i18n.mjs` 38.27 % lines; the whole regeneration-and-comparison flow, lines 33–74, is unexercised), and it is the repo's one fail-open gate: a bug making the comparison always-equal (e.g. an over-broad normalization in `withoutCreationDate`) would ship a stale `.pot` forever while CI stays green. Add an integration test with a fixture tree and a deliberately stale committed pot asserting the "template is stale" rejection, plus a current-template pass case; cover `withoutCreationDate` directly. | **[verified]** Mutation `if (committed !== regenerated)`→`if (false)` survives `node --test test/i18n.test.js`. The `msgattrib` mock was checked against real gettext output — the mocks are faithful; the composed flow is simply never run. Distinct from T528 (missing po-vs-pot check); this row is about testing the pot-vs-source check that already exists. |
| T532 | test coverage | Medium | open | M | Two of the three symlink-confinement guards in `scripts/package-spices.mjs` are mutation-live: the realpath-escape guard (lines 79–82, the only one catching a link chain that lexically stays in-tree but resolves outside) and the symlink-to-untracked-file guard (85–87) can both be disabled with `test/package.test.js` still 3/3 green; the traversal-segment guard in `validateManifestPath` (26–29) also survives. These guards are the boundary keeping host files out of the published Spices artifact that CI uploads. Extend the fixture-repo test with a symlink to an in-tree untracked file, a two-hop realpath escape, and direct `validateManifestPath` unit tests (`..`, `.`, empty, absolute, duplicates, missing required files, directory entry). | **[verified]** All three mutations survive `node --test test/package.test.js` (scratchpad copy). Only the lexical escape guard is covered (`package.test.js:107`). Overall 80.86 % lines / 69.81 % branches. |

### Low

| ID | Category | Severity | Status | Effort | Description | Notes |
|----|----------|----------|--------|--------|-------------|-------|
| T533 | reliability / correctness | Low | open | S | A failed holiday fetch for any year more than ±1 from today is silently invisible: `_acceptYear()` records the outcome and then immediately calls `_status.prune()` (`holidays.js:427`), which deletes every ledger key outside the ±1-year window — including the key it just recorded — so the grid's callbacks read the default `{ error: "" }` and show neither the ⚠ marker nor the status line. Paging 24+ months ahead while offline renders a bare month indistinguishable from "no holidays in this country". Prune before recording (or exclude the just-recorded key), or widen the ledger window to track the in-memory LRU (`MAX_CACHED_YEARS = 8`) rather than the persist window; add a test for a failing year outside the persist window. | **[verified]** Node repro with the real modules and an always-failing chain: `getHolidays(currentYear, …)` reports "Holiday data unavailable"; `getHolidays(currentYear + 2, …)` reports `""`. |
| T534 | reliability / correctness | Low | open | S | Nothing time-driven re-renders the 42-cell grid, so a popup left open across midnight keeps yesterday's cell highlighted as today: `today` is only recomputed in `Calendar._update()` (menu open, settings/locale changes, events-manager signals), while the per-minute tick (`appletPanelStatus.js:642-695`) updates the header labels — its `getFormattedToday()` cache key even rolls over exactly at local midnight — but never queues a grid update. Hook the key rollover to a port that calls the calendar refresh and `events_manager.queue_reload_today(false)`; fires once a day, only while the presenter is already ticking. | Code-path audit: no clock-driven caller of `_update`/`_queue_update` in `calendar.js`; `queue_reload_today` is wired to EDS signals only. |
| T535 | legacy / deprecation | Low | open | S | `files/chronos@geraldo-netto/localeUtils.js` is an orphaned compat barrel shipped in the Spices package: it re-exports localeText/localeQuery/dateFormats and claims (lines 39–41) "the aliases are how they reach a GJS consumer", but its last production consumer (the `utils.js` barrel) was deleted in `52bbd64` — zero production importers remain, the file is never evaluated in GJS, and it ships as dead code. Delete it and repoint the barrel assertions in `test/utils.test.js`/`test/gjs_import.test.js` at the three modules directly — or record a keep decision here and fix the stale in-file claim. Also fix the stale comment at `test/schema_static.test.js:412` ("localeUtils binds the textdomain" while asserting `localeText.js`). | **[verified]** `git grep`: every production "localeUtils" hit is a boilerplate header comment; callers are the two test files only. Beyond D02's scope — D02 accepts test-only exported *names* on live modules, not whole caller-less modules. |
| T536 | legacy / deprecation | Low | open | S | Stale legacy rationale in `scripts/package-spices.mjs:72`: the comment justifying the dereference-in-tree-symlink branch says "the repository still contains one legacy icon link", but `2cd8da0` deleted it — the tree has zero tracked symlinks (`git ls-files -s` shows no mode-120000 entries), so the branch's stated caller is gone and only test fixtures exercise it. Reword as a defensive guard with no current caller, or simplify to reject all source symlinks outright and adjust the two fixture tests. | Per the ledger's legacy rule, a live legacy branch must record its real caller; this one records a caller that no longer exists. Coordinate with T532, which extends the same tests. |
| T537 | code complexity | Low | open | S | The walker the complexity gate relies on has two blind spots: (1) `complexityOf` walks only `functionNode.body`, so branching in default parameter values scores 0 — an arbitrarily guard-laden default is invisible (`function f(a = p ? q ? r ? s : t : u : v, b = x && y \|\| z) {}` scores 0; the identical logic in the body scores 9); (2) `testBodies` (`cognitive.js:102-105`) matches only bare `test(` Identifier callees, so `test.skip`/`test.only`/`t.test` subtest bodies are never scored. Walk `functionNode.params` defaults with the same `score()` pass, match MemberExpression test callees, and add both shapes to the walker's self-test. | **[verified]** Both measured with the walker itself. Today's defaults are benign (`updateClocks(entries = this.getClockEntries())` etc.); the fix is to keep them that way. |
| T538 | memory and CPU management | Low | open | S | `textUtils.js:24 clampText` materializes `Array.from(source)` — a fresh code-point array — on every call, including the dominant case where the string is under the cap and returned unchanged; it sits on the hottest path (panel label every `notify::clock` tick, per-second with seconds enabled, inside the compositor) and runs per event summary and per holiday row during 4000-row cache loads. Add the one-line fast path `if (source.length <= maxLength) return source;` — sound because UTF-16 length ≥ code-point count. | **[verified]** Micro-benchmark against the real module: 128 ns/call with an allocation per call vs 2 ns/call zero-alloc with the fast path. Behavior preserved exactly. |
| T539 | documentation | Low | open | S | `CHANGELOG.md` Unreleased is missing the user-visible icon change from `2cd8da0`: the new original applet icon, and the fix for zip installs receiving a corrupt 12-byte icon via the tracked symlink. The release policy records user-visible changes as Unreleased bullets, and the flow enforces a non-empty Unreleased at bump time. Add the two bullets. | Unreleased currently lists only the Node 22 requirement. |
| T540 | documentation | Low | open | S | `agent-instructions/README.md:3` opens "extracted, composable instruction modules from `old/`", but no `old/` directory exists in the tracked tree — a dangling provenance reference for anyone composing an AGENTS.md from these modules. Drop the clause or state the actual provenance. | Rest of the file checks out: all 20 listed domain files exist and every tracked module is listed. |
| T541 | configuration discoverability | Low | open | S | The country tooltip's privacy disclosure (`5.4/settings-schema.json:163`) says the lookup "sends the selected country to a third-party holiday service" (singular), but since `122d746` the fallback chain can contact up to four services, each receiving the country; README already uses the plural. Reword the tooltip, regenerate the pot, and merge the catalogs. | Bundle with T527 (same msgid-churn cost); with CalDays removed the chain contacts up to three services. |
| T542 | release & deploy engineering | Low | open | S | The CI gates matrix tests Node 22 only (`ci.yml:24`) while `package.json` engines declares an unbounded `>=22` and development runs Node 24 — `70a5f12` dropped the dual-version matrix whose stated purpose was exercising both the floor and the dev runtime, and node:test coverage semantics differ across majors, so local-green vs CI-green can diverge silently. Add 24 to the matrix, or bound engines and document 22 as the sole supported tooling runtime. | The removed comment in `70a5f12`'s diff documents the lost intent. |
| T543 | release & deploy engineering | Low | open | S | The artifact handoff name embeds `github.run_attempt` (`ci.yml:89` upload, `:112` download), so after a transient release-job failure "Re-run failed jobs" requests `chronos-spices-<sha>-<N+1>` — a name never uploaded, since the succeeded packaging job is not re-executed. The natural recovery path for a flaky release job is guaranteed to fail; only "Re-run all jobs" works. Drop `run_attempt` from the name, or document the constraint. | Fail-closed (never ships a wrong artifact), hence Low. |
| T544 | release & deploy engineering | Low | open | S | `bumpRelease` has a validator/rewriter asymmetry plus non-atomic output: validation accepts the `[Unreleased]` compare link via unanchored `String.includes` (`release.mjs:68-71`) but the bump rewrites it with a line-anchored regex (124–128) whose miss is a silent no-op — a line that passes validation but misses the anchor (trailing whitespace) yields a half-bumped changelog written with exit 0; the four sequential `writeFile` calls also leave version owners disagreeing on a mid-bump crash. Re-validate the rewritten content before writing (throw when the link regex does not match) and add the silent-no-op regression test. | Downstream `release:check` catches both states (fail-closed), but the bump itself reports success on inconsistent output. Overlaps T530's test additions. |
| T545 | dependency | Low | open | S | Both devDependencies are one major behind: eslint 9.39.5 (latest 10.7.0) and @eslint/js 9.39.5 (latest 10.0.1); the `^9` carets never pick up 10.x, and an out-of-support major eventually stops receiving rule and fix updates for the ruleset the eslint-gate test proves fires. Plan the upgrade (flat config already in use, migration should be small) or record a deliberate pin decision here. | `npm audit` reports 0 vulnerabilities and the lockfile is healthy (v3, registry.npmjs.org only, integrity on all 86 entries) — maintenance drift, not exposure. |

## Clean categories

Verified on the 2026-07-16 rescan and re-traced on 2026-07-18 (weather schedulers, Nominatim queue, EDS retry chain, event batching, teardown, worldclock timezone handling, cache-file safety, CI pins all held); caveats point to the corresponding open row:

- **Secrets, TLS, injection.** No credential in the tree; all external services are keyless. Every runtime endpoint is `https://`, and `ioUtils.js:347` cancels a request *before* a redirect to plain `http` goes out, because the query string carries the user's location. No `set_markup`/`use_markup` anywhere; Cinnamon's own `Tooltip.set_text` calls `set_use_markup(false)`. Both inline-`style` sinks pass through `styleUtils.safeCssColor` — **fuzzed with 11 payloads** (`red; background-image: url(…)`, `)}*{background:red}`, `expression(alert(1))`, newline smuggling): every one returned `"transparent"`. Every subprocess is argv-form; `launchUuid` writes `"--uuid=" + uuid` as one argument so a feed-supplied UID beginning with `-` cannot be parsed as an option. No `eval`/`Function`/dynamic import; no SQL.
- **Untrusted-input bounds.** Body size capped twice (declared `Content-Length` refused before the read; the stream aborted at 4 MiB, which closes the chunked-transfer hole). Non-object JSON coerced to `null`. Every provider normalizer checks `Number.isFinite`/`Array.isArray` before use. Prototype pollution **tested**: a cache file containing `{"__proto__":{…}}` leaves `Object.prototype` untouched; the event list already uses `Object.create(null)` for feed-supplied UIDs. The disk loader also caps accepted holiday rows before expanding or indexing them.
- **Cache file safety.** Fixed filename under `GLib.get_user_cache_dir()`; no user or remote input in the path; dir `0700`; `REPLACE_DESTINATION` unlinks-and-recreates rather than writing *through* a planted symlink; etag-guarded merge with a bounded retry loop; every row re-validated on load, including the future-stamp check.
- **Egress containment and disclosure.** Weather is off by default; holidays may auto-enable from the local timezone and can be disabled with `None`. The world-clock **display name never leaves the machine**: only the IANA-derived city is geocoded, so a clock labelled "Mom's place" stays local. Calendar event summaries and hostnames never leave. Every log line is stripped of its query string (`_urlForLog`). README's egress list is an **exact** match for the 9 hosts in the source.
- **Timers, signals and teardown *of resources*.** Drove both weather providers through `destroy()` with requests in flight and retries armed, under a fake GLib loop tracking every id: zero leftover sources, zero `source_remove` on an unknown id, zero late callbacks. Generation counters abandon superseded work. Cinnamon's `XletSettingsBase.finalize()` covers the settings signals. (What teardown does **not** do is release the *data* — T463.)
- **Backoff and retry.** 24 h of simulated total failure: 54 attempts, no runaway; `backoffDelay` caps at the refresh period and adds jitter on top of the cap; `MAX_RETRY_ATTEMPTS = 8`; every HTTP path sets `timeout` + `idle_timeout` = 30 s. The holiday state machine never wedges: 20 rapid `_update()`s after a total failure issue 0 extra fetches.
- **Main-loop hygiene.** Startup tzdata inference is deferred out of applet construction. No sub-minute periodic timer: `notify::clock` fires only when the rendered string changes (0 emissions in 10 s with `%H:%M`). The idle tick is **1.3 µs, zero allocations**. The 42 grid cells are built once and mutated; dots are diffed by key; every label write has an equality guard. Heap delta after 200k `_update()` calls: **98 KiB** (flat). No `Intl.DateTimeFormat` in any loop.
- **Settings-schema → runtime parity.** All 17 keys are bound and acted on; all 60 locally declared countries agree across schema, `SUPPORTED_COUNTRIES` and `COUNTRY_TO_ISO2`; `has_region`'s 10 entries match `REGION_TO_SUBDIVISION`; every region option has a subdivision mapping and there are no orphan table entries; `MAX_CLOCKS = 8` agrees across `worldclockData.js`, `cityWeather.js` and `settings_widgets_common.py`; every `dependency` in the schema is honoured by Cinnamon for custom widgets too.
- **Keyboard navigation of the day grid.** Roving focus (one tab stop), arrows/PageUp/PageDown/Home, RTL mirroring of Left/Right, focus moved into the grid on menu open, focus rings for every focusable actor. Genuinely good.
- **The gates themselves.** The JS coverage gate holds every file to 98/90/100 *and* globs the shipped tree from disk to fail any `.js` no test loads — a hole was looked for and not found. The eslint gate lints real text through the real config to prove `no-unreachable`/`no-dupe-keys`/`no-cond-assign` fire. `lint:py` genuinely exits 1 when pyflakes is missing. The `.pot`'s **msgid set is complete** (zero missing across all JS, the schema and both Python files). No `Lang.bind`, `imports.byteArray`, `toLocaleFormat`, `Soup.SessionAsync` or `queue_message` anywhere.
- **Not applicable**: database / migrations, multi-tenancy, Electron, Rust, ML / retrieval / RAG, vectorization, CLI surface, SQL injection, CORS/CSRF, prompt injection.

## Rejected

| id | finding | why rejected |
|----|---------|--------------|
| R01 | `dtEquals` duplicated between `eventData.js` and `eventFormat.js`. | Deliberate boundary: `eventFormat.js` stays free of GJS imports so it is Node-pure. Centralizing re-couples the modules for a one-line function. (Re-raised by the 2026-07-13 architecture scan; re-rejected.) |
| R05 | Barrel re-exports as the GJS `var` bindings importers need. | The re-exports **are** the import mechanism; tests reach the modules through them. |
| R06 | `eventView.js:70`: commented-out `Util.trySpawn(["gnome-calendar"])`. | Deliberate documented switch with a maintainer note directly above it. |
| R08 | No watchdog/metrics/circuit-breakers/health surface. | For a panel applet these are reasonable omissions; the provider failover with last-success ordering plus a per-year freshness gate is a coherent substitute. |
| R09 | Per-calendar colour strip conveys calendar identity by colour alone. | Not actionable here: `cinnamon-calendar-server` sends the calendar's colour and never its display name, so there is no name to announce. The strip is `Atk.Role.SEPARATOR` so a screen reader skips it. Needs an upstream change. |
| R10 | Panel label written with no compare-before-assign. | **[verified]** The tick is not 1 Hz: `WallClock` emits `notify::clock` only when the rendered string changes (0 emissions in 10 s with `%H:%M`), so the handler never sees the same string twice. |
| R11 | `validDateParts`' **year** and **day** integer guards look unasserted (both mutants survive). | **Equivalent mutants.** An exhaustive witness search over 15 hostile value types found no distinguishing input: `d.getFullYear() === parts.year` and `d.getDate() === parts.day` compare strictly against the raw value, so the roundtrip check subsumes them. Only the **month** guard, whose comparison coerces (`"5" - 1`), is load-bearing — that one is T470. |
| R12 | The suite depends on the host timezone. | Ran the full JS suite under `TZ=Pacific/Auckland`, `TZ=America/Sao_Paulo`, `TZ=UTC`: 738/738 in all three. `test/helpers/clock.js` freezes the wall clock and the tests pin `LOCAL_TIMEZONE` explicitly. |
| R13 | `metadata.json` is missing `icon` / `author`. | Cinnamon's `requiredProperties` for an applet are `uuid`, `name`, `description`; `author` belongs in `info.json` per the Spices spec, and is there. `max-instances: -1` is valid. |
| R14 | The `5.4/` shims are indirection; the root modules "depend on files that only exist in the parent". | Intended and correct on every declared version: `appletManager.js:61` roots `imports.ui.appletManager.applets[uuid]` at the *applets folder*, so `[uuid]` is the parent dir. The seven-line shims work, and `gjs_import.test.js` pins them. (The forward-compat risk is T464, which is about `require`, not the shims.) |
| R15 | `settings_widgets_common.py` at 991 lines is a god module. | Many concerns, but they are all "the settings dialog"; the gi-free half is already extracted into `timezone_data.py`, and no change was identified that becomes dangerous because of the co-location. |
| R16 | `metadata.json` omits Cinnamon 6.6, so the applet is incompatible with the installed desktop. | Rejected after checking Cinnamon 6.6.7's loader: `cinnamon-version` entries are minimum compatible versions, and the declared 5.4 entry admits 6.6. The compatibility test now models that loader rule directly. |

## Recorded decisions

| id | decision |
|----|----------|
| D01 | Keep the third-party endpoints rather than a bundled data set or a single vendor. A bundled table goes stale and covers fewer countries; a single provider breaks whenever it is down — the fallback chain exists because the primary is a hobbyist service. Risk accepted with mitigations (weather opt-in; one-time local-tzdata holiday default with visible third-party disclosure and explicit disable/override; size caps; bounded spans; shape checks in and out of cache; colour filtering; no URL logging). |
| D02 | **Keep the widened export surface.** ~80 exported names have no cross-module consumer — used inside their own file and otherwise only by `test/`. The applet runs inside a compositor where a bug costs the whole session; testing only through the composition root would be slow and flaky. **This does not license the inverse:** production must not bend to a test double's shape. |
| D03 | **`author` is the display name "Geraldo Netto", not the GitHub handle.** `info.json`/`package.json` use "Geraldo Netto"; `schema_static.test.js` asserts it. The Spices convention prefers the handle, but the display name is the maintainer's explicit choice. |

## Resolved merge choices

Decisions from the calendar@ccprog / Simon Wiles merge. Kept so future audits do not re-pick them.

| id | resolution |
|----|------------|
| G01 | Kept the `calendar@ccprog` world-clock settings workflow (Cinnamon-native settings, up to 8 user clocks plus built-in UTC/local, region/city selection, IANA/city entry, timezone validation, inherited coverage). Simon Wiles' separate GTK editor remains unported. |
| G02 | Kept `calendar@ccprog` locale-aware weekend behaviour (locale-derived work weeks plus a configurable one- or two-day weekend length). |
| G03 | Kept the copied `calendar@ccprog` visual assets as the initial merged set. The Chronos screenshot was refreshed on 2026-07-16; the icon was replaced with an original design and the orphaned root `calendar.png` deleted on 2026-07-18. |
